# FASTTRACK — the Fable-window execution plan (2026-07-03 → 07-06/07)

> **What this is.** The operator has a 3–4-day window of Fable-model access and wants every remaining
> workstream planned in executable detail NOW, so architect/orchestrator/executor agents can build in
> parallel without re-deriving context. This document is the single source of truth for that sprint.
> Written 2026-07-03 ~13:20Z by the incident-watch session, immediately after the post-restoration
> steady state was reached.
>
> **What cannot be compressed (say it once, plan around it):** the live forward maker-exit paper gate
> (`/maker-exit`, the gate of record) needs **≥40 markets / ≥6 cities / ≥7 distinct days** of accrual —
> calendar time driven by market resolutions, not tokens. It will NOT adjudicate inside the Fable
> window (currently 8 markets / accruing since 06-30). The window is therefore spent on everything
> *around* it: the structural data-path fix, the backlog falsifications, the backtest refinements, and
> a handoff so a lighter model (or the operator) can read the gate when it matures.
> **No capital before a frozen paper PASS — the model window changes nothing about that boundary.**

## Hard rules for every agent in this sprint

1. **The DB is the shared fragile resource.** One heavy DB operation at a time, project-wide. The
   maker-exit panel tick (hourly at :35, ~6 min) is a blackout window for heavy reads. Two incidents
   today came from violating exactly this. Heavy = anything touching `opening_captures` TOAST,
   archive-scale pulls, or the sim cache build. Coordinate via the orchestrator; never assume.
2. **Worktree isolation per executor agent.** The main tree carries uncommitted signal-backlog engine
   work. `BUILD-STATE.md`, `FINDINGS.md`, `MAKER-EXIT-SIM.md`, `SIGNAL-BACKLOG.md` are single-writer:
   only the orchestrator/integration pass edits them.
3. **Frozen-gate discipline everywhere.** Pre-register the gate before measuring. Every signal test
   reports vs its pre-registered criteria; INSUFFICIENT_DATA is an honest outcome.
4. **Operator boundary unchanged.** No keys, no capital, no trades. Migrations + fn deploys + any
   destructive SQL are operator-gated: build + test + stage everything, then present a one-command
   bundle with verification steps.
5. **Suite + typecheck green before any commit; commits local unless told.**

## Current state (2026-07-03 13:20Z, verified live)

- `opening-capture` `*/10` — healthy (32–38s, full inserts). `maker-exit-panel` hourly `35 * * * *` —
  ticks complete, snapshots land, but **cityErrors ~41/45** (see WS-1 root cause). `convergence-panel`
  **paused** (v6 unbounded fetch — must not re-enable before WS-1). `whale-watch` + all light crons healthy.
- `bot.consensusSource` = `calibrated` (the queued flip is DONE). `bot.cities` = 45. `bot.gateStaleMin`
  180. `bot.tickStaleMin.paper` = **180 while hourly** (restore '45' when the panel returns to `*/15`).
- **Root cause found (WS-1):** `opening_captures` = 46 MB heap + **1.2 GB TOAST** (`buckets` jsonb).
  A 45-city × 21-day panel tick detoasts ~1.2 GB — bigger than the buffer cache → every tick is
  disk-bound; 40 × 30s-timeout statements/tick was eating the IO-burst refill. The 45-city panel has
  NEVER had a clean tick; today's incident exposed it.
- Forward gate: INSUFFICIENT (8 mkts / accruing). 07-02 grading 29/45 (venue resolutions pending —
  gates WS-2). Signal backlog: 1a answered (partially eligible → 1b live), 1b + 5 engine code BUILT
  (uncommitted, tested, 996 core tests green), 7 KILL (data structurally impossible keyless),
  9 KILL ($802/24h vs $7k floor).

---

## WS-1 — Panel data-path structural fix — **✅✅ DEPLOYED + VERIFIED 2026-07-03 15:20Z** (operator approved; outcome block + 3 evidence-based deviations in `docs/ops/WS1-ROLLOUT.md`: canary >60 s→instant, convergence-panel live again at 23.2 s/0 errors, gate scope 43–58 mkts/tick, maker-exit stays hourly, prune correctly empty until ~late July, vacuum deferred)

> 2026-07-03 ~14:00Z: 3-lens adversarial review × 2 rounds → 4 confirmed findings fixed (headline: the
> dropped 0069 last-tick invariant would have fired the gate-of-record's time-stops up to ~40 min late —
> caught by the SQL lens, fixed as a dual-window rule), 1 false-positive overturned (BUILD-STATE staleness,
> since remedied), 0 outstanding. Combined-tree suite 1904 green + 1 pre-existing real-timers flake
> (passes 2× in isolation; fake-timers fix added to WS-5). Worktree branch `worktree-agent-a12af96a47327ece2`
> commit `0a8896c`; main-tree local commit `52f115d` (signal-backlog work left uncommitted, untouched).

**Goal:** a maker-exit panel tick that completes in ≤120s with cityErrors ≤2 at 45-city scope, and a
convergence-panel that can safely reactivate. Kill the incident class, not the symptom.

**Deliverables (in dependency order):**

1. **Migration `0077_capture_read_thinning.sql`**
   - `convergence_capture_inputs(p_days, p_cities)` gains server-side **20-min grid thinning**: per
     event, keep one capture row per 20-min bucket (deterministic pick: min `captured_at` per bucket),
     using the 0076 slim-window-then-PK-join pattern so the thinning predicate never detoasts skipped
     rows. The replay engine already samples on a 20-min grid (`SAMPLE_MIN = 20` in
     `scripts/research/sim-maker-exit.ts`), and capture runs `*/10` → this halves (at `*/2` capture:
     ~10×) the detoasted volume with **zero fidelity loss to the replay**.
   - Signature/grants byte-compatible (additive default param if a raw mode is worth keeping for the
     Phase-3 backtest reader — decide in review; `bot_capture_series` stays untouched).
   - PGlite twin in `supabase/tests/migrations.test.ts` mirroring the 0068/0073 twin pattern: thinned
     row-count assertions + the {rows:[…]} wrap lock + grant checks.
2. **`opening_captures` retention prune (operator-gated, destructive — dry-run first)**
   - Policy: delete capture ticks for events **resolved ≥25 days** (> PANEL_DAYS 21; the on-disk
     price-history archive is the permanent record).
   - Pre-flight (MANDATORY): assert every prune-candidate `eventId` exists in the local archive index
     (`indexArchive` over the archive root) — no archive file, no delete. Emit the dry-run count +
     size estimate; operator approves; then batched deletes (≤5k rows/statement, off the :35 window).
   - Follow with `VACUUM ANALYZE public.opening_captures` (operator-gated, IO-heavy, quiet window).
     Collector shows autovacuum never ran post-incident.
3. **`maker-exit-panel` v5** — consume the thinned RPC (likely no handler change if the RPC keeps its
   signature; verify), keep the v4 bounded pool as belt-and-braces. After ONE verified clean tick:
   restore cadence `5,20,35,50 * * * *` + set `bot.tickStaleMin.paper` back to `'45'`.
4. **`convergence-panel` v7** — port v4's bounded worker pool (concurrency 5 / 30s per-city / 240s
   budget / partial-view degradation) into the convergence handler + the thinned RPC. Re-enable
   `*/15` (permanently offset from maker-exit). Twin tests mirror the maker-exit handler tests.
5. **Rollout runbook (operator bundle):** apply 0077 → deploy maker-exit-panel → verify a :35 tick
   (target: <120s, cityErrors ≤2, snapshot + gate land) → deploy convergence-panel v7 → re-enable
   job 26 → verify one tick → prune dry-run → operator yes/no → prune + vacuum → restore maker-exit
   `*/15` + tickStaleMin 45. Each step has a verification query; stop on any regression.

**Effort:** ~half a day of agent work + review. **DB-load:** the migration itself is cheap; the prune
and vacuum are the heavy steps (operator-scheduled).
**Review:** the project's proven 4-lens adversarial pattern (migration SQL · edge pipeline · pure
core · tests) — one round minimum, two if findings are non-trivial.

## WS-2 — The true 21-day baseline — **✅ DONE 2026-07-03 13:41Z** (07-02 graded 45/45 → 844 ev/45 c/21 d: **PASS holds** +6.8 %/+$535, CI [+0.25 %, +11.9 %]; LOCO 15/45, LODO 9/21, day-block PASS. Banner + BUILD-STATE updated. **The new cache unblocks WS-3b.**)

**Trigger:** 07-02 graded ≥44/45 (`select count(*) … market_events … target_date='2026-07-02' …
winner not null`). If one market is UMA-stuck for hours, proceed at 44 with a note.
**Steps (serialize with the :35 tick window and with WS-3c — ONE heavy pull at a time):**
1. `pnpm tsx scripts/research/sim-maker-exit.ts --build-cache` (the heavy pull; ~849 events expected).
2. Pinned baseline: `--from-cache --split --tp 0.12 --sl 0.20 --tstop-hours 18 --chw 0
   --max-entry 0.30 --depth 150 --maker-window 30 --rebate 0`.
3. `jackknife-maker-exit.ts` (LOCO/LODO + day-block).
4. Update the `MAKER-EXIT-SIM.md` top banner + BUILD-STATE morning-summary item 2 + memory.
**This cache is also the substrate for WS-3b — build once, share.** The prior banner (20-day):
+6.9%/+$534, CI [+0.4%,+12.1%], PASS. The question is only whether the PASS holds/strengthens at 21.

## WS-3 — Signal-backlog execution — **✅✅ CLOSED 2026-07-03 ~19:50 local (orchestrator /loop v2, C1–C13 below): all 9 verdicts adjudicated vs pre-registered gates — 1b gate-PASS (pool-share caveat → forward instrumentation follow-on) · 2/4/5/6/7/9/11 KILL · 3 NO-PASS (hardening revoked) · 10 INSUFFICIENT structural. Records: FINDINGS.md rows + SIGNAL-BACKLOG.md verdict blocks + BUILD-STATE 17:45Z addendum.**

- **3a — ✅ DONE (2026-07-03 ~15:08 local, parallel agent):** CLI wiring for `rewardCfg` + `basketSize`
  landed; items 2–4 pre-registered with the analysis script written and its SQL staged-not-executed.
  57 test files / 1159 tests green, whole-repo typecheck clean, prod untouched, uncommitted (6-file
  diff). State: `SIGNAL-BACKLOG.md` + the 15:08 memory entry.
- **3b (after WS-2's cache):** run 1b — pinned config ± reward income, `myPoolShareIfQualifying`
  swept {0.05, 0.10, 0.25, 0.50}; gate: does ciLow move materially toward/past 0 with everything else
  frozen. Run 5 — basket 1/2/3, judged on **jackknife fragility** (count of LOCO/LODO flips), not mean.
- **3c (after the orchestrator's stability green light, ~14:00Z):** items 2–4 as ONE data pull + three
  conditioning splits. Heavy read — schedule off the :35 window, never concurrent with WS-2's step 1.
- **3d (after 3c):** item 6 cheap check (adjacent-day join). Expected KILL per the backlog's own
  downgrade; budget minutes, not hours.
- **3e (only if time remains):** item 10. Item 8 stays dormant (needs a live PASS first).
- **Recording:** every verdict → a FINDINGS.md row + SIGNAL-BACKLOG.md status flip, via the
  single-writer integration pass. Items 7 + 9's KILLs are already due for recording.

## WS-4 — Live-loop alignment — **DONE** (verified this session)

`bot.consensusSource` = `calibrated` ✅ · 45-city panel scope deployed + verified ✅. Nothing remains.

## WS-5 — Ops hardening — **✅ BUILT + REVIEWED + INTEGRATED (local `6c053f1`); rides the combined operator bundle**

> 2026-07-03 ~14:35Z: terminal-write retry (2×, 3s/8s, 15s/attempt — panel snapshots only; never-pruned
> gate history explicitly NOT retried), 10s-bounded bookkeeping writes (complete wall arithmetic: 346s
> worst case vs the 400s wall), 0078 `claim_job_run` janitor (dead-isolate `running` rows self-heal at
> next claim; grants re-asserted per the 0046/0047 idiom), fake-timers determinism for the two flaky
> budget tests (3× proven). 2-lens review → 1 MEDIUM + 2 LOW confirmed → fixed → 0 outstanding.
> Combined tree 135 files / 1928 green, zero-flake. Deploys via the SAME bundle as WS-1
> (`docs/ops/WS1-ROLLOUT.md`, updated `c0892ec`).

1. **Terminal-write retry in the panel handlers** (or `runJob` helper): 2 retries + short backoff on
   `record_maker_exit_panel` / gate-snapshot writes — one transient timeout must not discard a
   6-minute tick. (Today's failures were pooler-jam, but the resilience is correct regardless.)
2. **Wedged-row janitor:** at job start, mark own-job `running` rows older than 30 min as `failed`
   (forensic noise class — 4 wedged rows today).
3. **Deadman audit:** thresholds now cadence-aware and verified paging correctly (they paged the
   operator correctly overnight). After WS-1 restores `*/15`, restore `bot.tickStaleMin.paper='45'`.

## WS-6 — Docs, memory, and the post-Fable handoff

1. BUILD-STATE addendum for today's second incident + the TOAST root cause + the steady state (owner:
   this session, today).
2. FINDINGS.md: backlog rows for 7 (impossible keyless) + 9 (liquidity KILL); later 1b/5/2–4/6 verdicts.
3. **`POST-FABLE-HANDOFF.md`:** the one-page adjudication guide — when the forward gate hits
   ≥40/≥6/≥7 (arithmetic at current accrual: ~07-08 to 07-10), run `dash_maker_exit` / read
   `/maker-exit`, the §9R-E function renders the verdict deterministically; what PASS/KILL each mean
   (PASS → operator decision on the §9R capital scope; KILL → the 12th signal joins the other eleven,
   rail back to DORMANT); plus the restore-state table (every cron/config knob touched this sprint and
   its steady-state value).
4. `/remember` at every session end.

## Day-by-day

- **Day 0 (today):** WS-1 build + review (agent team, worktree) · WS-3a now · WS-2 when grading lands ·
  WS-3c after stability green light · BUILD-STATE addendum · evening: WS-3b if the cache is in.
- **Day 1:** WS-1 operator bundle (0077 + deploys + verified restoration + prune decision) · WS-3b/3d
  runs · WS-5 build + review.
- **Day 2:** consolidation — FINDINGS/SIGNAL-BACKLOG/banners; WS-3e if time; WS-5 deploy; buffer
  (today proved surprises cost half a day).
- **Day 3:** POST-FABLE-HANDOFF.md · final memory entry · leave the system accruing.

## Future-build / orchestration plan — OPUS orchestrates from here (no Fable)

> Written 2026-07-06 (C79). The v6–v13 loops were orchestrated by **Fable** with **Opus** subagents.
> Going forward the operator wants the loop **orchestrated by Opus directly** (Sonnet-5/Opus cost parity
> makes Fable-for-orchestration unnecessary; Fable was only the cheap driver). Practical implications for
> whoever runs the v14 loop:
> - **Model:** run the main loop on **Opus 4.8** (already the session default). Subagents also Opus. There is
>   no Fable in the pipeline anymore — ignore any "Fable/Sonnet agents" phrasing left in older prompts.
> - **Cadence:** Opus is more expensive per token, so lean the watch cadence LONGER (steady wakeups 2700–3600s;
>   1500–1800s only when a tick/verification is genuinely in flight). Don't burn cycles re-reading state that
>   the durable files already hold — `FASTTRACK-PLAN.md` (this file), `BUILD-STATE.md`, `git log` ARE the state.
> - **Scope discipline:** the signal backlog is CLOSED. The remaining work is (a) finish the gate adjudication
>   (below), (b) steady-watch the shadow week + daemon, (c) execute operator-authorized deploys only. Do NOT
>   open new signal hunts or build lanes without an explicit operator directive (CITY-LIVE was one such
>   directive — it is now built + deployed dark).
> - **Deploy discipline:** every migration-apply / edge-fn-deploy is operator-gated. The MCP proxy is flaky;
>   the local ops toolkit (`scripts/ops/gate-read.ts`, `apply-migration.ts`, `sb.ts`) routes around it and is
>   the fallback when `mcp__…__execute_sql` 502s.
> - **The product thesis is unchanged:** twelve signals tested, all dead (the 12th — maker-exit — being recorded
>   now); the value is the analytics/insight layer. No taker-edge work without new out-of-market information.

## The autonomous /loop prompt — v16, LIVE KARACHI CITY-TAKER TEST (paste as-is after /clear; self-paced — no interval)

> Written 2026-07-07 (C97). The operator, having heard the WATCH-not-promoted verdict, authorized a LIVE
> real-capital test on Karachi (their sovereignty; the live test is the real gate). The rail is verified
> deploy-ready; the executor is the trade-bot DAEMON city lane (so the daemon must RUN — dry-run first).
> Boundary UNCHANGED: operator funds/keys/toggles/sets-live; Claude verifies + monitors + reports, never trades.

```
/loop Polyweather LIVE KARACHI CITY-TAKER TEST v16 (from 07-07; operator AWAKE + driving; **REAL CAPITAL authorized by the operator**). Orchestrate on OPUS. **SCOPED OPERATOR DIRECTIVE (supersedes the dormant-rail default):** the operator authorized a LIVE real-capital test on KARACHI — buy ONE city-taker stake/day at the best hour (recommended 14:00 Asia/Karachi = 09:00Z), hold to resolution, aim for profit. Operator sovereignty: promotion is ADVISORY, the live test IS the real gate. **BOUNDARY — NON-NEGOTIABLE, UNCHANGED: the operator physically funds a dedicated wallet + holds the signing key (.env.local, NEVER in chat) + sets TRADE_MODE=live + toggles the arm/config on /trading. Claude operates the software — verify, monitor, report — and NEVER places a trade, NEVER touches the wallet/key, NEVER sets TRADE_MODE=live, NEVER reads .env.local.** FILES ARE THE STATE — read FASTTRACK-PLAN.md C97 + docs/specs/CITY-LIVE.md (§2 preflight, §3 daemon city lane, §4 /trading).

HONEST CAVEAT (say ONCE, don't nag): Karachi is WATCH, NOT PROMOTED — 14:00 arm edge +6.4pp but edgeCiLo −3.1pp (CI includes zero), entry-watch provisional, net +$10.24/25d paper (only net-positive city but within 45-city selection-effect noise; paper = no fees). Real taker fee+spread may flip it negative (the same wall that killed the 12 signals). This is BUYING INFORMATION at small risk ($5/day, once/day) — NOT a proven edge.

STATE VERIFIED (07-07 C97): rail DEPLOYED — 0082+0085 applied; city_live_arms, trade_config, trade_live_preflight('city-taker'), dash_trading(), dash_city_live() all exist. **Executor = the scripts/trade-bot.ts DAEMON city lane** (Lane X: decideCityTick → FAK 'city-taker', once/city/day, shares=stakeUsd/ask, NO exit = hold to resolution; isolated from the dead maker-exit lane; wiring + city-live-decide.ts confirmed on main). TRADE_MODE ladder: default DRY-RUN; a live post needs BOTH TRADE_MODE=live AND trade_live_preflight('city-taker').ok; the wallet key lives ONLY in packages/trading/src/live.ts. **preflight('city-taker') does NOT require a gate PASS or promotion** — it checks (1) trade_config.mode='live', (2) active_until set AND ≥ today, (3) no daily-loss kill, (4) ≥1 enabled arm, (5) every enabled stake ≤$5, (6) ≤2 enabled cities. So Karachi-WATCH goes live purely by operator toggle (by design). Karachi tz Asia/Karachi = UTC+5, no DST: 14:00 local = 09:00Z.

**DAEMON MUST RUN — this REVERSES the C94 "daemon stopped" directive** (that stop was for the DEAD maker-exit lane; the CITY lane is now the point — maker-exit just no-ops). Relaunch in DRY-RUN first.

GO-LIVE SEQUENCE (in order; STOP if any check fails):
PHASE A — Claude, DRY-RUN verify (ZERO capital): ① relaunch the daemon dry-run detached: `set TRADE_MODE=dry-run&& set TRADE_TICK_SEC=60&& pnpm tsx scripts/trade-bot.ts >> out\trade-bot-detached.log 2>&1` (watch out/trade-bot-detached.log; suite+typecheck were green at C96). ② Operator enables KARACHI on /trading → City Live arms table: toggle on, stake $5, entry_hour_override 14 (or leave null to auto-follow the recommended hour). ③ At/after ~09:00Z (14:00 Karachi) verify the daemon logged a DRY-RUN city-taker Karachi intent (correct market / predicted-bucket / stake / ask, once/day, strategy='city-taker', mode='dry-run' in the ledger). This PROVES the path with ZERO capital. Report it. Do NOT proceed to live until a clean dry-run placement is observed.
PHASE B — operator, GO LIVE (capital, ALL physical; Claude only guides + verifies gate state, never executes): ① operator funds the dedicated wallet + puts the signing key in .env.local (NEVER chat); ② /trading config table: mode='live' + active_until (e.g. today+14d) + confirm stake / daily-loss-kill caps; ③ operator restarts the daemon with TRADE_MODE=live; ④ Claude verifies `select * from trade_live_preflight('city-taker')` returns ok:true (else fix the named reason first). Nothing places until all four hold.
PHASE C — Claude, DAILY MONITOR: each day after 09:00Z verify the live order placed (one live ledger row/day; live_orders / dash_trading()); track fill → resolution → realized P&L vs the paper prediction + the taker-vs-maker twin (dash_city_live twin cols: takerPnlUsd vs makerTwinPnlUsd, twinFilledFrac); report running P&L. Watch the daily-loss kill + active_until (expiry stops placement = expected, not a bug). Daemon-watch: out/trade-bot-detached.log LastWriteTime staleness = wedge → kill by EXPLICIT PID + relaunch SAME TRADE_MODE (never CommandLine match).

STOP CONDITIONS (any → tell the operator to set mode='off' on /trading + stop the daemon; do NOT debug with live capital): wrong stake, double-place, unexpected fill, daily-loss kill fires, preflight flips not-ok mid-run, or anything you don't understand.

GUARDRAILS: Claude NEVER places a trade / touches the wallet or key / sets TRADE_MODE=live / reads .env.local — operator-physical only. Deploys operator-gated. suite+typecheck green before CODE commits (docs-only exempt). subagents Opus. Signal backlog CLOSED — NO new signal hunts; this is the ONE authorized live test.

SECONDARY (light steady-watch — don't let it crowd the live test): forecasting pipeline is NOMINAL (self-healed 11:19Z 07-07); gate CLOSED (12/12 dead); opening_captures reclaimed to 75MB (C96); daily opening-captures-prune keeps the window trimmed. Spot-check poll-markets fresh + 0 errors occasionally; escalate only on a real new failure.

VERIFY HELPERS (MCP execute_sql, project lenysiqxihsmxljvyybt — ONE statement per call, it returns only the last): `select * from trade_live_preflight('city-taker');` · `select * from dash_trading();` · `select * from dash_city_live();` · `select * from city_live_arms;` · `select mode, active_until, stake_per_buy_usd, daily_loss_kill_usd from trade_config where id=1;` · Karachi board: `select jsonb_pretty(view) from city_promotion_board order by captured_at desc limit 1;`. Local fallbacks if MCP flaky: scripts/ops/sb.ts, gate-read.ts.

PACE: near the 09:00Z window tighten (600-1500s) to catch the placement; else steady 2400-3000s. C-tick on material change (first dry-run placement · go-live · first live fill · first resolution/P&L). WIND DOWN on the operator's word (final tick + /remember + summary ending with the live-test state + running P&L).
```

## The autonomous /loop prompt — v15, OVERNIGHT AUTONOMOUS GATE-RECORD + STEADY-WATCH (paste as-is after /clear; self-paced — no interval)

> Written 2026-07-07 ~00:45 local (C83 wind-down, operator to bed). The 07-06 evening was one long Micro-DB
> saturation: it 504'd prod (FIXED cd91d0a), blocked the gate clean-write, wedged the daemon (relaunched), and
> timed out the real-book sweep. All of it self-clears as European deep-night load drops — that is the overnight
> run's opening. The verdict is settled KILL; the job is to RECORD it on the first clean tick + run the deferred
> sweep. Operator is ASLEEP — surface morning decisions, never wait on him. Boundary unchanged: no capital, no
> keys, nothing live; the only sanctioned deploy path is web push-to-main/Vercel (used for the 504 hotfix).

```
/loop Polyweather OVERNIGHT AUTONOMOUS GATE-RECORD + STEADY-WATCH v15 (night 07-06→07-07; operator ASLEEP — never wait on him, surface morning decisions; capital/live OPERATOR-PHYSICAL; never set TRADE_MODE=live). Orchestrate on OPUS (no Fable). FILES ARE THE STATE — read FIRST: FASTTRACK-PLAN.md cycle log C76-C84 + the "Future-build / orchestration plan" note, docs/ops/GATE-DAY-PLAYBOOK.md, docs/specs/CITY-LIVE.md, MAKER-EXIT-SIM.md top banner (the backtest-vs-live root cause), task #63. NEVER query cron.job_run_details. DB Micro CONFIRMED (07-07: dashboard now officially reads Micro — the old Nano/Micro display mismatch is RESOLVED, close it) and was SATURATING at peak all evening (root cause of the whole 07-06 cluster). No resize pushes, but FLAG the item-③ downsize as reconsider→HOLD. MCP execute_sql TIMING OUT since ~19Z (and even the direct pool ECHECKOUTTIMEOUT'd at peak) — use local fallbacks: `pnpm tsx scripts/ops/gate-read.ts 5` (its "CLEAN" tag is a FALSE-POSITIVE on null-stats ticks — a real clean tick has non-null cErr≤2 AND gateSkip=none AND a KILL label), `scripts/ops/maker-exit-gap-read.ts` (panel decomposition), `apply-migration.ts`, `sb.ts`. Retry MCP for CI/net numbers as the DB eases.

TOP PRIORITY — RECORD THE KILL on the first clean overnight tick. The verdict is DEFINITIVELY KILL (last real panel 18:52Z: mean net -12.6%, CI [-21.6%,-3.5%] fully negative, 62 mkts/26 cities/7 days, makerFillRate 0.065 vs 0.49 backtest, rebate 0, net -$168); settled + robust across subsets (#63) — only the clean gate-row WRITE is infra-blocked (cErr>2 / null-stats every tick since 17:35Z under the peak load). FIRST ACTION each cycle: gate-read.ts 5. DECIDE: CLEAN (cErr≤2 AND gateSkip=none AND non-null KILL label → gate row WROTE) → ADJUDICATED KILL → execute GATE-DAY-PLAYBOOK.md Package 1 (KILL) EXACTLY, pre-authorized, LEAD with it: 1a FINDINGS.md 12th-signal closing block + summary-table row (~line 49) + fold the "one live exception" para into the now-unconditional bottom line + a "Where each finding lives" pointer (REFERENCE the MAKER-EXIT-SIM.md synthetic-vs-real-book banner as the mechanism/"why"); 1b CLAUDE.md pivot-block banner (newest entry); 1c SIGNAL-BACKLOG.md §13 re-open criteria. Pull REAL numbers first via dash_maker_exit() or maker-exit-gap-read.ts. Fill every placeholder. commit+push. Write the next C-tick. Present the closing verdict (twelve of twelve signals dead; rail DORMANT; analytics product stands). STILL degraded → note accrual + ride the next :35 cron; do NOT relax GATE_WRITE_MAX_CITY_ERRORS=2 (operator override only); do NOT re-fire at peak; do NOT deploy.

RESOLVED THIS SESSION (do NOT re-investigate): ① PROD 504 (MIDDLEWARE_INVOCATION_TIMEOUT) — was the middleware's unbounded `await supabase.auth.getUser()` hanging on the saturated Supabase. FIXED cd91d0a (2.5s Promise.race timeout, best-effort refresh; typecheck + suite 2845 green; page load VERIFIED). ② DAEMON — silent-wedged ~21:37Z → killed-by-PID + relaunched 21:46Z; verified ticking (0 failed, dry-run; discovery degraded + cadence stretching under load — self-heals as load drops). Resume normal daemon-watch (LastWriteTime staleness = wedge → kill by EXPLICIT PID, never CommandLine match → relaunch `set TRADE_MODE=dry-run&& set TRADE_TICK_SEC=60&& pnpm tsx scripts/trade-bot.ts >> out\trade-bot-detached.log 2>&1`). If it keeps wedging AFTER the DB eases, propose the discovery client-timeout code fix (the C76 no-timeout hang root; operator-gated CODE change). ③ Micro/Nano display mismatch — CLOSED (officially Micro now). ④ LOGIN magic-link "send link" failure (empty error / nothing happens) — diagnosed via get_logs auth as Supabase EMAIL RATE LIMIT (over_email_send_rate_limit + 15s per-email cooldown 429s from repeat clicks), NOT an outage (gateway 401 in 0.14s). Login UX HARDENED (apps/web/src/app/login/page.tsx: 12s send timeout + never-blank error + surface ?error=confirm expired-link bounce; suite 2845 green, pushed). Operator morning: wait a few min + click "send link" ONCE; optional Supabase Auth→Rate-Limits raise / real SMTP provider / verify redirect allow-list includes /auth/confirm.

OPEN RESEARCH ITEM (operator directive 07-06, deliver the grid to his morning): backtest-vs-live ROOT CAUSE established + recorded (SYNTHETIC house_gaussian book converges by construction = +6.7%; REAL efficient book doesn't = -12.6%; NOT an artifact — MAKER-EXIT-SIM.md banner, C81). The "can we adapt to boost it" empirical test = `pnpm tsx scripts/research/maker-exit-realbook-sweep.ts` (sequential; sweeps tp/tstop/tpMode/sl over the REAL 45-city book). It TIMES OUT at DB peak (a run returned 0 rows → its "no config clears" verdict is FALSE on empty data — NEVER report a 0-row run). WHEN gate-read shows the DB eased (non-null stats / low cErr), RUN it, verify captureRows>0 AND nMkt>0 before trusting ANY cell, and report the grid (strong prior: no config clears ciLow>0 — the real price doesn't converge to us).

EACH CYCLE ALSO: 1. DAEMON WATCH (per RESOLVED-② above; degraded≠wedged — only relaunch a STALE-log+procs-alive daemon, kill by explicit PID). 2. SHADOW-WEEK: live_orders growth / 0 failed (326 at C80; paused while the daemon discovers 0 candidates under load — resumes as the DB eases); trade-shadow-diff.ts first read once ≥1 full listing day of dry-run rows accrues (report the divergence table FIRST, never code-chase it unreported). 3. CITY-LIVE (0085 applied + fn redeployed C78): verify the 07-07 10:00Z city-paper-trade tick populates city_maker_twin + city_promotion_board (twinInserted/boardWritten non-null in stats) + dash_city_live() renders on /trading — report PROMINENTLY (first run of the redeployed fn); nothing trades until operator toggles a city + funds a wallet + TRADE_MODE=live + preflight('city-taker') PASSes. 4. GUARDRAILS: no capital/keys/live-mode; deploys operator-gated (web push-to-main/Vercel is the normal path); suite+typecheck green before CODE commits (docs-only exempt); subagents Opus; backlog CLOSED, NO new signal hunts. 5. RECORD+PACE: C-tick on material change; wakeups 2700-3600s steady / 1500-1800s in flight; WIND-DOWN on the operator's word: final tick + v16 prompt + /remember + summary ending with operator items.

Operator MORNING items: ① GATE — if a clean tick landed overnight the KILL is RECORDED (read the latest C-tick); if still degraded, verdict is settled KILL awaiting the write (or say "record it now"); ② RESEARCH — the real-book sweep grid (or, if the DB never eased enough, it's still pending); ③ COMPUTE — RECONSIDER the planned Micro downsize: 07-06's prod 504 + gate/daemon/sweep degradation were ALL Micro saturation at peak → HOLD or temp-upsize during high-traffic windows, do NOT downsize; ④ CITY-LIVE deployed dark — toggle a city on /trading + fund a wallet + TRADE_MODE=live for the live city-taker test (quadruple-gated); ⑤ live-smoke re-run with --token (v2 write-path proof; refresh a token from opening_captures if stale); ⑥ opening_captures VACUUM/prune late-July; ⑦ pg_net + MCP-proxy incidents (07-06) — close tickets once MCP execute_sql is stable; ⑧ 10 pre-session worktrees under .claude/worktrees (clean when convenient).
```

## The autonomous /loop prompt — v14, OPUS GATE-ADJUDICATION + STEADY-WATCH (paste as-is after /clear; self-paced — no interval)

> Orchestrated by Opus (no Fable). First job: finish the maker-exit gate adjudication — the two-part co-tune
> (FETCH_CONCURRENCY 5→3 d81e93a + FETCH_BUDGET_MS 240→270 efe0436) is DEPLOYED; the verification tick (req 827,
> :45 slot) was in-flight at the C79 handoff. Read it, and if clean, record the KILL.

```
/loop Polyweather OPUS GATE-ADJUDICATION + STEADY-WATCH v14 (from 07-06; operator reachable; capital/live OPERATOR-PHYSICAL; never set TRADE_MODE=live). Orchestrate on OPUS (no Fable in the pipeline). FILES ARE THE STATE — read FIRST: FASTTRACK-PLAN.md cycle log C76-C79 + the "Future-build / orchestration plan" note, docs/ops/GATE-DAY-PLAYBOOK.md, docs/specs/CITY-LIVE.md, task #63. NEVER query cron.job_run_details. DB Micro ACTIVE; no resize pushes. If MCP execute_sql 502s use local fallbacks: `pnpm tsx scripts/ops/gate-read.ts 3` (gate reads, CEST=UTC+2), `apply-migration.ts <file>`, `sb.ts <cli-args>`.

TOP PRIORITY — FINISH THE GATE ADJUDICATION. Both co-tune halves are DEPLOYED (maker-exit-panel FETCH_CONCURRENCY 5→3 + FETCH_BUDGET_MS 240→270). The 16:35Z cron proved 5→3 works: cErr 24→6, timeouts 14→0, nMkts 44→78; budget bump targets the last 6 budget-skips. FIRST ACTION: read the latest maker-exit-panel tick (the 16:52Z req-827 :45-slot re-fire, else the next :35 cron): SELECT started_at,duration_ms,status,stats->>'label',stats->>'cityErrors',stats->>'nMarkets',stats->>'budgetSkipped',stats->>'makerFillRate',stats->>'gateWriteSkipped' FROM job_runs WHERE job='maker-exit-panel' AND started_at > '2026-07-06 16:50:00+00' ORDER BY started_at DESC LIMIT 1. Manual re-fires only work on an UNCLAIMED 15-min slot (:00/:15/:45 — the :35 cron claims :30); command: select net.http_post(url:=(select decrypted_secret from vault.decrypted_secrets where name='project_url')||'/functions/v1/maker-exit-panel', headers:=jsonb_build_object('Content-Type','application/json','x-cron-secret',(select decrypted_secret from vault.decrypted_secrets where name='cron_secret')), timeout_milliseconds:=4500). DECIDE: CLEAN (cErr≤2 AND gateWriteSkipped null → gate row WROTE) → the day-7 verdict is ADJUDICATED: KILL (10x-confirmed, makerFillRate 0.045-0.086 vs 0.49 backtest). Execute GATE-DAY-PLAYBOOK.md Package 1 (KILL) EXACTLY, pre-authorized, LEAD with it: 1a FINDINGS.md 12th-signal closing block + summary-table row (~line 49) + fold the "one live exception" para into the now-unconditional bottom line + a "Where each finding lives" pointer; 1b CLAUDE.md pivot-block banner (newest entry); 1c SIGNAL-BACKLOG.md §13 re-open criteria. Pull REAL numbers first: `select * from public.dash_maker_exit();` (operator_guard'd — if ERR_FORBIDDEN read the latest maker_exit_panel.view jsonb: gate.{label,ciLow,ciHigh,nMarkets,nCities,nDistinctDays,winFrac,meanNetReturn,makerFillRate} + money.netPnlUsd + assumptions.{makerFillRate,realizedRebateUsd}). Fill every template placeholder. commit+push. Write C80. Present the closing verdict (twelve of twelve signals dead; rail DORMANT; analytics product stands). STILL cErr 3-6 → ride 1-2 more :35 cron ticks (budget-skips vary; the +30s headroom yields a ≤2 tick soon); do NOT relax GATE_WRITE_MAX_CITY_ERRORS=2 without operator override. STILL running → re-check in ~150s.

EACH CYCLE ALSO: 1. DAEMON WATCH: detached dry-run daemon (out/trade-bot-detached.log; watch LastWriteTime staleness = silent-wedge; find via Get-CimInstance scoped to cmd/node NAMES only, NEVER bare CommandLine~trade-bot; if wedged/fatal → kill tree + relaunch `set TRADE_MODE=dry-run&& set TRADE_TICK_SEC=60&& pnpm tsx scripts/trade-bot.ts >> out\trade-bot-detached.log 2>&1`). Last healthy 15:28Z (degraded:false, 0 failed). 2. SHADOW-WEEK: live_orders growth / 0 failed (was 214+); trade-shadow-diff.ts first read ~07-07 (report the divergence table FIRST). 3. CITY-LIVE (deployed dark, C78): verify the next 10:00Z city-paper-trade tick populates city_maker_twin + city_promotion_board + dash_city_live() renders on /trading; nothing trades until the operator toggles a city + funds a wallet + sets TRADE_MODE=live + preflight('city-taker') PASSes. 4. GUARDRAILS: no capital/keys/live-mode; deploys operator-gated; suite+typecheck green before CODE commits (docs-only exempt); subagents Opus; backlog CLOSED. 5. RECORD+PACE: C-tick on material change; wakeups 2700-3600s steady / 1500-1800s in flight; WIND-DOWN on operator's word: final tick + v15 prompt + /remember + summary ending with operator items.

Operator items standing: ① GATE co-tune fully deployed — adjudication finishing; ② CITY-LIVE deployed dark — toggle a city on /trading + fund wallet + TRADE_MODE=live to run the live city-taker test (quadruple-gated); ③ live-smoke re-run with --token (v2 write-path proof; refresh a token from opening_captures if stale); ④ compute downsize after the shadow week (~07-12; paying Micro); ⑤ opening_captures VACUUM/prune late-July; ⑥ pg_net + MCP-proxy incidents both recovered (07-06) — close any tickets; ⑦ 10 pre-session worktrees under .claude/worktrees (integration state unverified — clean when convenient).
```

## The autonomous /loop prompt — v10, DAY-WATCH + SHADOW-WEEK (paste as-is after /clear; self-paced — no interval)

> v9 finished the LIVE-RAIL slate overnight (C48–C49) and the operator approved + executed the go-live
> morning (C50–C51): 0082+0083 APPLIED, smoke PASS, cron offset live, the dry-run daemon RUNNING. v10 =
> the day watch (gate day-7 + #50 + shadow-week accrual) + the three small authorized hardening items.
> **The daemon belongs to the OPERATOR's terminal now** — the session's background copy was stopped at
> the C52 wind-down.

```
/loop Polyweather DAY-WATCH + SHADOW-WEEK orchestrator v10 (day 07-05; operator reachable — surface decisions, never block on him; capital/live stay OPERATOR-PHYSICAL; this loop never sets TRADE_MODE=live). FILES ARE THE STATE — read FIRST every cycle: FASTTRACK-PLAN.md cycle log C48–C52 (LIVE-RAIL night + go-live morning — the canonical record), docs/ops/TRADING-ACTIVATION.md (runbook; §11 = the two waived venue-edge follow-ups), docs/ops/GATE-DAY-PLAYBOOK.md, POST-FABLE-HANDOFF.md. Board: main == origin @ 6336436+ (verify via git log; suite 160 files/2583 green); 0082 + 0083 APPLIED; trade_config seeded mode='off' ($10/$25/$40/$100, kill $30∨25%); smoke PASS (creds+funder proven, apiKey 632d3ff9…); convergence cron job 26 = `2,17,32,47 * * * *`; the DRY-RUN DAEMON runs on the OPERATOR's terminal (`TRADE_MODE=dry-run pnpm tsx scripts/trade-bot.ts`) — if live_orders mode='dry-run' stops growing for >2h during listing hours, REMIND him (do not start your own copy unless he asks; concurrent copies are idempotency-safe but noisy); never read .env.local; backlog CLOSED, NO new signal hunts.

EACH CYCLE, in order: 1. GATE WATCH (bot_gate_snapshot mode='paper'; day 7 = the FIRST 07-05-targeted settle; n_open was 5 at 05:36Z). <7 days → note accrual, continue. ≥7 days → execute GATE-DAY-PLAYBOOK.md EXACTLY: KILL → the pre-authorized full record (FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13) + commit+push, LEAD the summary with it; PASS → present prominently + STAGE the §9R capital-scope decision doc ONLY (capital/wallet/keys operator-physical — a PASS changes NOTHING about what runs; note the interlock ALSO needs mode='live' + active_until set via trade_config_set, both operator calls); INSUFFICIENT-regression → package 3 (grading-lag, read-only SQL). 2. #50: at ≥10:05Z verify the 10:00Z city-paper-trade tick shows stats cities:4/placed>0 — report PROMINENTLY (closes the 0081 thread); once 07-04/07-05 grade, complete the houston °F graded-day confirmation (C37 partial record). 3. SHADOW-WEEK watch: live_orders mode='dry-run' row growth + status mix (dry-run entries NEVER fill — 'placed' rows accumulating is CORRECT; a 'failed' burst = investigate the daemon log with the operator) + /trading dryRun counts. 4. PROD probes (opening-capture */10 ok/<60s/ins>0 · maker-exit :35 ok/95–250s/≥40 mkts/cErr≤2 · convergence at 2,17,32,47 ok/<60s/errs≤2 — VERIFY the offset killed the :00/:30 collision-blip class; never probe :35–:42Z). Single blips stay benign; TWO CONSECUTIVE identical failures = the pre-authorized incident playbook. 5. SMALL BUILDS (the ONLY authorized ones, in priority order): (a) suppress the clob-client bootstrap 400-fallback console dump (it prints transient auth headers into logs — no key material but noise+hygiene; small packages/trading/src/live.ts bootstrap change, suite+typecheck, commit+push); (b) if idle, TRADING-ACTIVATION §11 follow-ups: getTrades cursor/at-limit response ⇒ treat as degraded + cancel_entry gated on a fresh successful entry poll — tests + a scoped opus lens each, merge on MERGE-CLEAN; (c) once dry-run rows span ≥1 full listing day: the FIRST SHADOW-DIFF READ — `pnpm tsx scripts/research/trade-shadow-diff.ts` (read-only; local env creds) → report the divergence table (divergences ARE the shadow week's product — report FIRST, never code-chase them unreported; expect entry-dims-only, dry-run never fills). 6. GUARDRAILS: no capital/keys/live-mode; no migrations/deploys beyond explicit operator approval; suite+typecheck green before any commit; subagents opus per the standing directive. 7. RECORD + PACE: cycle tick every cycle (bundle docs-only ticks with the next code commit when possible); wakeups 2700–3600s watch-only, 1800–2700s while a build lane is in flight. WIND-DOWN on the operator's word or ~20:00Z: final tick + /remember + a summary ENDING with the operator's open items (gate-label decision if PASS · optional --live-smoke write-path proof · daemon durability on his terminal · the §11 follow-ups if not done).
```

## The autonomous /loop prompt — v9, NIGHT LIVE-RAIL (paste as-is after /clear; self-paced — no interval)

> v8.1's gate-watch was overridden in-session ~22:47 local 07-04: the operator authorized the LIVE-RAIL
> build slate (buy-side execution + activation console). T1+T3 are MERGED (C46); T2 was in flight at the
> /clear. v9 = finish the slate through the night + keep every v8.1 watch duty. **The canonical brief
> source is `docs/ops/LIVE-RAIL-NIGHT-HANDOFF.md` — read it before anything else.**

```
/loop Polyweather LIVE-RAIL night orchestrator v9 (night 07-04→07-05; operator ASLEEP — never wait on him; everything capital/keys/apply/live stays OPERATOR-GATED and is NOT performed tonight). FILES ARE THE STATE — read FIRST every cycle: docs/ops/LIVE-RAIL-NIGHT-HANDOFF.md (board + T2 recovery + all briefs + integration protocol + seam contracts — THE canonical source), FASTTRACK-PLAN.md cycle log C44–C47, docs/ops/GATE-DAY-PLAYBOOK.md, POST-FABLE-HANDOFF.md, docs/ops/CITY-SIM-PLACEMENT-FIX.md. Board: main == origin @ d071c62+ (verify via git log; suite 155 files/2479 green at d071c62); 0079/0080/0081 APPLIED; 0082 MERGED-DARK (NEVER apply); TRADE_MODE never-live; never read .env.local; daemon+smoke are BUILT never RUN; backlog CLOSED, NO new signal hunts.

CYCLE 1 — T2 RECOVERY per handoff §"T2 recovery": inspect branch worktree-agent-acc5ba897c3517d45; complete-looking → dispatch t2-lens (handoff §t2-lens); incomplete/absent → re-dispatch T2 fresh (opus worktree) with the handoff §"T2 brief" VERBATIM (base = current main).

BUILD PIPELINE (handoff §"Integration protocol", proven tonight): lens → adjudicate (fix real, waive cosmetic w/ reason) → amend same branch → delta re-lens → stop on MERGE-CLEAN (cap ~3 rounds; non-converging → HOLD the merge + cycle-log note) → merge (keep-all-exports on barrel conflicts; suite+typecheck green on the merged tree; push) → cycle tick. After T2 merges → dispatch T4 (/trading page) + T5 (shadow-diff harness, BUILD-ONLY — its run needs 0082 applied) parallel per handoff briefs, lens each, merge, tick. T6 (CLAUDE.md ↳ UPDATE + BUILD-STATE addendum + POST-FABLE line) rides the wind-down.

EACH CYCLE, in order: 1. GATE WATCH (bot_gate_snapshot mode='paper'; day 7 = the FIRST 07-05-TARGETED POSITION SETTLING). <7 days → note accrual (mkts/cities/days, makerFillRate vs 0.49, rebate), continue. ≥7 days → execute GATE-DAY-PLAYBOOK.md exactly: KILL → the pre-authorized full record (FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13) + commit+push, LEAD the summary with it; PASS → present prominently + STAGE the §9R capital-scope decision doc ONLY (capital/wallet/keys OPERATOR-PHYSICAL, never act — a PASS tonight changes NOTHING about what runs); INSUFFICIENT-regression → package 3 (grading-lag, read-only SQL). 2. PROD HEALTH cheap probes (opening-capture */10 ok/<60s/inserted>0 · maker-exit-panel hourly :35 ok/95–250s/≥40 mkts/cityErrors≤2 · convergence-panel */15 ok/<60s/errors≤2; never probe :35–:42Z). KNOWN-BENIGN: single blips on :00/:30 capture-collision ticks + one-off partial maker-exit ticks — never playbook a single blip; TWO CONSECUTIVE identical failures = the pre-authorized incident playbook (throttle capture → pause offender → IO refill → staggered re-enable). 3. If ≥10:05Z: VERIFY the 07-05 10:00Z city-paper-trade tick shows stats cities:4/placed>0 — report PROMINENTLY (closes the 0081 thread); once 07-04/07-05 grade, complete the houston °F graded-day confirmation (partial record C37). 4. GUARDRAILS: no capital/keys/apply/live; no migrations/deploys beyond explicit operator authorization; suite+typecheck green before any commit; subagents on opus per the standing directive. 5. RECORD + PACE: cycle tick every cycle; wakeups 2700–3600s (agent notifications wake you sooner; while a lens/build lane is in flight 1800–2700s is fine). WIND-DOWN when the slate (T2, T4, T5, T6) is merged AND (the operator returns OR the 10:00Z verification is recorded): final tick + /remember + a summary that ENDS with the operator morning items from handoff §"Operator morning items" (apply 0082 · run trade-smoke · start the dry-run daemon · gate label / §9R decision if PASS · optional convergence cron offset). The deadmen page on real prod problems — do not poll faster than hourly for the gate label.
```

## The autonomous /loop prompt — v8.1, GATE-WATCH (paste as-is after /clear; self-paced — no interval)

> v7 drove the 07-04 DAY-BUILD to completion (C33–C42: D5/D6/D7 shipped, the city-sim placement defect
> found+fixed, #31 done). v8.1 reflects the board after the operator's in-session "go" (C43): the
> CITY-SIM-PLACEMENT-FIX bundle steps 1–3 are EXECUTED LIVE (0081 applied 19:26Z · fn redeployed 19:27Z ·
> smoke PASS 4 configs); only the §3 gap-fill (conditional — the 07-04 session's wakeup chain may have run
> it at ~22:05Z, check before running) + the 07-05 10:00Z verification remain. NOTHING is left to build —
> the loop is a gate-watch + verification sentinel until the day-7 label renders (~07-05).

```
/loop Polyweather FASTTRACK orchestrator v8.1 (GATE-WATCH; all build lanes DONE — do NOT dispatch any build agents). You are WATCHER/ADJUDICATOR ONLY. FILES ARE THE STATE — read FIRST every cycle: FASTTRACK-PLAN.md (cycle log C33+ = the 07-04 record), docs/ops/GATE-DAY-PLAYBOOK.md (the pre-drafted KILL/PASS/INSUFFICIENT packages), POST-FABLE-HANDOFF.md ("What changed 07-03→07-04" + steady state), docs/ops/CITY-SIM-PLACEMENT-FIX.md (deploy status banner), TaskList. Board: main == origin (verify via git log; suite 151 files/2308 green); migrations 0079+0080+0081 APPLIED and city-paper-trade REDEPLOYED (bundle steps 1–3 done 19:27Z 07-04, smoke PASS); backlog CLOSED, NO new signal hunts, NO build lanes.

CYCLE 1 ONLY — conditional gap-fill: check via read-only SQL whether city_paper_bets has target_date='2026-07-04' rows. IF ABSENT and current time ≥22:00Z 07-04: run `pnpm tsx scripts/city-sim.ts --from 2026-07-04 --to 2026-07-04` (local env creds, never echo secrets), verify the rows landed per city (ankara/houston arms 11–16, karachi/singapore 10–15 where snapshots existed), record a cycle tick + update the CITY-SIM-PLACEMENT-FIX.md banner, commit+push. IF PRESENT: the 07-04 session's chain already ran it — just note and proceed.

EACH CYCLE, in order: 1. GATE WATCH (bot_gate_snapshot mode='paper'; day 7 = the FIRST 07-05-TARGETED POSITION SETTLING — entries were blocked 07-04 by thin holiday books, expect entries+settles through 07-05). <7 days → report accrual (mkts/cities/days, makerFillRate vs 0.49, rebate) and sleep. ≥7 days → execute GATE-DAY-PLAYBOOK.md exactly: KILL → the pre-authorized full record (FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13) + commit+push, lead the summary with it; PASS → present prominently + STAGE the §9R capital-scope decision doc ONLY (capital/wallet/keys OPERATOR-PHYSICAL, never act); INSUFFICIENT-regression → package 3 (grading-lag definitions, read-only SQL). 2. PROD HEALTH cheap probes (opening-capture */10 ok/<60s/inserted>0 · maker-exit-panel hourly :35 ok/95–250s/≥40 mkts/cityErrors≤2 · convergence-panel */15 ok/<60s/errors≤2; never probe :35–:42Z). KNOWN-BENIGN: single convergence blips (5 errors/14 mkts) on :00/:30 capture-collision ticks and one-off partial maker-exit ticks — self-recovering, do NOT playbook a single blip; TWO CONSECUTIVE identical failures = the pre-authorized incident playbook (throttle capture → pause offender → IO refill → staggered re-enable). 3. VERIFY the placement fix end-to-end: the 07-05 10:00Z city-paper-trade tick must show stats cities:4/placed>0 (report it prominently — it closes the 0081 thread); once 07-04/07-05 grade, complete the houston °F graded-day confirmation (partial record in C37). 4. GUARDRAILS: no capital/keys; rail paper/DORMANT; no new signal hunts; no migrations/deploys beyond what the operator explicitly authorizes; suite+typecheck green before any commit. 5. RECORD + PACE: cycle tick + TaskList every cycle; wakeups 2700–3600s watch-only (the gate updates on the hourly :36 snapshot). WIND-DOWN when the gate label is handled AND the 10:00Z verification is recorded (or the operator ends the session): final tick + /remember + a summary ending with the operator's pending items (a PASS adds exactly one — the §9R capital-scope decision; a KILL adds none; the optional convergence cron offset 2,17,32,47 stays on the list). The deadmen page on real prod problems — do not poll faster than hourly for the label.
```

## The autonomous /loop prompt — v7, DAY-BUILD 07-04 (paste as-is after /clear; self-paced — no interval)

> v6 (below) drove the NIGHT-BUILD to completion (C28–C31: 6/6 lanes merged+pushed, suite 2080 green).
> v7 reflects the board after the operator's 07-04 morning blanket approval (recorded C32): migrations
> 0079+0080 APPLIED live this session; the day = gate-day execution + #31 + three build lanes D5/D6/D7
> + the D8 docs wind-down. Subagents are OPUS (cost parity, C28). WS1-ROLLOUT keeps maker-exit HOURLY
> by decision — a cadence restore is NOT on the board.

```
/loop Polyweather FASTTRACK orchestrator v7 (DAY-BUILD 07-04; operator away all day — blanket approval recorded C32: commit+push authorized; migrations 0079+0080 ALREADY APPLIED; a gate KILL is pre-authorized to record; a PASS is stage-only; capital/wallet/keys OPERATOR-PHYSICAL, never blurred). You are PLANNER/ORCHESTRATOR/CHECKER/ADJUDICATOR ONLY — OPUS agents build/run/draft in worktrees (never commit); you verify, review, integrate serially, record, push. FILES ARE THE STATE — read FIRST every cycle: FASTTRACK-PLAN.md ("DAY-BUILD slate" D3–D8 briefs + cycle log C1–C32), docs/ops/GATE-DAY-PLAYBOOK.md, POST-FABLE-HANDOFF.md, SIGNAL-BACKLOG.md §12, docs/ops/WS1-ROLLOUT.md (maker-exit stays HOURLY by decision — do NOT restore */15), TaskList. Board: main == origin (verify via git log); suite 2080 green / 144 files; backlog FULLY CLOSED — NO new signal hunts; D5/D6/D7 are the ONLY build lanes.

CYCLE 1: (a) prod probes (bars: opening-capture */10 ok/<60s/inserted>0 · maker-exit-panel hourly :35 ok/95–250s/≥40 mkts/cityErrors≤2 · convergence-panel */15 ok/<60s/errors≤2; never probe :35–:42 UTC); (b) dispatch D5+D6+D7 as parallel background OPUS worktree agents, each brief pasted VERBATIM from the DAY-BUILD slate + standing constraints (no commits, no DB writes, no prod access, exact suite/typecheck counts reported; D5's ERA5 fetch throttled + disk-cached + resumable, external API only); (c) read the latest bot_gate_snapshot.

EACH CYCLE, in order: 1. GATE WATCH headline (bot_gate_snapshot mode='paper'; day 7 = the FIRST 07-05-TARGETED POSITION SETTLING, expected mid-day — C31's settled-entry-date correction). <7 days → report accrual (mkts/cities/days, makerFillRate vs 0.49, rebate). ≥7 days → execute docs/ops/GATE-DAY-PLAYBOOK.md exactly: KILL → PRE-AUTHORIZED full record (FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13 re-open criteria), commit+push, lead the summary with it; INSUFFICIENT-regression → package 3 (run the grading-lag definitions via MCP read-only SQL); PASS → present prominently + STAGE the §9R capital-scope decision doc ONLY and list it as THE operator item — never act on it. 2. #31 at ≥10:05Z: the 07-04 10:00Z city-paper-trade tick is ok; ankara+houston bets exist (arms 11–16 local, target 07-04); dash_city_sim surfaces 4 cities; houston's °F ladder/bucketing sane (the °F graded-day confirmation completes only when 07-04 grades — record the partial result, leave the remainder to the evening). 3. INTEGRATE finished lanes SERIALLY: footprint vs brief · exact counts green · ONE opus review lens on computed-display numbers (D5's climatology math; D7's copied table) before merge · full suite+typecheck on the merged tree · commit (conventional style, Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>) · push (Vercel deploys). Stalls >45 min → ONE corrective re-brief to a fresh agent, else record the lane blocked. 4. PROD HEALTH cheap probes each cycle; two identical failures = the pre-authorized incident playbook (throttle capture → pause offender → IO refill → staggered re-enable). NEW migrations beyond the applied 0079/0080: STAGE + list for the operator — nothing else is pre-approved. 5. GUARDRAILS: ONE heavy DB op project-wide; :35–:42 blackout; no capital/keys; rail paper/DORMANT; no new signal hunts; agents never commit; suite+typecheck green before every commit; D5's fetch talks ONLY to the external ERA5 archive API, never prod. 6. RECORD + PACE: cycle tick + TaskList every cycle; wakeups 1500s lanes-in-flight / 2700–3600s watch-only. WIND-DOWN when (all D-lanes integrated/blocked AND the gate label handled AND #31 done) OR ~20:00Z, whichever first: execute D8 (POST-FABLE-HANDOFF.md refresh + BUILD-STATE addendum), final cycle tick, /remember (lanes landed/blocked · the gate verdict + how it was handled · #31 result · anything newly staged), and a final summary ending with the operator's pending items (a PASS leaves exactly one — the §9R capital-scope decision; a KILL leaves none). No wakeup past wind-down — the deadmen page on real prod problems.
```

## DAY-BUILD slate (2026-07-04 ~08:15Z — operator blanket approval C32; operator away all day; maximize build)

> Board at slate creation: main == origin; suite 2080 green / 144 files; NIGHT-BUILD 6/6 landed (C29–C31);
> **0079 + 0080 APPLIED live at 08:10Z** (guard verified unbypassable — ERR_FORBIDDEN on every non-operator
> path; behavior under the PGlite twins; the /maker-exit sparklines + /paper-trade intended-bet tiles are now
> LIVE for the operator session). Gate 6/7 (day 7 = first 07-05-targeted settle, ~mid-day). WS1-ROLLOUT keeps
> maker-exit HOURLY by decision — no cadence change today. Discipline unchanged: worktree per lane, agents
> never commit, orchestrator integrates serially, ONE lens on computed-display numbers, no new signal work.

- **D3 — GATE DAY (orchestrator-only; the headline).** Adjudicate per `docs/ops/GATE-DAY-PLAYBOOK.md`
  verbatim when n_distinct_days ≥ 7. KILL → pre-authorized FULL record (FINDINGS.md 12th-signal closing
  block with realized numbers + CLAUDE.md banner line + SIGNAL-BACKLOG §13 re-open criteria) + commit/push.
  PASS → stage the §9R capital-scope decision document ONLY. INSUFFICIENT-regression → package 3. The
  capital/wallet/keys boundary is OPERATOR-PHYSICAL and survives every blanket approval.
- **D4 — #31 enrollment verification (orchestrator, ≥10:05Z).** Ankara + Houston first forward day:
  10:00Z tick ok; bets with target_date 07-04 for both (arms 11–16 local); dash_city_sim shows 4 cities;
  houston °F ladder sane end-to-end (0070's convert-then-round path — the graded-day confirmation lands
  when 07-04 grades; record the partial).
- **D5 — ERA5 45-city peak-hour climatology (opus lane, worktree; the flagship).** Generalize the
  Amsterdam peak-hour floor-confidence model to every city in `cities`: (a) `scripts/research/city-peak-hour.ts`
  mirroring `amsterdam-peak-hour.ts`'s math but sourcing ~20 years of hourly temperature per city from the
  FREE Open-Meteo ERA5 archive API (throttled, chunked per city×year-range, disk-cached under a gitignored
  dir, resumable, NO DB access); (b) `--emit` a committed static asset `packages/core/src/sim/city-climatology.ts`
  (per-city hour-of-day floor-confidence curves, compact — seasonal split only if the size stays sane) +
  shape/consistency tests; (c) SELF-CHECK: the EHAM curve derived from ERA5 must approximately reproduce the
  committed KNMI-derived `amsterdam-climatology` (report correlation + max-abs-diff; document tolerance);
  (d) display-only wiring: a climatology column on /paper-trade per city (peak hour + floor-confidence at
  the recommended arm) NEXT TO the entry-watch ⭐ — ZERO changes to the sim engine, bet placement, or
  entry-watch math. Lens on the fetch/compute script before merge (computed-display numbers).
- **D6 — home `/` twelve-ways hero (opus lane, small).** Feature the verdict headline + a /signals link on
  the home overview: reuse `SIGNAL_HERO` from `core/sim/signals-findings.ts` (numbers imported from the
  asset, never re-typed), glass idiom, render-test. Small diff.
- **D7 — per-city "model use by use-case" panel on /data (opus lane).** The operator's 2026-06-29 follow-up:
  surface the 07-03 per-city source-accuracy result (~2,100 events; the calibrated blend dominates every
  source at every lead — recorded in MAKER-EXIT-SIM.md / the source-accuracy research script) as a committed
  static asset + a /data section: per city, the best single source vs the blend, and the two use-cases made
  explicit (accuracy forecast = bias-corrected blend vs convergence seed = raw crowd consensus, per CITY-SIM
  §7). Numbers COPIED from the canonical recorded output with golden tests; if the recorded table cannot be
  located in-repo, the lane reports that instead of re-deriving (a fresh heavy pull needs the orchestrator's
  ONE-heavy-op lane + blackout coordination — coordinate, don't improvise).
- **D8 — docs wind-down (orchestrator).** POST-FABLE-HANDOFF.md refresh (night+day changes, the C31
  day-count correction, applied 0079/0080, new pages /signals + sparklines + intended-bet + climatology,
  restore-state table check) + a BUILD-STATE.md day addendum.

## The autonomous /loop prompt — v6, NIGHT-BUILD (paste as-is after /clear; self-paced — no interval)

```
/loop Polyweather FASTTRACK orchestrator v6 (NIGHT-BUILD; token reset ~04:10Z/06:10 local — build maximum value until then, wind down at ~04:00Z). You are PLANNER/ORCHESTRATOR/CHECKER/ADJUDICATOR ONLY — sonnet agents build/run/draft in worktrees (never commit); you verify, review, integrate, record, push. FILES ARE THE STATE — read FIRST, every cycle: FASTTRACK-PLAN.md ("NIGHT-BUILD slate" = the six lane briefs N1–N6 + cycle log C1–C27), docs/ops/GATE-DAY-PLAYBOOK.md, docs/ops/REWARD-INSTR-ROLLOUT.md (v2 section), SIGNAL-BACKLOG.md §12, POST-FABLE-HANDOFF.md, TaskList. Board: main == origin @ (git log; c2893e4 or later); suite 138 files/2009 green; backlog FULLY CLOSED — NO new signal hunts, N1–N6 are the ONLY build lanes (frontend/docs/scripts; none touch the gate engine).

CYCLE 1 (setup): (a) verify the first post-v2-deploy :35 maker-exit tick (23:35Z 07-03 or later): job_runs.stats carries dominantDisqualifier, tick healthy (ok/cityErrors≤2/nMarkets≥40/~95–250s), snapshot view.assumptions carries the WHY fields — RECORD the first observed values in the cycle log (expected signature: high in-band + low fail-min-size + dominant 'none' ⇒ the mid<0.10 two-sided regime); (b) dispatch ALL SIX lanes N1–N6 as parallel background sonnet agents (worktree isolation; paste each lane's brief from the NIGHT-BUILD slate section VERBATIM + the standing constraints: no commits, no DB writes, no migrations APPLIED — stage-dark only, report exact suite/typecheck counts).

EACH CYCLE after: 1. INTEGRATE finished lanes SERIALLY (verify footprint matches brief · exact counts green · numbers copied-not-recomputed where applicable · ONE lens only for staged SQL or computed-display numbers (N2/N3), direct review otherwise → apply diff to main → full suite+typecheck on merged tree → commit (conventional style, Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>) → push (Vercel deploys)). Stalls >45 min → ONE corrective re-brief to a fresh agent, else record the lane blocked and move on.
2. GATE WATCH every cycle: latest bot_gate_snapshot (mode='paper'). <7 days → note accrual (mkts/cities/days + makerFillRate vs 0.49 + rebate). ≥7 days → the label is rendered: execute docs/ops/GATE-DAY-PLAYBOOK.md — KILL → PRE-AUTHORIZED (C21): record the full package (FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13 re-open criteria) without re-asking, commit+push, lead the summary with it; INSUFFICIENT-regression → package 3 checks; PASS → present + stage package 2's §9R decision doc ONLY — capital/wallet/keys are OPERATOR-PHYSICAL, never pre-authorized, never blurred.
3. PROD HEALTH (cheap probes, never :35–:42 UTC): opening-capture (*/10: ok/<60s/inserted>0) · maker-exit-panel (:35 hourly: ok + qualifyingTickFrac trend, reads so far 0 @ 19:35Z + 0 @ 20:35Z 07-03) · convergence-panel (*/15: ok/<60s/cityErrors≤2). Two identical failures = the pre-authorized playbook (throttle capture → pause offender → IO refill → staggered re-enable). Migrations/destructive SQL: STAGE ONLY at night.
4. GUARDRAILS: ONE heavy DB op project-wide; :35–:42 blackout; no capital/keys, rail paper/DORMANT; no new signal hunts; never touch other agents' uncommitted files; suite+typecheck green before every commit; agents never commit.
5. RECORD + PACE: cycle tick + TaskList each cycle. Wakeups: lanes in flight → 1500s; integration backlog → continue now. WIND-DOWN at ~04:00Z (or all lanes integrated/blocked earlier): stop dispatching, integrate what's done, record any un-integrated lane's worktree path + state in the cycle log, /remember (full state: lanes landed/blocked, staged migrations awaiting the operator, gate status, the 10:00Z #31 check owed by the morning session), then a final summary ending with the operator's morning items: apply any staged migrations (N2/N3 if dark), the 10:00Z ankara/houston °F verification, the gate label if still unrendered, and the pre-placement-forecast decision if N2 went dark. The gate deadmen page on real prod problems — do NOT keep a wakeup alive past wind-down.
```

## NIGHT-BUILD slate (2026-07-04 ~01:15 local — operator directive: token reset ~06:10 local / ~04:10Z; build maximum value until then; NO new signal work — the backlog stays closed)

> Board at slate creation: main == origin @ `c2893e4`; the evening token-spend slate is 5/5 integrated
> (gate-drafts `3365a06` · °F tests `44e4603` · cs-harden `7efedac` · reward-instr v2 `5a38f6d`
> MERGED+DEPLOYED · paper-trade page `c2893e4` incl. the operator current-bet box + Stockholm times).
> Suite 138 files / 2009 tests green. Open watches: #34 one-time v2 tick verification (23:35Z+),
> #30 gate label (6/7 days last read; KILL pre-authorized per C21+GATE-DAY-PLAYBOOK.md, PASS stage-only),
> #31 enrollment verification (10:00Z — lands AFTER the token reset; morning session handles it).
> All lanes below are frontend/docs/scripts — NONE touch the gate engine. Discipline: worktree per lane,
> no commits by agents, orchestrator integrates serially (suite+typecheck on each merged tree), pushes
> (Vercel deploys); ONE review lens only where a lane stages SQL or computes displayed numbers;
> migrations NEVER applied at night — STAGE-DARK pattern (build the UI behind a null-guard so it ships
> dark and lights up when the operator applies the staged migration).

- **N1 — /signals verdict explorer (the product flagship).** New page + committed static asset
  (the `city-scan-results.ts` idiom): every signal from FINDINGS.md — the 12 numbered signals + the
  major prior angles — as structured rows: name, mechanism class (A adverse-selection / B fee-fill-wall /
  latency / structural), the ONE key number with CI, verdict, doc pointer. Hero: "the market measured
  efficient twelve ways." Glass idiom per /efficiency. Golden-value tests asserting figures match
  FINDINGS.md verbatim (copy, never recompute). The forward maker-exit gate row renders its LIVE status
  from dash_maker_exit (the one dynamic row — reuse the /maker-exit loader pattern).
- **N2 — pre-placement forecast completing the current-bet box.** The known gap from `c2893e4`: before
  the 10:00Z tick the box shows the latest bet, not today's intended temp. Agent FIRST maps the keyless
  data path (existing RPCs only — what does the web app have access to that carries the current
  house_gaussian blend per enrolled city? dash_data? any city-sim RPC?). If an existing RPC suffices →
  build. If not → write migration `009x_dash_city_forecast.sql` (SECURITY DEFINER read-only dash RPC,
  operator_guard idiom) STAGED-NOT-APPLIED + the UI behind a null-guard (ships dark). One lens on the
  SQL if staged.
- **N3 — /maker-exit assumptions trend (gate-day instrumentation).** The three measured assumptions
  (makerFillRate / realizedRebateUsd / qualifyingTickFrac + the v2 WHY fields) over time — the data
  exists in `maker_exit_panel` snapshots (view.assumptions per snapshot). Agent maps the read path: if
  dash_maker_exit only returns latest → STAGE-DARK a `dash_maker_exit_history(n)` RPC migration + build
  the sparklines behind a null-guard. Small multiples above tile #4; the fill-rate line annotated with
  the 0.30 warning + 0.49 backtest reference lines. One lens on staged SQL.
- **N4 — README + product positioning (the repo is PUBLIC).** Root README.md presenting the analytics
  product: what Polyweather measures (calibrated multi-model Tmax forecasting vs prediction markets),
  the honest headline (12 signals tested, market efficient — FINDINGS.md), the dashboards, the
  architecture in 10 lines, run/test instructions, the no-trading-advice + DORMANT-rail boundary.
  Terse, numbers-first, zero hype; no secrets/keys/refs beyond what's already public in the repo.
- **N5 — grading-lag ops helper (gate-day package-3 support).** `scripts/ops/grading-lag.ts`: read-only;
  lists resolved-but-ungraded markets for the forward panel window (target_date passed, winner unset)
  + the distinct-day impact ("day N would join at grading"). Playbook package 3 references it. Tiny.
- **N6 — dashboard nav + glass-idiom consistency sweep.** Shared compact nav (the dash pages are
  islands: /, /efficiency, /maker-exit, /paper-trade, /amsterdam, /data, /sharps, /rewards,
  /whaletracker, /convergence…) + per-page idiom drift fixes (fonts/cards/spacing only — NO data-logic
  changes). Screenshot-free: assert via render tests where they exist; keep diffs per page small.

## The autonomous /loop prompt — v5, gate-watch + enrollment-forward (paste as-is; self-paced — no interval)

> v4 (below, superseded) closed at C20: deploy verified (first `qualifyingTickFrac` = 0), item 12 adjudicated
> (2 candidates) + recorded + pushed. The operator then gave in-session blanket approval → enrollment EXECUTED
> (C21): ankara + houston live in `city_sim_config`, backfilled, forward clock starts 07-04. Board at v5 start:
> backlog FULLY closed; the ONLY live questions are (a) the forward maker-exit gate label (renders when 07-03
> grades — 73/34/6-of-7 at last read) and (b) the new cities' first forward day. Pre-authorization recorded:
> a KILL verdict may be RECORDED without re-asking; a PASS only ever gets STAGED — capital/wallet/keys remain
> operator-physical, never pre-authorizable.

```
/loop Polyweather FASTTRACK orchestrator v5 (gate-watch; Fable window closes ~2026-07-06/07). You are PLANNER/ORCHESTRATOR/CHECKER/ADJUDICATOR ONLY — sonnet agents build/run/draft; you verify, adjudicate, record. Files are the state: FASTTRACK-PLAN.md (cycle log C1–C21 + this prompt), POST-FABLE-HANDOFF.md (THE gate adjudication guide), SIGNAL-BACKLOG.md §12 (verdict + executed enrollment + the confirmation-clock rule), FINDINGS.md, docs/ops/REWARD-INSTR-ROLLOUT.md, TaskList — read every cycle. Board: main == origin; backlog FULLY CLOSED (item 12 executed — do NOT start new signal work; re-open criteria live in SIGNAL-BACKLOG.md); city_sim_config = 4 active cities (ankara/houston enrolled 07-03, backfill IN-SAMPLE, candidate confirmation = target_date ≥ 07-04 ONLY); commits local by default, push only follow-ups to already-authorized threads.

EACH CYCLE, in order:
1. GATE WATCH (headline): read the latest bot_gate_snapshot (mode='paper'). While n_distinct_days < 7 → report accrual (mkts/cities/days + makerFillRate vs 0.49 backtest + realizedRebateUsd) and do nothing. WHEN n_distinct_days ≥ 7 the code renders the label — report it PROMINENTLY with POST-FABLE-HANDOFF §adjudicate's meaning. Handling is PRE-AUTHORIZED (operator blanket approval 2026-07-03 ~22:05 local, recorded C21): KILL → RECORD it fully without re-asking (FINDINGS.md 12th-signal closing row with the realized numbers; CLAUDE.md banner line; SIGNAL-BACKLOG re-open criteria; commit+push as a follow-up) and lead the summary with it; INSUFFICIENT → keep accruing; PASS → present prominently + STAGE the §9R capital-scope options as a decision document — capital, wallet funding, and keys are OPERATOR-PHYSICAL and were explicitly NOT pre-authorized. Never blur that line.
2. ONE-TIME ENROLLMENT VERIFICATION (task #31, skip once done): after the 07-04 10:00Z city-paper-trade tick — bets exist for ankara+houston (arms 11–16 local, target_date 07-04), the tick's job_runs row is ok, /paper-trade data surface shows 4 cities. Explicitly check houston's first graded °F day buckets correctly (first real °F exercise of 0070's unit-agnostic bucketing). Report; any defect → one sonnet fix agent, lens if engine-adjacent.
3. REWARD-INSTR TREND (cheap, ~once per 2 cycles): qualifyingTickFrac across the recent :35 ticks (first read 0 @ 19:35Z 07-03) — report the trend; it bounds the 1b reward term. No action, measurement only.
4. PROD HEALTH (cheap probes, never :35–:42): opening-capture (*/10: ok/<60s/inserted>0) · maker-exit-panel (:35 hourly: ok/snapshotId/nMarkets≥40/~95–250s) · convergence-panel (*/15: ok/<60s/cityErrors≤2) · city-paper-trade (daily 10:00Z: ok). One failure = note+watch; two identical = the pre-authorized playbook (throttle capture → pause offender → IO refill → staggered re-enable). NEW migrations/fn deploys/destructive SQL: stage + ask.
5. GUARDRAILS (non-negotiable): ONE heavy DB op project-wide; :35–:42 blackout; no capital, no keys, rail paper/DORMANT; no new signal hunts; never touch other agents' uncommitted files; the PASS→capital boundary above is absolute.
6. RECORD + PACE: cycle tick + TaskList each cycle; /remember before any >1h idle. Wakeups: overnight/watch-only → 3600s; label imminent or agent in flight → 1500s. STOP when BOTH the gate label is rendered+handled per the pre-authorization AND enrollment verification (#31) is done → final tick + /remember + summary ending with the operator's pending items (only a PASS leaves one: the capital-scope decision; a KILL leaves none — the analytics product continues per FINDINGS.md).
```

## (v4, post-deploy — superseded) The autonomous /loop prompt (paste as-is; self-paced — no interval)

> v3 (below, superseded) was written before the operator authorized commit/deploy/push. Board at
> 2026-07-03 ~21:30 local: WS-3 + reward-instr are MERGED/DEPLOYED/PUSHED (main == origin @ `b982ea9`);
> item 12 (CITY-SCAN) was in flight when the session ended — its runner dies with the session, so verify
> whether results were recorded and re-dispatch if not (the pre-registered spec is committed). The forward
> gate is ~1 graded day from sufficiency.

```
/loop Polyweather FASTTRACK orchestrator v4 (post-deploy; Fable window closes ~2026-07-06/07). You are PLANNER/ORCHESTRATOR/CHECKER/ADJUDICATOR ONLY — sonnet agents build/run/draft; you verify, review, adjudicate, record. Files are the state: FASTTRACK-PLAN.md (cycle log C1–C14 + workstream statuses), POST-FABLE-HANDOFF.md (steady state + THE gate adjudication guide), SIGNAL-BACKLOG.md (all verdicts + item 12 CITY-SCAN pre-registration), BUILD-STATE.md (17:45Z addendum), docs/ops/REWARD-INSTR-ROLLOUT.md, TaskList — read every cycle. Board: main == origin @ b982ea9 (WS-3 verdicts + reward-instr diagnostic merged/deployed/pushed 07-03 ~21:15 local); backlog CLOSED except item 12; commits stay local by default — push only follow-ups to already-authorized threads.

EACH CYCLE, in order:
1. ONE-TIME DEPLOY VERIFICATION (skip once done, per docs/ops/REWARD-INSTR-ROLLOUT.md): first post-deploy :35 tick (first eligible 19:35Z 07-03) — job_runs.stats carries qualifyingTickFrac (number|null) and the tick stays healthy (ok, cityErrors ≤2, nMarkets ≥40, ~95–250 s); Vercel READY + /maker-exit renders tile #4 (em-dash on pre-deploy snapshots = correct). Record the first observed qualifyingTickFrac in the cycle log — it bounds the 1b reward term (income ∝ frac × pool × share) BEFORE the share question opens.
2. CITY-SCAN (item 12): check whether a verdict block exists under SIGNAL-BACKLOG §12 — if not, re-dispatch a sonnet runner implementing the pre-registered block VERBATIM (one new file scripts/research/city-scan.ts; local maker-exit cache + ONE cheap bucket_probabilities pull, blackout-guarded, stop-on-2-identical-failures; raw tables only, no self-verdicts; reuse entry-watch LB + clusterMeanTCi + the live engine's fee math). On report: verify footprint, then YOU adjudicate vs the locked bars (candidate = TRAIN LB>0 AND TEST net>0 among the top-5 TRAIN cells; all else descriptive) → record FINDINGS row + §12 verdict block + tick → present candidates + the pooled entry-hour curve to the operator as a city_sim_config ENROLLMENT decision (stage exact commands; do NOT apply). One review lens on the script ONLY if a candidate emerges (behavior-changing); waive with documented reasoning for a null.
3. PROD HEALTH (cheap probes, never :35–:42): opening-capture (*/10: ok/<60 s/inserted>0) · maker-exit-panel (hourly :35: ok + snapshotId + nMarkets ≥40) · convergence-panel (*/15: ok/<60 s/cityErrors ≤2). One failure = note+watch; two identical = the pre-authorized playbook (throttle capture → pause offender → IO refill → staggered re-enable). NEW migrations/fn deploys/destructive SQL: stage + ask the operator.
4. GATE WATCH — sufficiency is IMMINENT (18:36Z 07-03: 73 mkts / 34 cities / 6 of 7 days; read: net −$160 / −11 % ROI, makerFillRate 0.082 vs 0.49 backtest — the POST-FABLE-named KILL path). When nDistinctDays ≥ 7 the code renders the gate label — report it PROMINENTLY with what POST-FABLE-HANDOFF §adjudicate says it means. NEVER adjudicate it yourself: PASS → operator decision (capital scope conversation, nothing auto-authorized); KILL → STAGE the FINDINGS.md + CLAUDE.md updates and ask the operator to confirm before recording the 12th signal dead; INSUFFICIENT → keep accruing.
5. GUARDRAILS (non-negotiable): ONE heavy DB op project-wide; :35–:42 blackout; no capital, no keys, rail paper/DORMANT; no new signal hunts (re-open criteria in SIGNAL-BACKLOG.md only); never touch other agents' uncommitted files.
6. RECORD + PACE: cycle-log tick + TaskList each cycle; /remember before any >1 h idle. Wakeups: runner in flight → 1500 s; watch-only → 2700–3600 s. STOP when: deploy verified + item 12 adjudicated/recorded + the gate status reported (label rendered, or still <7 days with nothing else open) → final tick + /remember + a summary ending with the operator's pending decisions (city enrollment; the gate verdict handling; nothing else is open).
```

## (v3, post-WS-3-close — superseded) The autonomous /loop prompt (paste as-is; self-paced — no interval)

> v2 (below, kept for reference) drove the WS-3 adjudication sprint to completion (cycle log C1–C13).
> v3 reflects the board after 2026-07-03 ~20:30 local: ALL workstreams closed; the tree is committed
> locally (feat/research/docs triplet on main); ONE build is in flight (the operator-greenlit 1b
> follow-on: forward reward-eligibility instrumentation, worktree agent); the forward gate accrues.

```
/loop Polyweather FASTTRACK orchestrator v3 (post-WS-3; Fable window closes ~2026-07-06/07). You are PLANNER/ORCHESTRATOR/CHECKER/ADJUDICATOR ONLY — sonnet agents build/run/draft; you verify, review, adjudicate, record. Files are the state: read FASTTRACK-PLAN.md (WS statuses + cycle log), POST-FABLE-HANDOFF.md (steady-state config + gate adjudication guide — do not "fix" it without evidence), BUILD-STATE.md (17:45Z addendum = the WS-3 scorecard), and TaskList, every cycle. Board expectations: main carries the local WS-3 commit triplet (feat 0064445 · research 899f77e · docs — plus predecessors); NEVER push; the signal backlog is CLOSED (all 9 verdicts recorded; re-open criteria live in SIGNAL-BACKLOG.md — do NOT start new signal work).

EACH CYCLE, in order:
1. INTEGRATE the reward-instrumentation build (agent 'reward-instr', worktree): verify diff scope is ADDITIVE-DIAGNOSTIC ONLY (a qualifyingTickFrac/pool-context diagnostic on the forward view + /maker-exit; the §9R-E gate math byte-identical, proven by unchanged existing tests + exact suite/typecheck counts reported green; no prod touches; no migration unless jsonb-additive was impossible — if a migration appears, treat it as a finding to adjudicate, not auto-accept). Then TWO sonnet review lenses (it touches the engine-adjacent view + an edge handler; SEVERITY·file:line·claim·evidence; you adjudicate; stop at zero code defects; 1-2 rounds). On zero defects: merge the worktree branch into main locally (suite+typecheck green on the merged tree before the commit), then have a sonnet agent draft docs/ops/REWARD-INSTR-ROLLOUT.md (deploy = redeploy maker-exit-panel [+ apply migration if any]; per-step verification queries; rollback = redeploy previous fn) — you review + commit it. Present the bundle to the operator in the cycle summary, bolded. If the agent stalls >45 min or dies: ONE corrective re-brief to a fresh agent, else record the lane blocked.
2. PROD HEALTH (cheap probes, never :35–:42): last ticks of opening-capture (*/10: ok/<60s/inserted>0), maker-exit-panel (hourly :35: ok/snapshotId/nMarkets≥40 — healthy reference is now ~95s/0 cityErrors), convergence-panel (*/15: ok/<60s/cityErrors≤2). One failure = note+watch; two identical = the pre-authorized playbook (throttle capture → pause offender → refill → staggered re-enable). NEW migrations/fn deploys/destructive SQL stay operator-gated — stage, never apply.
3. GATE ACCRUAL (read-only): once per ~2 cycles read the latest bot_gate_snapshot / dash_maker_exit — report realized markets/cities/days vs the frozen ≥40/≥6/≥7 bars + the three assumptions (makerFillRate — 0.092 last read vs 0.49 backtest, THE number to watch; realizedRebateUsd; days). You NEVER adjudicate the gate: at sufficiency the code renders the label and POST-FABLE-HANDOFF.md §adjudicate says what each label means — a PASS is an OPERATOR decision; INSUFFICIENT means do nothing.
4. GUARDRAILS (non-negotiable): ONE heavy DB op project-wide; :35–:42 blackout; no capital, no keys, rail paper/DORMANT; commits local, never push; no new signal hunts (the backlog's closing rule stands).
5. RECORD + PACE: FASTTRACK cycle-log tick + TaskList each cycle; /remember before any >1h idle stretch. ScheduleWakeup: build/lenses in flight → 1500s; only watching prod + accrual → 2700-3600s. STOP the loop when the reward-instr bundle is merged + staged (or the lane is recorded blocked) and prod is steady: write a final tick + /remember + status summary ending with the operator's pending actions (deploy the bundle; read the gate at sufficiency ~07-08→12; push is your call) — the deadmen page on real prod problems and POST-FABLE-HANDOFF.md carries the rest.
```

## (v2, WS-3 sprint — superseded) The autonomous /loop prompt (paste as-is; self-paced — no interval)

> v1 (below, kept for reference) drove the day-0 build sprint. v2 reflects the board after 15:20Z
> 07-03: WS-1/2/4/5/6 DONE + DEPLOYED; what remains is WS-3 execution/adjudication, verdict recording,
> and the accrual/health watch.

```
/loop Polyweather FASTTRACK orchestrator v2 (post-day-0; Fable window closes ~2026-07-06/07). You are PLANNER/ORCHESTRATOR/CHECKER/ADJUDICATOR ONLY — you never write bulk code, run long jobs, or draft docs yourself; Agent(model:'sonnet') does, in worktrees for code and the MAIN tree only for cache-consuming runs; model:'haiku' for trivial mechanical work. Files are the state: read FASTTRACK-PLAN.md (workstream statuses), POST-FABLE-HANDOFF.md (steady-state config — do not "fix" it without evidence), SIGNAL-BACKLOG.md (item specs + pre-registered gates), and TaskList, every cycle.

EACH CYCLE, in order:
1. RECOVER + INTEGRATE: git status (expect 5 local commits ending b31917e + the parallel agent's uncommitted signal-backlog work — do not commit or clobber files you don't own); integrate any finished background agent FIRST (verify: diff scope matches brief · exact suite+typecheck counts reported green · no prod touches · gate pre-registered BEFORE measurement) — accept into the plan/docs or reject with ONE corrective re-brief.
2. PROD HEALTH (cheap probes only, never during :35–:42): last 2 job_runs each for opening-capture (*/10, bar: ok/<60s/inserted>0), maker-exit-panel (hourly :35, bar: ok + snapshotId + nMarkets≥40 scope), convergence-panel (*/15, bar: ok/<60s/cityErrors≤2). One failed tick = note and watch; two identical failures = the incident playbook (throttle capture → pause the offender → let IO refill → re-enable staggered) — that ops set is pre-authorized; NEW migrations/fn deploys/destructive SQL are NOT (stage + ask the operator).
3. ADVANCE WS-3 (the only build lane left) — coordinate, don't duplicate: the operator's parallel agent owns 3b/3c unless SIGNAL-BACKLOG.md + git show no progress for >3h, in which case dispatch your own sonnet runner per the backlog's own spec. Order: 3b (1b reward-stacking sweep {0.05,0.10,0.25,0.50} + basket 1/2/3, --from-cache on the 844-event cache, LOCAL compute only, judge 1b on ciLow movement / 5 on jackknife-fragility reduction) → 3c (items 2–4 ONE pull + three splits, heavy DB — serialize globally, off :35–:42) → 3d (item 6 cheap check, expect KILL) → 3e (item 10, only if idle). YOU adjudicate every result against its PRE-REGISTERED gate — never let a runner self-declare PASS/KILL — then record: FINDINGS.md row + SIGNAL-BACKLOG.md status + a one-line FASTTRACK tick (you are the single writer for FINDINGS/SIGNAL-BACKLOG/BUILD-STATE/MAKER-EXIT-SIM/FASTTRACK).
4. REVIEW anything that touches engines or SQL with 1-2 sonnet lenses before accepting (the day-0 pattern: reviewers report SEVERITY·file:line·claim·evidence; you adjudicate; stop at zero code defects). Pure analysis scripts reading existing data need one lens only.
5. GUARDRAILS (non-negotiable): ONE heavy DB operation project-wide at a time; :35–:42 blackout; no capital, no keys, rail stays paper/DORMANT — a forward-gate PASS is an OPERATOR decision, never yours (POST-FABLE-HANDOFF.md §adjudicate); commits stay local; never push; never touch the parallel agent's uncommitted files.
6. RECORD + PACE: update TaskList + FASTTRACK ticks each cycle; BUILD-STATE addendum at day boundaries; /remember before any sleep >1h-equivalent idle stretch. ScheduleWakeup: agents in flight → 1500s fallback; only waiting on the hourly :35 tick or the parallel agent → 1800-2700s; mid-integration → continue now. STOP the loop (no wakeup) when: WS-3 verdicts are all recorded and no lane has work — write the final BUILD-STATE tick + a /remember entry and end with a status summary; the deadmen page the operator on real prod problems, the gate accrues by itself (~sufficiency 07-08→12), and POST-FABLE-HANDOFF.md carries the rest.
```

## (v1, day-0 — superseded) The autonomous /loop prompt (paste as-is; self-paced — no interval)

```
/loop Polyweather FASTTRACK orchestrator — Fable window closes ~2026-07-06/07. You are PLANNER/ORCHESTRATOR/CHECKER ONLY: you never write bulk code, tests, or doc drafts yourself — cheaper models do; you plan, brief, review, adjudicate, integrate. Files are the state: read FASTTRACK-PLAN.md + TaskList first every cycle.

EACH CYCLE, in order:
1. RECOVER: git status; FASTTRACK-PLAN.md workstream statuses; cheap prod probes only (cron.job actives; last 2 job_runs for opening-capture + maker-exit-panel; 07-02+ grading count). Integrate any finished background agent FIRST: verify (diff scope matches brief · suite+typecheck counts reported green · no prod touches · gates pre-registered) → accept into the plan or reject with a corrective re-brief. Never patch a rejected deliverable by hand.
2. ADVANCE the highest unblocked gate: WS-1 build → 3-lens adversarial review (parallel Agent calls, model sonnet: migration-SQL / edge-pipeline / tests-and-twins; YOU adjudicate findings, 1-2 rounds, stop at zero code defects) → stage the operator deploy bundle. WS-2 when 07-02 graded ≥44/45: dispatch a sonnet agent to run cache rebuild + pinned baseline + jackknife and return raw outputs; YOU judge vs §9R-E and write the MAKER-EXIT-SIM.md banner. WS-3c once prod stable ≥1h; WS-3b after the new cache; then 3d, 3e. WS-5 build anytime a lane is idle. WS-6 last.
3. DELEGATE every build/run/draft to Agent(model:'sonnet', isolation:'worktree' for code) with: the exact FASTTRACK-PLAN.md section pasted in, file-level deliverables, acceptance criteria, "run pnpm test + pnpm typecheck, report exact counts", and "no prod access, no commits, don't touch BUILD-STATE/FINDINGS/MAKER-EXIT-SIM/SIGNAL-BACKLOG". Trivial mechanical work → model:'haiku'. YOU alone touch: the four single-writer docs, prod SQL (cheap probes + already-authorized cron/config repairs only), verdict adjudication, operator bundles.
4. GUARDRAILS (non-negotiable): ONE heavy DB operation at a time project-wide; :35–:42 every hour is panel-tick blackout for heavy reads; migrations/fn deploys/destructive SQL are OPERATOR-GATED — stage, never apply; convergence-panel cron stays paused until WS-1 is deployed; no capital, no keys, rail stays paper/DORMANT; commits local only.
5. RECORD: tick statuses in FASTTRACK-PLAN.md + tasks every cycle; BUILD-STATE addendum at day boundaries; /remember before any long idle. When an operator bundle is ready, lead the cycle summary with it in bold and keep other lanes moving.
6. PACE via ScheduleWakeup: background agents in flight → 1500s fallback; blocked only on venue grading → 1800s; mid-integration → continue now. STOP when all workstreams are done or operator-blocked, or the window closes → write POST-FABLE-HANDOFF.md + a final memory entry, then end the loop.
```

## Orchestration notes

- Executor agents get ONE workstream each, in a worktree, with this file + the workstream section as
  the brief. The orchestrator serializes every DB-heavy step on a single global lane and owns the
  four single-writer docs.
- Review pattern: adversarial multi-agent for anything touching prod SQL or the engines (the project's
  asymptotic-findings experience says 1–2 rounds, stop on zero code defects).
- The operator's actions are batched into at most one bundle per day (0077+deploys+prune is Day 1's).

## The autonomous /loop prompt — v13, GATE-UNLOCK + STEADY-WATCH (written at C72 wind-down, 07-06 ~00:45 local — token-constrained close at 95% Fable; run on any model, subagents default Opus)

```
/loop Polyweather GATE-UNLOCK + STEADY-WATCH v13 (from 07-06; operator reachable; capital/live OPERATOR-PHYSICAL; never set TRADE_MODE=live). FILES ARE THE STATE — read FIRST: FASTTRACK-PLAN.md cycle log C53–C72 (incident + review + fix-wave + resize + bundle + outage arc), docs/ops/GATE-DAY-PLAYBOOK.md, docs/ops/TRADING-ACTIVATION.md §12. BOARD: main == origin @ C72-final (verify git log; suite 164 files/2752 green, NO code changes since — docs-only commits C70–C72; all C64 review-fix lanes integrated: 39fc2e4 · a867439 · fdcdf1e · 83e41e8). §12 BUNDLE EXECUTED (0084 APPLIED + maker-exit-panel + convergence-panel DEPLOYED ~19:55Z 07-05 — 45 s city timeout + degraded-tick gate-write skip LIVE). DB: Micro ACTIVE since 18:01:20Z 07-05, CPU real; tell the operator to STOP any further resize pushes (they fail against the platform incident and a success would just restart the DB). NEVER query cron.job_run_details (retention job 30 handles it, 03:15Z).

OUTAGE (the blocker at v12 close): pg_net outbound DOWN since ~19:55Z 07-05, STILL DOWN at 22:43Z close (~2h50m, 4th+ hour possible; signature shifted to full-DNS hang — net._http_response rows through id 164 ALL timed_out at the 4 500 ms cap; NO edge fn invoked since 19:47:09Z 07-05 — capture, whale-watch, ensembles, panels ALL stopped; crons keep firing and self-retry, so recovery needs NO action). Deadman pages = same incident. The 07-05 22Z snapshot-ensembles wave was LOST (job_runs empty 21:45→22:43Z) → the 07-05 10Z gap did NOT heal; on recovery just verify the next waves run ok.

GATE (THE HEADLINE — unchanged, the answer is one healthy tick away): n_distinct_days=7 CONFIRMED, n_open 0 on every post-resize row — the day-7 verdict EXISTS; only a full-panel read is missing (nMkts≈73, cErr≤2). FIRST ACTIONS each cycle until recovered: ① net._http_response ORDER BY id DESC LIMIT 3 (200/202 = path back) + job_runs latest (scope WHERE started_at > now()-interval '2 hours' — unscoped sorts time out); ② once ticks flow, the FIRST clean full-panel tick (fixed handler: ~120 s expected, partials show stats.gateWriteSkipped='degraded' and can no longer write gate rows) adjudicates → execute GATE-DAY-PLAYBOOK.md EXACTLY (KILL → pre-authorized full record: FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13 + commit+push, LEAD with it; PASS → present + STAGE the §9R decision doc ONLY — makerFillRate observed 0.09–0.22 vs 0.49 backtest, §3 mechanism-check applies in full; INSUFFICIENT → note accrual, keep riding). ONE manual jobid-28 re-fire allowed once the path is proven back but no tick ran (SELECT command FROM cron.job WHERE jobid=28 → EXECUTE it) — it was NEVER used in v12, still banked.

EACH CYCLE ALSO: 1. #50 CLOSE (first cycle ≥10:05Z 07-06): the 10:00Z city-paper-trade tick must show stats cities:4 / placed>0 → closes the 0081 thread (07-05 gap-fill of 21 bets/4 cities already in). If 10:00Z falls inside the outage, the FIRST post-recovery daily tick is the proof. 2. DAEMON WATCH: detached dry-run daemon UP on fixed code through the whole outage (honest degraded ticks, 17 positions ledger-managed, 0 failed — 83e41e8 working as designed; discovery reads statement-timeout while the DB is sluggish, self-heals). Log out/trade-bot-detached.log; find via powershell Get-CimInstance CommandLine match trade-bot; if trade-bot.fatal/ECHECKOUTTIMEOUT → retry: powershell Start-Process -WindowStyle Hidden cmd -WorkingDirectory repo -ArgumentList "/c","set TRADE_MODE=dry-run&& set TRADE_TICK_SEC=60&& pnpm tsx scripts/trade-bot.ts >> out\trade-bot-detached.log 2>&1". Shadow-week: live_orders growth normal / 0 failed expected; next trade-shadow-diff.ts read ~07-07. 3. GUARDRAILS: no capital/keys/live-mode; no migration applies/deploys without explicit operator go; suite+typecheck green before any CODE commit (docs-only cycle-log commits exempt by precedent); subagent default Opus; backlog CLOSED, no new signal hunts. 4. RECORD+PACE: C-tick each cycle (bundle docs-only commits); wakeups 2700–3600s steady / 1800s when a tick or verification is in flight; WIND-DOWN on operator's word: final tick + v14 prompt + /remember + summary ending with operator items.

Operator items standing: ① **LIVE WRITE PATH UN-BLOCKED — the `@polymarket/clob-client-v2` migration is BUILT + fresh-eyes REVIEWED (MERGE-CLEAN, all 10 items PASS, 0 CRITICAL/HIGH, 5 LOW notes → C75 micro-lane) + MERGED; the detached dry-run daemon was restarted onto v2 by the orchestrator (~00:15Z 07-06). Your ONE action: re-run `--live-smoke` — step 4 now expected to POST-then-cancel OK (the write-path PROOF, pending an actual run). The 07-06 `400 invalid order version` (the CLOB V2 exchange cutover, EIP-712 domain 1→2) is fixed by v2's runtime version resolution; v2 exposes a real `post_only` but we deliberately don't pass it (maker-ness stays price-enforced; adopt only after a live fill proves the migration);** ② STOP further compute-resize pushes (Micro already active); ③ compute downsize decision after the shadow week (~07-12; paying Micro now); ④ optional --live-smoke ($env: form, reset TRADE_MODE after — now on the v2 client, see ①); ⑤ opening_captures VACUUM/prune stays late-July; ⑥ if the outage passes ~6h with prod otherwise healthy, consider a Supabase support ticket referencing the platform incident (pg_net outbound DNS from eu-north-1).
```

## The autonomous /loop prompt — v12, GATE-UNLOCK + STEADY-WATCH (written at C67 wind-down, 07-05 ~21:50 local)

```
/loop Polyweather GATE-UNLOCK + STEADY-WATCH v12 (from 07-05 late evening; operator reachable; capital/live OPERATOR-PHYSICAL; never set TRADE_MODE=live). FILES ARE THE STATE — read FIRST: FASTTRACK-PLAN.md cycle log C53–C69 (incident + review + fix-wave + resize + bundle arc), docs/ops/GATE-DAY-PLAYBOOK.md, docs/ops/TRADING-ACTIVATION.md §12. BOARD: main == origin @ C69-final (verify git log; suite 164 files/2752 green; ALL C64 review-fix lanes integrated: 39fc2e4 replay-precedence · a867439 venue-truth CRITICAL · fdcdf1e gate-sql · 83e41e8 daemon-robustness). **§12 BUNDLE EXECUTED on operator go (C68, 07-05 ~19:55Z): migration 0084 APPLIED to prod + maker-exit-panel + convergence-panel DEPLOYED (45 s city timeout + degraded-tick gate-write skip are LIVE).** DB: Micro ACTIVE since 18:01:20Z 07-05, CPU verified real; the operator was pushing further resizes at close — they FAIL against the platform incident and are unnecessary (tell him to stop if it recurs; a success would just restart the DB). NEVER query cron.job_run_details (retention = cron jobid 30, 03:15Z nightly).

GATE (THE HEADLINE — the answer is one healthy tick away): n_distinct_days=7 CONFIRMED with n_open 0 on every post-resize row — the day-7 verdict EXISTS; only a full-panel read is missing (nMkts≈73, cErr≤2). AT v11 CLOSE (~20:45Z 07-05) the blocker was a pg_net outbound DNS/TLS outage from ~19:55Z (3rd platform-weather wave; net._http_response rows all timed_out at the 4 500 ms cap; NO edge fn invoked after ~19:47Z incl. capture + whale-watch — NOT the deploy's fault). FIRST ACTIONS: ① check net._http_response ORDER BY id DESC LIMIT 3 (status 200/202 rows = path recovered) + job_runs latest maker-exit-panel; ② once ticks flow, the FIRST clean full-panel tick (fixed handler: expect ~120 s, or stats.gateWriteSkipped='degraded' on partials — partial gate rows can no longer be written) adjudicates → execute GATE-DAY-PLAYBOOK.md EXACTLY (KILL → pre-authorized full record: FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13 + commit+push, LEAD with it; PASS → present + STAGE the §9R decision doc ONLY — makerFillRate observed 0.09–0.22 vs 0.49 backtest, the §3 mechanism-check applies in full; INSUFFICIENT → note accrual and keep riding). ONE manual jobid-28 re-fire allowed once the net path is proven back but no tick ran (DO block: SELECT command FROM cron.job WHERE jobid=28 → EXECUTE). Deadman pages during the outage = expected staleness, same incident.

EACH CYCLE ALSO: 1. 22Z PIPELINE (first cycle ≥23:00Z 07-05): verify the 22:15/22:35/22:50 snapshot-ensembles ran ok (heals the 07-05 10Z statement-timeout gap; job_runs only). 2. #50 CLOSE (first cycle ≥10:05Z 07-06): the 10:00Z city-paper-trade tick must show stats cities:4 / placed>0 — that closes the 0081 thread; the 07-05 gap-fill (21 bets/4 cities) is already in. 3. DAEMON WATCH: detached dry-run daemon runs on the fixed code (log out/trade-bot-detached.log; find via powershell Get-CimInstance CommandLine match trade-bot); if trade-bot.fatal/ECHECKOUTTIMEOUT → retry: powershell Start-Process -WindowStyle Hidden cmd -WorkingDirectory repo -ArgumentList "/c","set TRADE_MODE=dry-run&& set TRADE_TICK_SEC=60&& pnpm tsx scripts/trade-bot.ts >> out\trade-bot-detached.log 2>&1". Shadow-week ledger: live_orders growth normal, 0 failed expected; re-run scripts/research/trade-shadow-diff.ts every ~2 days (first read 07-05 was CLEAN, entry-dims only). 4. GUARDRAILS: no capital/keys/live-mode; no migration applies/deploys without the operator's explicit go (§12 = the one pre-authorized-on-go bundle); suite+typecheck green before any commit; subagent default opus (Fable on operator's word); backlog CLOSED, no new signal hunts. 5. RECORD+PACE: C-tick each cycle (bundle docs-only); wakeups 2700–3600s steady / 1800s when a tick or deploy verification is in flight; WIND-DOWN on operator's word: final tick + v13 prompt + /remember + summary ending with operator items.

Operator items standing: ① say "deploy the bundle" → the §12 apply+redeploy unlocks the gate answer within ~1 tick; ② compute downsize decision after the shadow week (~07-12; now paying Micro); ③ optional --live-smoke ($env: form, reset TRADE_MODE after); ④ the opening_captures VACUUM/prune stays late-July.
```

## The autonomous /loop prompt — v11, RECOVERY-WATCH + GATE-DAY completion (written at C59 wind-down, 07-05 ~15:45 local)

> Paste after /clear. Full text also in the session-close summary.

```
Polyweather RECOVERY-WATCH + GATE-DAY completion v11 (day 07-05 PM; operator reachable; capital/live OPERATOR-PHYSICAL; never set TRADE_MODE=live). FILES ARE THE STATE — read FIRST: FASTTRACK-PLAN.md cycle log C53–C59 (TODAY's incident arc — the canonical record), docs/ops/GATE-DAY-PLAYBOOK.md, docs/ops/TRADING-ACTIVATION.md (§11 both CLOSED today), POST-FABLE-HANDOFF.md. BOARD: main == origin @ 04b13ec+ (verify git log; suite 161 files/2609 green; today merged: 7e9604e bootstrap-log suppression · e45e6b7+62579d7 both §11 follow-ups lens-MERGE-CLEAN). INCIDENT CONTEXT (C54–C58): the DB CRASHED 07:36:03Z (CPU-burst-credit exhaustion; Supabase platform incident "Project status change failures" ONGOING since Jun 30 incl. eu-north-1 — status.supabase.com); operator pushed a compute resize to 2-core/1GB TWICE, NOT yet verified applied (postmaster never cycled — possibly a cgroup-style resize; dashboard verification pending); ALL crons RESTORED ~13:06Z (capture */10 job 21 · convergence 2,17,32,47 job 26 · maker-exit :35 job 28 · side-rails 17/19/20 active); panel record-RPCs (record_convergence_panel/record_maker_exit_panel) were failing 15s statement timeouts on the starved instance — EXPECTED to keep failing until real CPU; treat those as PLATFORM WEATHER (not the 2-consecutive playbook) until the resize is CONFIRMED, then normal probe rules resume. Deadman pages = same incident, expected until a panel tick succeeds. NEVER query cron.job_run_details (huge, unindexed — caused a 102s stall). Daemon: operator's copy died 07:46Z; the orchestrator's stopgap died at /clear → PRESUME NO DAEMON unless operator says his is up (his PowerShell form: cd repo; $env:TRADE_MODE = "dry-run"; pnpm tsx scripts/trade-bot.ts). Ledger at close: 7 placed/9 canceled dry-run rows (07-05 + 07-06 entries), 0 failed.

EACH CYCLE: 1. RESIZE/HEALTH CHECK: fast heavy-query test (pg_stat_user_tables + pg_total_relation_size on opening_captures — instant = CPU real; timeout = still starved) + ask/await operator dashboard confirmation (Settings→Compute and Disk shows 2-core ACTIVE). Do NOT re-push resizes; do NOT manual-tick panels while starved. 2. GATE (THE HEADLINE): on the first healthy maker-exit tick (scheduled :35, or ONE manual re-fire via the jobid-28 DO-block ONLY after CPU is confirmed real) read bot_gate_snapshot mode='paper' latest FULL-panel row (n_markets≈73+, cErr≤2 — snaps 221 was a 22-mkt partial view, NOT adjudicable): n_distinct_days ≥7 → execute GATE-DAY-PLAYBOOK.md EXACTLY (KILL → pre-authorized full record: FINDINGS closing block + CLAUDE.md banner + SIGNAL-BACKLOG §13 + commit+push, LEAD with it; PASS → present + STAGE the §9R decision doc ONLY — note the C58-era makerFillRate was 0.082 vs 0.49 backtest, §3 of the playbook's PASS package applies in full; INSUFFICIENT → note accrual). 3. #50 RECOVERY: today's 10:00Z city-paper-trade cron slot was LOST (pg_cron dead) — once DB healthy, gap-fill target 2026-07-05 via the C45/C49 scripts/city-sim.ts idiom + verify rows/banner; the CRON-SLOT proof (cities:4/placed>0) = tomorrow's 10:00Z tick at ≥10:05Z. 4. SHADOW-DIFF FIRST READ (eligible): once DB healthy run pnpm tsx scripts/research/trade-shadow-diff.ts (read-only, local env creds) → report the divergence table FIRST, never code-chase unreported; expect entry-dims-only. 5. 22Z PIPELINE: the 10Z snapshot-ensembles failed (10:35Z statement timeout) — verify tonight's 22:15/22:35/22:50 pipeline runs ok (heals the gap). 6. SHADOW-WEEK watch: live_orders growth/status mix (placed accumulating = correct; failed burst = investigate w/ operator) + remind operator re durable daemon if down >2h during listing hours. 7. OPERATOR-GATED items to offer, not execute unbidden: the cron.job_run_details retention job (SQL staged in C-log/summary — apply on his "approved" once DB healthy) · the deferred VACUUM/prune of opening_captures (1.34GB, late-July due) · compute downsize decision after the shadow week · optional --live-smoke (his terminal, $env: form, reset TRADE_MODE after). 8. GUARDRAILS unchanged: no capital/keys/live-mode; no migrations/deploys beyond explicit approval; suite+typecheck green before any commit; subagents opus; backlog CLOSED, no new signal hunts. 9. RECORD+PACE: cycle tick each cycle (bundle docs-only when possible); wakeups 1800–2700s while recovery pending, 2700–3600s once green; WIND-DOWN on operator's word or ~20:00Z: final tick + /remember + summary ending with operator's open items.
```

## Orchestrator cycle log (v2 loop, from 2026-07-03 ~17:30 local)

- **C100 (~13:50–14:35Z 07-07 — GOOGLE PANEL multi-exit BUILT+COMMITTED+PUSHED (Vercel /convergence cutover live; 2 Supabase steps operator-pending); Karachi entry+exit RE-TEST done; session wound down for /clear):** Operator directives executed. **① GOOGLE PANEL (Test 2):** operator said override the killed-convergence caveat ("new data-sources now"), approved + push live. Modified the Google-picks-bucket panel to **sweep 5 TP exit variants {0.30,0.35,0.40,0.45,0.50}** over a **FIXED entry** so the favourable exit is read off after: entry = buy the Google bucket when **execAsk ≤ 0.15** (operator-confirmed "below or exactly 15c"; the ">15c" was a typo), **no SL** (slAbs≤0 sentinel → hold-to-resolution floor). `buildGoogleView` carries a per-TP-variant comparison block; /convergence renders the comparison table. Migration/RPC/cron UNTOUCHED (flexible jsonb view). Commits `b00c708` (google multi-exit) + `0896543` (karachi re-test); suite **2926 green**, tsc 0; **PUSHED `facf457..b00c708`** → Vercel auto-deploys /convergence to the Google panel (degrades to "filling in" until the 2 supabase steps). **OPERATOR TODO to finish the deploy (CLI/RUNBOOK, as always): (1) apply migration `0086_google_paper.sql`; (2) deploy the `google-paper-panel` edge fn.** Until both, the page shows "filling in" (getGooglePaper null-degrades, no 500). First snapshot ≤15 min after (>/15 cron). **② KARACHI RE-TEST (Test 1, `0896543`, `scripts/research/karachi-entry-exit.ts` +18 tests):** 130-config grid (cheap-entry × TP/SL) on the 49-event triple → **the lever is the ENTRY, not the exit**: cheap-entry (~21¢ at open) + plain HOLD = +1.27/$1 on MID (higher than any TP); 14:00-hold baseline −0.37. BLUNT: MID not ask; the TP-exit is the killed convergence-bracket (dies at real spread); HOLD is spread-safest (zero exit spread) — no credible tradable edge, but 14:00-hold loses purely because 14:00 is late/expensive. **Operator PINNED this discussion for after /clear.** **③ LIVE KARACHI TEST unchanged:** trade_config mode='live'/stake $5, but preflight('city-taker') still ok:FALSE (active_until null + 0 arms) → NOTHING places; daemon healthy dry-run single instance. Karachi `city_sim_config.arm_hours=[10..15]` (so an overnight live entry needs an arm_hours change — flagged). Session-close: /remember written; next session opens with the KARACHI discussion (Test 1 findings) + continues the live-test /loop. Boundary intact throughout: no capital, no keys, nothing traded; the only prod action was the operator-authorized web PUSH (paper analytics).
- **C99 (~12:40–13:47Z 07-07 — THREE operator-directed ANALYSES built+committed in the live-test idle window (LOCAL ONLY, unpushed; paper/analysis, boundary intact); operator moved to Phase B (mode='live'/$5) but rail still gated:** In the ~20h before the Karachi 09:00Z window, the operator directed three tests. All built, full-suite-green (174 files), committed direct to main, **NOT pushed** (main ahead of origin 4 commits: add5a08 C98 · 3b4107b · 8a1e0e2 · 1a686be — pushing 1a686be is the /convergence Vercel cutover). Two prior Opus Explore agents mapped the data+code landscape first. **① TEST C (3b4107b) — `scripts/research/winner-band-prices.ts` + test (10):** across the 46-city market-history archive (6,273 resolved events), the winning bucket ±2° min/max/range/σ → CSV + WINNER-BAND-ANALYSIS.md. Finding: the eventual winner dips to a **median 12.5¢** (traded <18¢ in 66% / <30¢ in 90%), cheapest ~50h pre-resolution — BUT the ±neighbours are cheap 99.8% *because they lose* (cheap≠edge; picking the winner is the forecast's job). **Karachi is a HARD buy-cheap market** (winner <18¢ only 45% vs 85-97% for the Chinese cities). **② TEST 1 (8a1e0e2) — `scripts/research/karachi-entry-time.ts` + test (16):** joins forecast-by-event.csv (pred_bucket_l0) to the 1-min Karachi path + resolvedOutcome (49-event triple); day-of hourly sweep of buy-our-bucket-hold-to-resolution netReturn (mid) + a wrong-prediction signal. **HEADLINE — decision-relevant to the LIVE test: buying at 14:00 (the live test's hour) and holding is NEGATIVE-EV, mean net −0.37/$1 on MID** (by 14:00 the winner is priced ~88¢; ~52% losses cost −1). The edge is overnight (01-05h, winner still ~41¢) but only breakeven on mid (bootstrap-LB +0.01); real ask likely negative. Hit rate 51% (l0), EVENT-level (hour never changes whether we're right). Wrong-signal gap-to-favorite<0 skips 16 losers / 0 winners @14:00 (100% precision) but strong only because it's late → an early-exit trigger, not a standalone edge. **Operator told bluntly + given the tension with the +6.4pp paper edge (different metric); their call (sovereignty; $5/day = buying information).** **③ TEST 2 (1a686be) — BUILT-DARK, the Google-picks-bucket forward paper panel replacing /convergence** (Opus build agent, reviewed by me — engine + migration audited, page degrades gracefully): core `google-bucket-replay.ts`/`google-bucket-view.ts` (+24 tests) — pure taker, buy the Google-forecast bucket when execAsk<0.18, TP execBid≥0.30, SL≤0.15, else hold; forks opening-bracket-replay (absolute TP/SL, no-look-ahead exit from the tick AFTER the fill). Migration `0086_google_paper.sql` (google_paper_panel + record + google_paper_inputs + dash_google_paper + google-paper-panel */15 cron; leaves 0069 convergence intact until cutover). Edge fn `google-paper-panel` (all 45 cities). /convergence page rewritten + getGooglePaper loader + nav label. migrations.test.ts manifest updated (86 files/30 crons/dash_google_paper). **Google = ~1-week forward SEED (35/45 cities, 7 resolved days from 06-30) → reads INSUFFICIENT for weeks by design.** OPERATOR DEPLOY (gated, in order): apply 0086 → deploy edge fn → wait one 15-min tick → push web to Vercel (the /convergence cutover). **Data note (operator-confirmed): minute-by-minute Polymarket data already local at `scripts/research/out/market-history/` (fidelity=1, 46 cities, resolvedOutcome baked in) — no new collection needed; MID only (bid/ask is the 10-min opening_captures).** **Live test unchanged:** operator set trade_config.mode='live' + stake $5, but preflight('city-taker') still ok:FALSE (active_until null + 0 arms) → NOTHING places; earliest window 07-08 09:00Z; daemon healthy dry-run single instance. Boundary intact: no capital, no keys, nothing deployed/pushed.
- **C98 (~12:11–12:23Z 07-07 / 17:11–17:23 Karachi — PHASE A ① DONE: dry-run daemon RELAUNCHED + verified HEALTHY (single instance); KEY TIMING FINDING: today's Karachi window already CLOSED → earliest placement = TOMORROW 07-08 09:00Z; docs-only, no capital):** Kicked off the v16 LIVE-KARACHI loop. **① Verified rail state via MCP:** `trade_config.mode='off'`, `active_until=null`, stake default $10 / loss-kill $30 (untouched since 07-05); **`city_live_arms` EMPTY** (no Karachi arm yet); `trade_live_preflight('city-taker')` → `ok:false` (reasons: mode off / no active_until / no enabled arm — all expected). Note `dash_city_live()` + `dash_trading()` are **operator-guarded** (`operator_guard()` → `ERR_FORBIDDEN` via the MCP service role) → verify live-rail state via the **base tables** (`city_live_arms`, `live_orders`, `trade_config`) + `city_promotion_board`, NOT those RPCs. **② KEY OPS FINDING — the entry gate is STRICT HOUR-EQUALITY** (`scripts/lib/city-live-decide.ts:204`: `if (localHour(tz,now) !== entryHour) → off_hour skip`): the lane places ONLY during the exact 14:xx Karachi clock hour (= 09:00–09:59Z), NOT "at or after." It was **17:1x Karachi** at loop start → today's 14:00 window passed ~3 h ago → **NOTHING can place today (dry-run or live); earliest possible placement = tomorrow 07-08 09:00Z.** (14 is a valid sim arm — migration 0071 widened arms to {10..15}.) **③ Daemon relaunch (Phase A ①):** the daemon was DEAD (last tick 06:18:27Z, clean stop — the earlier degraded ticks were the tail of the 07-06/07 Micro-saturation; recovered before stopping); PID 42052 was only the `gsd-read-injection-scanner` hook, not the daemon. First detached launch DIED silently on a **launcher-script bug** (a Bash `printf` interpreted `\03`→octal-null and `\t`→TAB in the `.cmd` body → `cd /d` hit a bad path + the log-redirect target was garbage) — **left NO stray daemon** (process audit: no trade-bot proc created at 12:11). **Fixed** the launcher with a quoted heredoc (no escape interpretation) + CRLF (`scratchpad/launch-daemon-dryrun.cmd`) → relaunched detached (cmd PID 89948, 12:19:43Z). **Verified HEALTHY:** `trade-bot.start` 12:19:46Z (mode=dry-run, tickSec=60, karachi in the 46-city list) then clean ~62 s ticks (12:19:48 / 12:20:50 / 12:21:51 / 12:22:53Z, all `degraded:false`, city-lane "no enabled city arms", maker-exit holds 21 dormant positions posting nothing). **Process audit confirms EXACTLY ONE trade-bot daemon** (chain 89948→pnpm 102200→tsx 46112→node 74296). Appends `out/trade-bot-detached.log`. **Housekeeping flag (operator):** 3 orphaned `scripts/research/opening-spike.ts --days 8` node processes are running (NOT the daemon, NOT mine — pre-existing, likely a stuck/duplicated prior research run) — kill if unintended. **Operator TODO (Phase A ②, anytime before tomorrow 09:00Z):** on /trading → City Live arms, enable **Karachi**, stake **$5**, `entry_hour_override` **14**; keep **exactly one** dry-run daemon alive to 09:00Z (a durable copy replaces mine if the session ends). **Do NOT go live until a clean dry-run Karachi placement is observed** (the v16 Phase-A gate). Boundary intact: dry-run only, no capital, no keys, nothing live; suite untouched (docs + local ops file only).
- **C97 (~14:00 local 07-07 / 12:00Z — OPERATOR AUTHORIZED A LIVE REAL-CAPITAL TEST ON KARACHI (city-taker); v16 loop prompt written; boundary intact, no capital moved, docs-only):** Having heard the WATCH-not-promoted verdict (Karachi 14:00 arm: edge +6.4pp but edgeCiLo −3.1pp / CI includes zero, entry-watch provisional, net +$10.24/25d — within 45-city selection-effect noise, paper = no fees), the operator exercised sovereignty and directed a **LIVE test: buy ONE city-taker stake/day on Karachi at the best hour (recommended 14:00 Asia/Karachi = 09:00Z), hold to resolution, aim for profit.** SCOPED exception to the dormant-rail default (cf. the C78 CITY-LIVE directive) — promotion is ADVISORY, the live test IS the real gate. **Verified the rail is deploy-ready end-to-end:** 0082+0085 applied (`city_live_arms`/`trade_config`/`trade_live_preflight`/`dash_trading`/`dash_city_live` all exist); the executor is the **`scripts/trade-bot.ts` DAEMON city lane** (Lane X `decideCityTick` → FAK `city-taker`, once/city/day, `shares=stake/ask`, hold-to-resolution, isolated so a city fault never breaks the dead maker-exit lane); `scripts/lib/city-live-decide.ts` + the lane wiring + the TRADE_MODE ladder (default dry-run; live needs BOTH `TRADE_MODE=live` AND the DB interlock; key lives only in `packages/trading/src/live.ts`) confirmed on main. **preflight('city-taker') does NOT require a gate PASS or promotion** (source: reads only `mode='live'`, `active_until≥today`, no daily-loss kill, ≥1 arm enabled / stake ≤$5 / ≤2 cities) — so Karachi-WATCH goes live purely by operator toggle, by design. **This REVERSES the C94 daemon-stopped rule** (that stop was for the DEAD maker-exit lane; the city lane is now the point) → relaunch DRY-RUN first, verify a clean dry-run Karachi placement at ~09:00Z, THEN the operator funds a dedicated wallet + key + `mode='live'` + `TRADE_MODE=live`. **Boundary UNCHANGED + NON-NEGOTIABLE:** operator funds/keys/toggles/sets-live-mode (physical); Claude verifies/monitors/reports and NEVER places a trade, touches the wallet/key, sets TRADE_MODE=live, or reads `.env.local`. Wrote the v16 loop prompt (below) + this record. Suite untouched (docs-only); no capital, no deploy.
- **C96 (~09:00–11:15 local 07-07 / 07:00–09:15Z — EXECUTED the C95 handoff: `opening_captures` dumped (raw bid/ask) → aggressively pruned → `VACUUM FULL` → 1.37 GB → 75 MB, 95% reclaimed; forecasting pipeline untouched; loop steady-watch, daemon STAYS STOPPED):** The C95 next-session task, done end-to-end. **① BUILT `scripts/ops/dump-opening-captures.ts`** (+ 7-test twin): the price-path archive has no bid/ask, so this dumps the raw order book keyless — keyset (bigint `id`) batches, `to_jsonb(oc.*)` lossless rows (buckets bid/ask + the 0083 identity cols), gzipped-NDJSON shards + a resumable manifest. A full-table read statement-timeouts under Micro contention (the C91/C94 wall), so batches ARE the fix; `fetchBatchAdaptive` halves the batch on 57014 and backs off + retries a post-cancel malformed result (a real bug the first run hit: postgres-js returned a misaligned result on a connection still draining a cancelled query → crashed on `cursor_id`; fixed with backoff + shape-guard + default batch 3000→1500). `--verify` cross-checks rows/events vs the live table (index-only walk, survives contention) and stamps `manifest.verified`; writes an `_events.json` sidecar. **② RAN the dump** (resumable across the contention; ~1 h wall): **319,434 rows / 475 events / 208 shards / 569 MB gz**, verified rows+events == live table EXACTLY (the stale `reltuples` said 283k; actual live count 319k). Spot-checked: real `bestBid`/`bestAsk`/`execAsk`/`depthUsd`/tokens/`conditionId` per bucket. **③ ENHANCED `prune-opening-captures.ts`** (generalized `preflightArchive` to a `keyOf` + any `{has}` index; new `--preflight dump` gates on the VERIFIED dump's event set — the only archive with the raw bid/ask AND 100% coverage → unlocks a sub-25-day prune; `--resolved-age-days` override; default price-path path unchanged). **④ PRUNED** `--preflight dump --resolved-age-days 2 --execute`: pre-flight PASS (all 344 candidates in the dump) → **deleted 301,608 rows / 344 resolved events**, kept the recent ~2-day window (17,826 rows / 131 events; verified nothing reads `opening_captures` live — the dashboards read the `convergence_panel`/`maker_exit_panel` SNAPSHOT tables via `dash_convergence`/`dash_maker_exit`, not the capture table). **⑤ VACUUM (FULL, ANALYZE)** — extended ANALYZE→FULL deliberately: plain VACUUM ANALYZE leaves the 1.37 GB file on disk (space only reusable), but the operator's stated GOAL (C93–C95) is footprint reclaim, and post-prune the live set is tiny (17.8k rows) with NO live reader, so FULL was fast + safe + lossless (the pruned data is already dumped locally). Result **18.5 s: total 1370 MB → 75 MB** (heap 51→2.9 MB, TOAST 1266→70 MB), 17,826 live rows preserved with bid/ask intact. **⑥ VERIFIED** forecasting pipeline untouched (`forecast_snapshots` 363k / `model_stats` 6.5k / `observations` 40k). Suite **170 files / 2854 green**, typecheck clean; tooling committed local `d172c5f`. **The 1.37 GB Micro working-set — the root of the whole 07-06→07 saturation cluster — is now 75 MB, with the raw bid/ask preserved offline.** Loop mission COMPLETE; steady-watch only; daemon STAYS STOPPED (do NOT relaunch). Boundary intact: read-only dump + archive-gated prune (operator-authorized dump-then-prune) + a lossless maintenance vacuum; no capital, no keys, nothing live, no deploy.
- **C95 (~08:45 local 07-07 / 06:45Z — HANDOFF for a fresh session: operator directive = dump `opening_captures` (bid/ask) locally, THEN aggressively prune + VACUUM. /clear at a clean milestone (KILL recorded C92, compute audit C93)):** Answered the operator's "what is this data / is it forecasting-eval backdata": **NO.** `opening_captures` (prod, **1.37 GB**, 283k rows, ~46 MB heap + ~1.2 GB `buckets` jsonb TOAST) = the real ORDER-BOOK capture (bid/ask/depth per bucket, */10) for the CLOSED 12th signal (convergence/maker-exit). **No live reader** — convergence-panel + maker-exit-panel crons unscheduled (C93), trade-bot daemon stopped (C94); only on-demand research scripts read it. **Forecasting evaluation is a SEPARATE, KEPT pipeline** (`forecast_snapshots`, `bucket_probabilities`, `edge_evaluations`, `observations`, `model_stats`, `calibration_scores`) — safe, untouched. The local price-PATH archive `scripts/research/out/market-history/` (**4.4 GB, 46 cities**) already covers all sim/backtest locally, BUT it is single-implied-prob-per-bucket (**no bid/ask**) — so the raw order book is the ONE thing not archived. Operator chose **(b): dump `opening_captures` locally first (belt-and-suspenders raw bid/ask + enables a local real-book sweep), then prune.**

  **➜ NEXT-SESSION TASK (execute after /clear):**
  1. **BUILD `scripts/ops/dump-opening-captures.ts`** (check `scripts/backup-db.ts` first — may already do a table dump; `pg_dump -Fc -t public.opening_captures` via `$DATABASE_URL` is the simplest robust path and streams via COPY). If a TS script instead: read in **PK-keyed batches ≤5000 rows** (mirror `prune-opening-captures.ts` `executePrune`) → stream to gzipped NDJSON or per-event JSON under `scripts/research/out/opening-captures-archive/`; idempotent/resumable — a single full-table read STATEMENT-TIMEOUTs (that IS the contention wall), so chunk everything. Include ALL columns (esp. `buckets` jsonb + the 0083 identity cols). Get schema: `information_schema.columns where table_name='opening_captures'` (light MCP; retry — it's intermittent).
  2. **RUN the dump** (chunked, off-peak); verify dumped rowcount == `select count(*), count(distinct event_id) from opening_captures`; spot-check a file has real bid/ask.
  3. **PRUNE aggressively** — stock `prune-opening-captures.ts` `RESOLVED_AGE_DAYS=25` clears too little (only events resolved ≥25d ago; the signal is CLOSED so the 21-day panel window is moot). Lower the retention (e.g. keep ~7d for the `/convergence` + `/maker-exit` dashboards) or full-prune resolved events. Its archive pre-flight (price-path, "no archive file, no delete") still guards; if it blocks recent events, either re-pull `backfill-market-history --full-series` OR wire the pre-flight to accept the new opening_captures dump. Run `--execute` in a QUIET window (off the DB-contention peak), then **`VACUUM ANALYZE public.opening_captures`**.
  4. Verify table size dropped; C-tick + update `docs/ops/COMPUTE-AUDIT-2026-07-07.md` "Still open".
  **Guardrails:** dump is read-only/non-destructive; the prune `--execute` is the only destructive step (archive-gated). No capital/keys/live. DB still contention-prone → chunk + run heavy ops off-peak. The loop's mission is COMPLETE (steady-watch only); the daemon stays STOPPED (do NOT relaunch). Boundary intact.
- **C94 (~08:20 local 07-07 / 06:20Z — loop re-entered (operator AWAKE, mission COMPLETE → steady-watch); sweep root-caused to pooler exhaustion + the 1.37 GB table; inert daemon STOPPED):** The v15 loop's TOP PRIORITY (record the KILL) is DONE (C92); the compute audit is DONE (C93). This re-entry became: finish the sweep + cleanup. **① Sweep root cause fully isolated.** Two more retries crashed identically: config read `ECHECKOUTTIMEOUT` → 10-city fallback → per-city `ECHECKOUTTIMEOUT` → `CONNECTION_CLOSED` at ~90s. That is **Session-mode pooler connection EXHAUSTION** (not compute) — Micro's pooler has few connections and the persistent dry-run daemon held some. **② STOPPED the inert daemon** (PIDs 91824/94988/95132 → 0 remaining). Correct cleanup: post-cron-kill `opening-capture` is dead → the daemon discovers 0 candidates forever, so it's pointless; it also freed the pool. **DO NOT relaunch — this is an intentional stop, not a wedge.** **③ Sweep re-run with the pool freed → real progress but a second, distinct wall:** the config read now succeeds (**scope: 45 cities**, no 10-city fallback) and connections check out, BUT the per-city read over the **1.37 GB `opening_captures` table** hits a `statement timeout` (amsterdam, city 1). Killed the grind — it would produce a partial/unreportable grid. **④ Conclusion: the sweep's sole remaining blocker is the 1.37 GB `opening_captures` table** (per-city reads too slow to finish in the statement timeout). The **prune the operator deferred in the C93 audit is the real unblock** — and it's also the bulk of the Micro working set that the cron-kill alone does NOT shrink (the 1.37 GB is already on disk). Re-raised the prune decision with this new evidence. The sweep is **confirmatory** for the already-recorded 12th-signal KILL (mechanism established, `MAKER-EXIT-SIM.md`) — not decision-changing. Boundary intact: read-only sweep + local daemon stop; no capital, no keys, nothing live.
- **C93 (~08:05 local 07-07 / 06:05Z — COMPUTE AUDIT executed (operator directive 3): 29 → 19 cron jobs; killed the closed-gate + dead-signal captures that were saturating Micro; forecasting analytics + paper-trades untouched):** Full inventory (29 crons / 24 edge fns / table sizes). Root cause of the chronic Micro saturation = a few HIGH-FREQ HEAVY crons feeding now-CLOSED signals; `opening_captures` is the biggest table by 4× (**1.37 GB / 283k rows**), fed by the `opening-capture` pipeline that existed for the just-closed 12th-signal gate. Operator chose "crons only" (data intact). **Executed via `cron.unschedule`/`cron.alter_job`: UNSCHEDULED 10** — opening-capture + its 2 deadmen + convergence-panel + maker-exit-panel (12th signal CLOSED), arb-depth-capture (8th KILL), cross-venue-capture (10th KILL), reward-snapshot (REC-8 closed), sharps-snapshot + sharp-wallet-track (9th dead); **SLOWED whale-watch */1 → */10** (was 1,440×/day for a standalone alarm). Removes ~450 heavy invocations/day (incl. every opening-capture 45-city read = the saturation driver) + ~1,300 whale-watch/day. **KEPT** the full forecasting pipeline (discover/poll/fetch/build/snapshot×3/calibrate/grade/downsample/metar) + the paper-trades (amsterdam/city) + maintenance (health-monitor, 3 prune/retention) = **19 jobs**. Verified via `cron.job` re-read. Record: `docs/ops/COMPUTE-AUDIT-2026-07-07.md` (kill/keep table + re-enable pointers per source migration). **Still open (operator's call):** prune `opening_captures` (1.37 GB) to reclaim the working set; stop the now-inert local dry-run daemon (opening-capture dead → 0 candidates). The DB should ease within the hour → the real-book sweep becomes runnable (now confirmatory: the 12th-signal KILL + its synthetic-vs-real-book mechanism are already recorded). Boundary intact: operator-authorized prod cron change (reversible); no capital, no keys, nothing live.
- **C92 (~07:35 local 07-07 / 05:35Z — OPERATOR AWAKE → the KILL is RECORDED (Package 1 executed via override); twelve of twelve signals now dead, investigation CLOSED; sweep crashed again → folded into the compute audit):** Operator woke and directed: record the KILL, run the sweep, then audit Supabase compute. **① GATE — RECORDED.** The clean gate-row auto-write stayed infra-blocked ~2 days (Micro saturation: every panel tick degraded cErr>2 → the gate correctly refused to write; the durable `bot_gate_snapshot` newest row was still 2026-07-05 INSUFFICIENT), so with the verdict settled + robust across subsets the operator authorized recording it directly. **Executed GATE-DAY-PLAYBOOK Package 1 (KILL) in full** on the last real panel numbers (18:52-local read, cross-confirmed all night): mean net **−12.6%**, CI **[−21.6%, −3.5%]** (fully negative), **62 mkts / 26 cities / 7 days** (above the ≥40/≥6/≥7 floor), makerFillRate **0.065** vs backtest 49.0%, rebate **$0**, winFrac 0.27, net **−$168**. Wrote: FINDINGS.md (12th-signal closing block + summary-table KILL row + folded the "one live exception" para into an unconditional all-twelve-dead bottom line + "Where each finding lives" pointer, referencing the MAKER-EXIT-SIM.md synthetic-vs-real-book banner as the mechanism); CLAUDE.md pivot-block banner (newest entry, investigation CLOSED); SIGNAL-BACKLOG.md §13 (re-open criteria = a measured maker-fill-rate mechanism change, NOT a backtest re-tune). **Twelve of twelve tradable signals falsified; the rail stays DORMANT with no scoped exception pending; the analytics/insight product stands. No capital was ever risked.** **② SWEEP — crashed again, NOT runnable.** Two more attempts (05:37Z): the config read `ECHECKOUTTIMEOUT`'d (fell back to a 10-city default), then the Supabase pooler dropped the connection mid-fetch (`CONNECTION_CLOSED aws-1-eu-north-1.pooler.supabase.com:5432`) → uncaught, died at 25s. Same C91 wall + the pool now actively closing connections under load. Correctly reported NO grid (never a crashed/partial run). The sweep's un-runnability is fundamentally a DB-saturation problem → **folded into the compute audit (③)**: options are a genuinely quiet window, a temporary Micro upsize, or a chunked/per-city-retry read fix. **③ COMPUTE AUDIT — next**, per operator directive (kill unneeded cron/edge/table load, keep the essentials fresh; directly addresses the chronic Micro saturation that caused the whole 07-06→07 cluster). Daemon healthy throughout (dry-run, 0 failed). Boundary intact: docs-only recording + read-only sweep; no capital, no keys, nothing live, no deploy.
- **C91 (~05:55 local 07-07 / 03:55Z — the real-book sweep was ATTEMPTED in the first sustained-ease window and FAILED on heavy reads → key finding: daemon-discovery-ease ≠ panel/sweep-ease, so the clean gate tick is NOT imminent):** (Bridging note: C87–C90 were uncommitted verbal ride cycles across 23:05Z→03:17Z — DB saturated at US-evening peak then began flapping; gate stayed infra-blocked, daemon healthy-degraded throughout; nothing material to commit.) At 03:47Z the daemon showed **5 consecutive `discoveryDegraded:false` ticks** (03:40→03:47Z) — read as sustained ease → **ran the real-book sweep** `scripts/research/maker-exit-realbook-sweep.ts` at 03:50Z. **It immediately `statement timeout`'d on amsterdam AND ankara** (the first 2 of 45 cities). Root cause: the sweep's per-city read pulls the **full backtest price-history window**, ~10–20× heavier than the daemon's `p_days:1` discovery read — so the ease that lets the daemon discover does NOT cover the sweep. **Killed the sweep tree** (exact `maker-exit-realbook-sweep` CommandLine match; daemon PIDs 91824/94988/95132 untouched + verified alive, `failed:0`) to (a) avoid a partial/untrustworthy grid I must never report, and (b) stop it hammering the fragile DB and contending with the higher-priority panel clean-write. **KEY REALIZATION: the `maker-exit-panel`'s clean-write requires the SAME full-45-city heavy read → the panel also can't complete cleanly at this ease level.** This is *why* the panel has logged no `job_runs` row since 20:35 local (~9h): every fire's full-panel read times out / is wall-reaped before it can write. **The daemon-discovery ease was a FALSE leading indicator for panel/sweep readiness** (corrects the C89/C90 in-flight framing). **Consequence: a clean gate tick (→ the auto-record of the KILL) is NOT imminent** — it needs a *much* quieter DB (deep US-overnight ~06–09Z) or a chunked/per-city-retry read code fix (operator-gated), and may not land before the operator wakes. **The operator override ("record it now") remains the fast path.** Before any future sweep re-run: LIGHT single-city probe first (confirm one `convergence_capture_inputs` per-city read returns under the timeout), THEN launch the full 45-city sweep. Gate verdict unchanged: DEFINITIVELY KILL, settled, awaiting only the durable write. Daemon healthy (dry-run, 0 failed). Boundary intact: no capital, no keys, nothing live, no deploy; the sweep + its kill are read-only ops actions.
- **C86 (~02:00 local 07-07 / 00:00Z — v15 cycle 2: MCP light-reads RECOVERED → authoritatively confirmed the durable gate-of-record has NO 07-06 verdict; gate + daemon otherwise unchanged; ride):** `gate-read.ts 5` identical to C85 (no new `maker-exit-panel` job_run in ~55 min; last real KILL verdict 18:52 local; verdict unchanged DEFINITIVELY KILL). **MCP `execute_sql` came back for LIGHT reads** (`select now()` instant; was connection-timing-out at C85) — the daemon's HEAVY discovery reads are still `discoveryDegraded:true` at 23:54Z, so this is the MCP-proxy/light path recovering, NOT the DB easing for the heavy per-city joins (panel + sweep still blocked). Used the light path for the **authoritative gate-of-record check** (`bot_gate_snapshot`, the durable table the panel writes only on a clean cErr≤2 tick): **its newest row is 2026-07-05 19:39Z `INSUFFICIENT_DATA` (9 mkts/5 cities/6 days) — there is NO 07-06 row at all.** Definitive proof the KILL is unrecorded: every 07-06 tick that *computed* KILL (from the `maker_exit_panel` view / job_runs stats, e.g. the 18:52-local −12.6% panel) was degraded (cErr>2) and correctly refused the durable write. Note the settled KILL's extent (62 mkts / 26 cities / 7 days) is **above** the sufficiency floor (≥40 / ≥6 / ≥7) — so the FIRST clean tick writes a durable **KILL** row, not another INSUFFICIENT; Package 1 fires the moment that lands. **Daemon: healthy-degraded, ticking** (last tick 23:54:09Z / 01:54 local, ~9-min cadence, dry-run, **0 failed**, 22 positions from ledger) → no relaunch. **Sweep: still not run** (heavy read, would false-verdict on 0 rows). **CITY-LIVE:** 10:00Z tick not due until ~12:00 local. Note-and-ride: no record (no clean tick), no re-fire at peak, no threshold relax, no deploy. Boundary intact.
- **C85 (~01:05 local 07-07 / 23:05Z — v15 overnight cycle 1: gate STILL infra-blocked; the deep-night ease has NOT arrived because the load is US-evening peak, not European; daemon healthy-degraded; note-and-ride):** First cycle of the v15 overnight-autonomous loop. **Gate: STILL no clean tick — verdict unchanged DEFINITIVELY KILL.** `gate-read.ts 5`: last REAL verdict 18:52 local KILL (cErr 15, fill 0.0645, nMkts 62); 19:35/20:35 local both null-stats false-CLEAN; and **no `maker-exit-panel` job_run row AT ALL since 20:35 local (~4.5h)** — a deepening of the C80 signature: under sustained saturation every panel isolate is now wall-reaped (ADR-12) before it can write even a null-stats completion row, not just skipping the gate write. **The DB has NOT eased at 1 AM local** — MCP `execute_sql` still connection-timeouts, and the daemon's heavy discovery reads still `canceling statement due to statement timeout` at 23:01:00Z. **Reason (correcting the C83 "European deep-night" framing): the panel/discovery contention tracks US-evening peak — 23Z ≈ 7 PM ET — so the ease is unlikely before US-overnight (~06–10Z / 08–12 local), which may be AFTER the operator wakes.** Confirmed the DB is NOT down: **light reads via the direct script-db pool succeed in seconds** (gate-read returned instantly); it is specifically the heavy per-city `convergence_capture_inputs` joins (panel + daemon discovery + the real-book sweep) that self-contend and time out. **Real-book sweep: NOT run** — a heavy read, would return 0 rows → a false "no config clears" verdict; correctly deferred until gate-read shows cErr/duration easing. **Daemon: ALIVE + healthy-degraded, NOT wedged → no relaunch** — log fresh (LastWriteTime 01:01, last tick 23:01:00Z, PIDs 91824/94988/95132 = the C82 21:46Z relaunch), mode dry-run, **0 failed**, `degraded/discoveryDegraded:true`, 22 positions managed from ledger identity (zero capital impact). **Shadow-week: paused-by-degradation** (daemon discovers 0 candidates under the load → no new dry-run rows; 0 failed; resumes as the DB eases). **CITY-LIVE: verification still pending the 07-07 10:00Z tick** (not due until ~12:00 local). **Decision: note accrual + ride the next :35 cron** — per v15, did NOT record (no clean tick, operator asleep), did NOT re-fire at peak (more load is counterproductive; US-peak is now), did NOT relax `GATE_WRITE_MAX_CITY_ERRORS=2`, did NOT deploy. Boundary intact: no capital, no keys, nothing live; read-only + a docs cycle-tick only. Steady wakeup (~50 min) — nothing in flight; the ease is hours away.
- **C84 (~01:00 local 07-07 / 23:00Z — LOGIN magic-link failure DIAGNOSED (Supabase email RATE LIMIT, not an outage) + login UX hardened):** Operator reported `/login` "send link" doing nothing / showing empty red error brackets. **Diagnosis (definitive, via `get_logs auth`):** NOT a Supabase outage — the gateway is up + fast (auth/rest both 401 in <0.15s). The auth logs show `error_code:over_email_send_rate_limit` on `POST /otp` + 7× `429: "For security purposes, you can only request this after 15 seconds"` from repeat clicks + a `403: Email link is invalid or has expired` on `/verify`. Root cause = the operator hit Supabase's **15s per-email cooldown + hourly magic-link email cap** by re-clicking (because the UI gave no feedback). The login form (`apps/web/src/app/login/page.tsx`) rendered `{error}` raw → a blank/rate-limit error showed as empty brackets, and a hung send left `busy` stuck ("nothing happens"). **FIX (web hotfix):** bounded the send with a 12s timeout + always show a non-empty message (blank auth error → "service may be busy, try again") + surface the `?error=confirm` expired-link bounce. typecheck + suite 2845 green, committed + pushed → Vercel. **Operator resolution:** wait a few minutes for the limit to reset, click "send link" ONCE (don't spam), open the emailed link in the SAME browser. Optional hardening (Supabase dashboard): raise Auth→Rate Limits and/or configure a real SMTP provider (built-in SMTP is heavily throttled), and verify the redirect allow-list includes `weather-edge-two.vercel.app/auth/confirm`. Boundary intact (web push-to-main/Vercel path).

- **C83 (~00:45 local 07-07 / 22:45Z — WIND-DOWN on operator's word (getting ready for bed); Micro OFFICIALLY confirmed; DB at PEAK saturation at handoff):** ① **Operator confirms the Supabase account now officially reads MICRO** in the dashboard — the long-standing Nano/Micro DISPLAY MISMATCH (C73: "billing-vs-instance display mismatch"; the backend was Micro, the card showed Nano) is RESOLVED. Close that tracking thread. ② **DB is at PEAK saturation at bed-time:** `gate-read.ts` itself now fails `ECHECKOUTTIMEOUT` (can't check out a pool connection in 15s) — the strongest evidence yet that Micro is at/over capacity under peak load. This is the single root cause of tonight's whole cluster (prod 504 + gate can't clean-write + daemon wedge + sweep timeouts). ③ **State at handoff:** PROD 504 FIXED+verified (cd91d0a, middleware 2.5s auth-refresh timeout); daemon relaunched 21:46Z, alive/0-failed but degraded + cadence stretching (~2 min, borderline — the next session watches it); gate STILL no clean tick (18:52Z KILL is the last real verdict; 19:35–22:35Z all null/unrecorded); real-book re-tune sweep still deferred. Verdict unchanged DEFINITIVELY KILL. ④ **Wrote the v15 overnight-autonomous loop prompt** (below) for the operator's /clear+paste. **The overnight run's job:** as European deep-night load drops → first CLEAN gate tick auto-records the KILL (Package 1, pre-authorized) + the deferred real-book sweep runs → grid to the operator's morning. Boundary intact all session: no capital, no keys, nothing live; the only deploy was the web 504 hotfix (normal push-to-main/Vercel path). Suite 2845 green, typecheck clean, main == origin @ cd91d0a.

- **C82 (~00:00–00:40 local 07-07 / ~22:00–22:40Z — PROD 504 HOTFIX (middleware unbounded auth call) + daemon silent-wedge relaunch; all one root cause: the Micro DB/Supabase is saturated under peak load):** ① **Daemon:** the detached dry-run daemon SILENT-WEDGED ~21:37Z (log froze 7 min, 5 procs alive — the C76 frozen-on-hung-discovery signature under the peak-load DB) → **killed by explicit PID + relaunched detached 21:46:54Z** (pre-authorized; `trade-bot.start` dry-run/tickSec 60; first tick pending under the same DB load — zero capital, dry-run). ② **PROD INCIDENT — operator reported `504 MIDDLEWARE_INVOCATION_TIMEOUT` on `weather-edge-two.vercel.app`.** Root cause found in `apps/web/src/middleware.ts`: the session-refresh middleware ran `await supabase.auth.getUser()` on EVERY request with **no timeout** — a network call to the Supabase auth service. With the Micro instance saturated all evening, that call hung → the middleware exceeded Vercel's invocation limit → **504 on every page** (same unbounded-dependency class as the daemon/edge-fn hangs, but this one took the whole dashboard down). **FIX:** bounded the best-effort refresh with a **2.5 s timeout** (`Promise.race`; on timeout/unreachable, skip the refresh and let the request through on existing cookies — the page's own auth guard still runs, next request retries). typecheck clean, **suite 169 files / 2845 green**. Committed + pushed → Vercel auto-deploys. ③ **Gate/sweep still blocked** by the same saturation (17:35Z reaped; 18:35/19:35/20:35Z null-stats; 21:35Z unrecorded; the real-book re-tune sweep still times out) — riding to overnight low-load; verdict unchanged DEFINITIVELY KILL (18:52Z panel: -12.6%, CI [-21.6%,-3.5%]). ④ **THEME → operator item ④ flipped:** tonight's cluster (prod 504 + gate can't clean-write + daemon wedge + sweep timeouts) is ALL the Micro instance at/over capacity under peak load. **A compute DOWNSIZE (the item-④ plan) would make this WORSE — reconsider; if anything the peak-load headroom argues HOLD or a temporary upsize during high-traffic windows.** Boundary intact: web hotfix is the normal push-to-main/Vercel path (no Supabase migration/edge-fn/capital/keys).

- **C81 (~22:35 local 07-06 / 20:35Z — operator research directive: ESTABLISH why backtest (+6.7 %) ≠ live (−12.6 %) + how to adapt → ROOT CAUSE FOUND (synthetic vs real book); gate still infra-stuck, daemon honest-degraded):** Operator (engaged, mid-loop) asked to "truly establish the reason" for the maker-exit backtest-vs-live gap and how to boost it. **Traced end-to-end (code + live data):** the whole gap is the maker-fill rate (0.49→0.065); the replay engine is IDENTICAL (`replayMakerExitEvent`, credits a maker TP iff bid≥entry+0.12) so the gap is entirely in the price series each side feeds it. **The backtest replays a SYNTHETIC book** — `sim-maker-exit.ts`→`tune-convergence.ts buildSet(panel,'house_gaussian',1)` builds each bucket's bid/ask from the calibrated `house_gaussian` forecast ± a modeled spread ("synthetic (calibrated) book", the doc's own CAVEAT). Because the forecast is good, a book centered on it converges to truth BY CONSTRUCTION → 49 % maker fills. **The live gate replays the REAL Polymarket book** (`convergence_capture_inputs`, real `execBid`), which is EFFICIENT and does NOT converge to our forecast → 6.5 % fills; the missing 42.5 pts went to the taker **time-stop (41 %→87 %)** at −13.4 % (bid drifts flat/down). **NOT an artifact** — ruled out by the live data: censoring 4.6 % (3/65 open), density 104 ticks/event, 20-min thinning ≈2× (can't make a 7.5× gap), open marks 14pp below the sell limit. **The +6.7 % never existed on real books** — it was an upper bound conditional on the market pricing like our forecast; the live data proves it doesn't → the same efficiency wall as the other 11 signals, on real books. **Built two read-only tools:** `scripts/ops/maker-exit-gap-read.ts` (the decomposition diagnostic) + `scripts/research/maker-exit-realbook-sweep.ts` (the honest re-tune: sweeps tp/tstop/tpMode/sl over the REAL 45-city book to test if ANY config clears). **The sweep is DEFERRED to overnight** — at US-evening peak every per-city `convergence_capture_inputs` times out (the SAME DB degradation starving the gate; a first 4-way run returned 0 rows → correctly rejected the false "no config clears" verdict rather than report it). Recorded the finding as a banner atop `MAKER-EXIT-SIM.md` (it's the mechanism behind the KILL — folds into the FINDINGS Package-1 "why" when the gate clears). **Gate this cycle: STILL no clean tick** — 19:35Z + 20:35Z crons both null-stats (4 straight non-writing ticks); last real verdict 18:52Z KILL; riding to overnight per the operator's self-clear choice. **Daemon: honest-degraded, NOT wedged** — ticking (log 1 min fresh, 64 s cadence) but `degraded:true/discoveryDegraded:true/metaDegraded 22/22`, 0 failed, dry-run → zero capital impact (the C72-validated outage-ride behavior); no relaunch. Shadow-week paused-by-degradation (daemon discovers 0 candidates under the load → no new dry-run rows, but 0 failed). Boundary intact: no capital, no keys, nothing live, docs + read-only scripts only.

- **C80 (~20:30 local 07-06 / 18:30Z — v14 cycle 1, OPUS orchestration takes over: the co-tune did NOT clear the gate; the verdict is definitively KILL but the clean gate-row write is infra-blocked → ESCALATED, not recorded):** First Opus-orchestrated cycle. Read the C79 verification tick (req 827, 16:52Z :45 slot) + the surrounding trend: **NOT clean.** Tick 827 finished `ok` but DEGRADED — cErr **15**, budgetSkipped 12, 303 s, `gateWriteSkipped:'degraded'` → no gate row. Worse, **2 of the last 6 ticks HARD-REAPED**: 16:26Z + 17:35Z both `reaped by health-monitor: exceeded wall limit (ADR-12)` (the isolate ran ~25 min and was swept at the top of the hour — same signature as the 09:35Z reap in C76). The co-tune (`FETCH_CONCURRENCY 5→3` + `FETCH_BUDGET_MS 240→270`) did **NOT** converge: cErr oscillating 14→24→6→15 across 14:35–16:52Z, NOT trending to the ≤2 clean bar; reaps predate (16:26Z) AND postdate (17:35Z) the budget bump, so the root cause is post-outage per-call latency / hung connections on `convergence_capture_inputs`, not a tunable knob — C79's "~9 s margin under the wall" proved too optimistic (827 ran 303 s; 17:35Z hung to the reap). **The verdict itself is settled and unambiguous** (from `maker_exit_panel.view`, every tick, since `dash_maker_exit()` is operator_guard'd): label **KILL**, mean net **−10.7 % to −12.6 %**, 95 % CI **fully negative** (latest **[−21.6 %, −3.5 %]** — the WHOLE interval, not just ciLow≤0), n **62–78 mkts / 26–33 cities / 7 distinct days**, winFrac **0.27–0.28**, makerFillRate **0.045–0.086** (vs the 0.49 backtest floor), realizedRebate **$0**, net **−$168 to −$212**; robust across subsets (#63). **DECISION — escalate, do NOT record:** the prompt gates the KILL recording on a CLEAN tick OR an operator override, and explicitly forbids relaxing `GATE_WRITE_MAX_CITY_ERRORS=2` unilaterally; no clean tick is coming cheaply (infra, not verdict); operator is reachable → surfaced the decision with every Package-1 number pre-filled and ready. Did NOT manually re-fire (US-afternoon peak load; the last re-fire WAS the cErr-15 827 — more load is counterproductive); the zero-deploy path is to let overnight European low-load drop cErr ≤2 → clean write → auto-record per the standing pre-authorization. **Each-cycle watch:** ① Daemon HEALTHY — log fresh (12 s), ~64 s cadence, `degraded:false`/`failed:0`, 22 dry-run positions; `metaDegradedPositions:10` = the SAME post-outage capture-pipeline symptom in a 2nd consumer, zero capital impact (dry-run; positions still managed from ledger identity). ② Shadow-week HEALTHY — live_orders **326 rows** (304 canceled + 22 placed, all dry-run/maker-exit), **0 failed**, growing (was 214+); first `trade-shadow-diff.ts` read ~07-07. ③ CITY-LIVE — 0085 applied + city-paper-trade redeployed w/ the twin/board extension (C78 ④); `city_maker_twin` + `city_promotion_board` correctly EMPTY (today's only tick was 10:00Z, BEFORE the 15:27Z redeploy) → verification PENDING the **07-07 10:00Z** tick (first run of the redeployed fn). Boundary intact: no capital, no keys, nothing live, no deploys, docs-only.

- **C79 (~18:55 local 07-06 / 16:55Z — GATE UNBLOCKED at the code level: the two-part co-tune is DEPLOYED, gate verification in-flight; Fable→Opus orchestration handoff):** The maker-exit gate was infra-stuck (9 degraded KILL ticks, cErr 10-25, NOT self-healing over ~6h). ROOT CAUSE measured, not guessed: a single-city `convergence_capture_inputs` is ~6.5s UNCONTENDED (DB idle, 0 running jobs) but 18.4s mean / 39.6s max under the 5-way fetch pool (`pg_stat_statements`) — the 5 concurrent heavy reads SELF-CONTEND on Micro, ballooning past the 45s CITY_TIMEOUT. **Two-part fix, each operator-deploy-authorized and deployed:** (1) `FETCH_CONCURRENCY 5→3` (d81e93a, deployed 15:52Z) → first clean test (16:35Z cron) dropped **cErr 24→6, timeouts 14→0, nMarkets 44→78** — self-contention was the whole problem; the 6 residual errors were 100% BUDGET-SKIPS. (2) `FETCH_BUDGET_MS 240→270` (efe0436, deployed 16:52Z) → +30s recovers ~2 more 3-way waves to clear the last skips; capped at 270 for a ~9s margin under the 400s isolate wall (all-hang worst case 391s; common ~250-290s). CITY_TIMEOUT + the #20 tripwire untouched; tests 13/13, typecheck clean. Manually re-fired at 16:52Z (req 827) on the UNCLAIMED :45 15-min slot (cron only claims :30 via its :35 fire; finishes ~16:57Z, before the 17:00 health-monitor → reap-safe) to verify without waiting for 17:35Z. **VERIFICATION PENDING at handoff (tick 827 status 'running' at 16:55Z).** The verdict itself is settled: KILL, 9x-confirmed across independent city-subsets, makerFillRate 0.045-0.086 vs the 0.49 backtest floor. This cycle also built the MCP-outage ops toolkit (gate-read.ts/apply-migration.ts/sb.ts) used to apply 0085 + redeploy during the proxy outage. **Handoff: the loop continues under OPUS orchestration (not Fable) — see the v14 prompt + orchestration plan below.**

- **C78 (~17:30 local 07-06 / 15:30Z — operator authorized "apply 0085 and redeploy"; DONE + VERIFIED via a direct-DB/CLI path built around the concurrent MCP-proxy outage):** Operator green-lit the CITY-LIVE deploy mid-outage (Anthropic mcp-proxy.anthropic.com 502-ing since ~14:44Z, ~45min — Supabase itself fine). ① **Discovered the normal path was doubly blocked:** MCP `apply_migration` unreachable (proxy down), and `supabase db push` is the WRONG tool here — the prod ledger uses TIMESTAMP versions (every prior migration went in via MCP apply_migration, not the repo's sequential `0085` names), so `db push` refused ("remote versions not found locally"). ② **Built a hook-safe direct-DB toolkit** (reuses `loadEnv`/`makeScriptDb`, never surfaces DATABASE_URL/SUPABASE_ACCESS_TOKEN): `gate-read.ts` (committed d6fee37 — read-only job_runs gate reader, the outage's first fruit), `apply-migration.ts` (transactional applier: runs the file + records the schema_migrations ledger row in apply_migration's exact format — version/name/statements — inside one `sql.begin`, rolls back on any error, aborts if already applied), `sb.ts` (env-loading Supabase-CLI passthrough for the functions deploy). ③ **0085 APPLIED** at version `20260706152516`, transactional, idempotent (staged-DARK). Verified: `city_live_arms`/`dash_city_live()`/`trade_live_preflight(text)`/`live_orders.strategy` all present; **DARK** (city_live_arms 0 enabled, `trade_config.mode='off'`); `bot_order_reserve_intent` is now a SINGLE 12-arg signature (p_strategy last, no PGRST203 ambiguity — the daemon's 11-arg calls resolve via the default); the detached dry-run daemon rode the DROP+RECREATE clean (15:24Z tick degraded:false, 19 pos, 0 failed). ④ **`city-paper-trade` edge fn REDEPLOYED** (`sb.ts functions deploy … --use-api`, server-side bundle incl. the maker-twin + promotion-board handler extension → "Deployed Functions"; a bad bundle would have failed there, so crash-on-load is ruled out). Did NOT force-trigger it — twin/board populate naturally at the next 10:00Z tick (an off-cadence manual invoke would inject stray paper bets into the measurement). ⑤ **Net:** operator items ①→ DONE; the /trading winners board + input-table writes + maker twin are LIVE-capable (still need an operator city-toggle + TRADE_MODE=live + preflight PASS before any capital — the quadruple gate is intact and untouched). Boundary held: no capital, no keys, mode stays off. Gate unchanged — still infra-stuck (8 degraded KILL ticks; §63 co-tune staged for ~17Z if the plateau holds).

- **C77 (~14:05 local 07-06 / 12:05Z — CITY-LIVE BUILT + REVIEWED + MERGED + PUSHED in one arc (operator directive → prod-ready staged-dark in ~2.5h); gate still blocked on degraded ticks (3× KILL-computed, no clean row)):** ① **Operator directive (mid-loop): continuously evaluate the multi-city paper-trades and FRONT the winners for real testing** (Karachi leading) + make /trading an INPUT table. AskUserQuestion locked: manual per-city Live toggle · $5/day/city, max 2 cities · faithful TAKER replication live + a longitudinal MAKER-ENTRY PAPER TWIN. ② **Spec-first**: `docs/specs/CITY-LIVE.md` (21572ee) locked the four-lane interface contract, incl. the strategy-aware `trade_live_preflight(text)` design catch (the no-arg interlock is hard-wired to the maker-exit gate — a KILL there would have permanently blocked the city lane). ③ **Four parallel Opus worktree lanes** (recon agent mapped 5 surfaces first): P core promotion engine (`city-promotion.ts` — LB-ranked via entry-watch's edgeCiLo, floors nBets≥20/nDays≥10/sufficient, PROMOTED/WATCH/INSUFFICIENT/DEMOTED w/ hysteresis; 10 tests) · D `0085_city_live.sql` staged DARK (city_live_arms $5-CHECK + max-2 trigger, audit, `city_maker_twin`, promotion board, `live_orders.strategy`, 12-arg `bot_order_reserve_intent` w/ dropped 11-arg sig, strategy-aware preflight; + city-paper-trade handler twin/board extension, deploy-order-safe; 16 tests) · X daemon city-taker lane (`city-live-decide.ts` pure decide + trade-bot tick integration, inert pre-0085, dry-run ledger idiom, tz in-hour gate; 41 tests) · W /trading INPUT TABLE (editable trade_config + winners board + per-city Live toggles w/ confirm dialog + 2-enabled lockout; requireOperator→operator_guard'd RPC writes, verbatim DB RAISE surfacing; 20 tests). Cross-lane contract negotiated mid-flight (p_strategy threading; tokenId bridge via `cityBucketIdentity`). ④ **Integrated linearly** (cherry-pick P→D→X→W + 1 type-alias reconcile), full suite **169 files / 2845 green**, typecheck clean. ⑤ **Adversarial fresh-eyes review (Opus): MERGE-CLEAN — 0 CRITICAL/HIGH, 1 MEDIUM + 3 LOW**; ALL addressed pre-push in 0085 (`dbaf8e1`): twin differential on ONE population (join taker P&L to twin keys — the headline number was mismatched-population), twin grades from the paper bet's own graded outcome (one settlement basis by construction), twin limit from the sim's exact ask snapshot row, `pg_advisory_xact_lock` on `city_live_arm_set` (max-2 MVCC race, already failed closed). Affected suites re-run green (170). **PUSHED `ec8da5a..dbaf8e1` (7 commits).** Nothing arms itself: live city entries need 0085 applied + operator toggle + TRADE_MODE=live + preflight('city-taker') PASS — quadruple-gated. ⑥ **Gate (still the headline): 3 degraded ticks post-recovery** (10:02Z cErr 11 / 10:35Z cErr 25 / 11:35Z cErr 12), ALL computed KILL (makerFillRate 0.06–0.075), all correctly refused the gate write; cause = venue fetch latency vs the 45s city timeout (~265–272s ticks, 24k captureRows); riding hourly :35 slots, stage a budget fix if none clean by ~18Z (#63). ⑦ Housekeeping: 4 lane worktrees removed; 10 pre-session worktrees remain (integration state unverified — operator/later). OPERATOR DEPLOY (in order): apply 0085 → redeploy city-paper-trade → Vercel already auto-deploying (page degrades gracefully). Boundary intact throughout: no capital, no keys, nothing live, no migration applied.

- **C76 (~12:15 local 07-06 / 10:12Z — v13 cycle 1: OUTAGE RECOVERED after ~14 h → banked jobid-28 re-fire USED → first full-panel replay computes KILL but is DEGRADED (cErr 11) → gate write correctly SKIPPED (0084 doing its job), adjudication rides to the hourly :35 ticks; #50 CLOSED; daemon silent-wedge found + restarted; operator smoke = v2 AUTH PASS, token discovery starved by the outage):** ① **Recovery ~09:47Z, zero action needed (as predicted):** net._http_response 202s (ids 147–149); convergence-panel 09:47Z ok (42.6 s, 41 mkts, 0 cErr); the full 10:00Z wave ok (whale-watch/poll-markets/opening-capture 78/78/reward/arb/cross-venue/metar/health). The 09:35Z maker-exit tick died at the wall and was formally reaped by health-monitor at 10:00:06Z ("exceeded wall limit (ADR-12)"). ② **The banked jobid-28 manual re-fire USED at 10:02:57Z** (conditions met: path proven back, no completed panel tick since 19:39:31Z 07-05): ran 263.7 s over the 14 h backlog (captureRows 24 493, freshEvents 249, budgetSkipped 3) → computed label **KILL** (nMarkets 64, entries 66, makerFillRate **0.0625**, realizedRebateUsd 0, qualifyingTickFrac 0) **BUT cityErrors 11 > the ≤2 clean bar → `gateWriteSkipped:'degraded'` → NO gate row written. NOT adjudicable** — a panel missing 11 cities can't render the verdict of record; the gate still awaits the first CLEAN full-panel tick (hourly :35 slots; expect lighter/faster now the backlog is replayed). The 0.0625 fill-rate read — below even the 0.09–0.22 observed band — pre-figures the playbook §3 mechanism note if KILL confirms. ③ **#50 CLOSED:** the 10:00Z city-paper-trade tick ran cities:4 / placed:5 (ankara 3, karachi 2) — the 0081 thread is done. ④ **Daemon silent-wedge (a NEW failure signature) found + fixed:** last log write 07:51:17Z — discovery died on "canceling statement due to user request" then the process FROZE (no fatal line, no ticks, 5 procs alive → the fatal/ECHECKOUTTIMEOUT watch misses it; watch LastWriteTime staleness instead). Killed the tree + relaunched dry-run 10:04:09Z; first tick 10:04:25Z fully healthy: 17 positions, 7 dry-run intents (reprice_entry re-peg targets = capture meta restored), 0 failed. Ops caution recorded: the kill filter must be scoped to cmd/node — a broad CommandLine~'trade-bot' match kills the orchestrator's own shells. ⑤ **Operator live-smoke (his morning run) answered:** steps 1–2 **PASS on the v2 client** (L2 creds derived · sigType 2 · funder recognized · 0 open orders — the AUTH path of the C75 migration is proven); steps 3–4 no-op'd because `discoverToken` reads `convergence_capture_inputs` p_days:1, which was empty during the outage. Re-run works now (captures flowing since 09:50Z); a deterministic fallback `--token` (amsterdam 2026-07-07 19°C bucket) handed over. Step 4 POST-then-cancel OK = the write-path PROOF closing operator item ①. ⑥ Board verified main==origin @ fb6d92c (C75 tip); docs-only this cycle; boundary intact (no capital, no keys, nothing live — the re-fire and daemon restart are both pre-authorized ops actions).

- **C75 (~02:00 local 07-06 — clob-client-v2 MIGRATION BUILT (the C74-diagnosed V1→V2 write-path fix) → live rail un-blocked, DARK behind the unchanged gate; suite green; STOP before commit):** The CLOB V2 cutover 400 that C74 diagnosed is now FIXED in code (uncommitted, fresh-eyes review follows). ① **Dependency:** `@polymarket/clob-client@^4.22.8` → **`@polymarket/clob-client-v2@^1.0.8`** (installed 1.0.8; deps viem + @ethersproject/wallet) in `packages/trading/package.json` + pnpm-lock; the Deno + Node import specifiers swapped in ALL THREE sites (`live.ts` clobSpec, `execute-bet/index.ts` eszip hint, `_shared/npm-specifiers.d.ts`) — the invariants eszip-lockstep + §15 boundary tests stay green. ② **`bootstrapClobClient` (live.ts):** rewritten to v2's OPTIONS-OBJECT constructor `new ClobClient({host, chain:137, signer, creds, signatureType, funderAddress})` (was positional). Ethers@5 `Wallet` KEPT — v2's `ClobSigner` union feature-detects `_signTypedData` at sign time (verified in the installed `signing/signer.js`), NO viem rewrite; `SignatureTypeV2.POLY_GNOSIS_SAFE===2` unchanged. ③ **The actual 400 fix (source-verified against the installed dist, not assumed):** v2 `createOrder` resolves the order version from the live CLOB `/version` endpoint (post-cutover = 2; hardcoded default 2) and builds a **V2-signed order** (EIP-712 Exchange domain version "2"); `postOrder` self-heals a version-mismatch via `resolveVersion(true)`. We pass no explicit version → V2 by default. That dynamic resolution IS the fix. ④ **postOrder arg SWAP handled:** v2 sig is `(order, orderType, postOnly?, deferExec?)` — 3rd/4th swapped from v4. ALL our calls stay 2-arg (we pass NEITHER); every inverted comment/invariant in live.ts/types.ts/order-intent.ts rewritten to v2 truth (post_only is a gated future lever — adopting it would change fill semantics vs the running shadow week; maker-ness stays price-enforced). ⑤ **Trade parse:** v2 `Trade`+`MakerOrder` field names (trader_side, maker_orders[], matched_amount, maker_address, order_id, asset_id, side, price) are field-verified IDENTICAL to what `parseTrades`/`mapMakerLeg` read — the C64 taker-centric CRITICAL fix is preserved 1:1, no adaptation. ⑥ **Credential-leak defense re-confirmed:** grepped the WHOLE v2 dist for `console.*` → ZERO hits (v2's http-helper returns `{error,status}` silently), so the v4 axios-header console.error leak class is GONE by the upgrade itself; the C74 `withRedactedConsole`/`redactConsoleClient` wrapper is KEPT active as defense-in-depth (a future dep/patch re-opens the class; behavior-neutral, log-only). ⑦ **Verify:** typecheck clean; suite **164 files / 2757 tests green** (baseline unchanged — v2 wire shapes are mock-identical to v4, no test logic forced; test + trade-smoke comments updated to v2 truth). ⑧ **Residual risks (verified, none blocking):** numeric tickSize into v2's string-typed `TickSize` coerces safely (parseFloat + string-keyed ROUNDING_CONFIG — identical to v4, no throw); v2 getTrades auto-paginates and returns the complete `Trade[]`, so the `tradesResponseTruncated` ≥100 backstop is now moot-but-harmless. **Fresh-eyes review VERDICT (clob-v2-review, ~00:10Z): MERGE-CLEAN — all 10 items PASS (incl. the two builder-uncovered angles: NEG-RISK exchange selection flows correctly from market metadata `parseGammaEvent.negRiskMarketId` → v2 `options.negRisk` → negRiskExchangeV2; and 6-decimal amount scaling verified UNCHANGED), 0 CRITICAL/HIGH, 5 LOW safe-direction notes → a v13 micro-lane:** LOW-A `String(tick)` at the 3 numeric-tickSize call sites (works today via JS coercion) · LOW-B v2 getTrades auto-paginates so the ≥100 truncation backstop can false-positive (HOLDs, never frees wrongly — switch to cursor-absence) · LOW-C rare HTTP-4xx business rejections classify state-unknown → held until the startup sweep (safe direction, not a regression) · LOW-D guard `funder || undefined` against a set-but-empty POLY_FUNDER_ADDRESS · LOW-E the "self-heals version-mismatch" comment overstates (the CACHE refreshes; the in-flight order is not retried). COMMITTED + PUSHED post-review by the team lead; **daemon RESTARTED onto v2 by the orchestrator (~00:15Z). Operator: re-run `--live-smoke` on the v2 client (the write-path PROOF — step 4 now expected to POST-then-cancel OK); edge fns keep the V1 eszip hint until the next operator redeploy (harmless — no edge fn posts orders, keyless reads are V1-tolerant).** Boundary intact: no capital, no keys, nothing live, no venue calls (verification = installed package source + unit tests only).
- **C74 (~01:35 local 07-06 — LIVE-SMOKE step-4 venue 400 DIAGNOSED (CLOB V2 cutover, NOT a version bump) → clob-client-v2 migration STOPPED at the redesign gate; the step-4 CREDENTIAL-LEAK hardened + committed):** Operator ran the `--live-smoke` write-path probe (his terminal, TRADE_MODE=live). ① **Smoke result:** steps 1–3 PASS (L2 creds derived · funder recognized · dry-run payload built); **step 4 REJECTED — `400 {"error":"invalid order version, please use the latest clob-client"}`, nothing rested (`getOpenOrders` 0).** ② **Root cause (source-verified, not guessed):** the Polymarket **CLOB V2 exchange cutover (~2026-04-28)** — EIP-712 order domain version bumped 1→2 with new exchange contracts (`exchangeV2 0xE111…996B` / `exchangeV3 0x9fE6…0C3D`). The ENTIRE `@polymarket/clob-client` line is V1 and superseded (py-clob-client archived 05-25; no Node release since 5.8.1, 03-23). I packed 5.8.1 and confirmed it STILL signs `PROTOCOL_VERSION "1"` against the same exchange `0xdFE0…9E40` → **bumping 4.22.8→5.8.1 would emit a V1 order and get the SAME 400.** The real fix is the separate package **`@polymarket/clob-client-v2`** (1.0.8, signs `CTF_EXCHANGE_V2_DOMAIN_VERSION "2"`; ethers signer still works, `SignatureTypeV2.POLY_GNOSIS_SAFE===2` unchanged, `Trade` maker_orders/trader_side compatible — BUT options-object constructor + versioned V1/V2/V3 orders + a postOrder `postOnly`/`deferExec` arg-position SWAP → a signing-seam **redesign**). Per the bounded-lane "redesign ⇒ STOP, commit nothing, report" rule, the migration was **NOT built** — reported to team-lead as its own lane. ③ **Scope add — the step-4 credential leak (root cause):** on the 400, `@polymarket/clob-client`'s http-helper `errorHandling` does `console.error("[CLOB Client] request error", JSON.stringify({… config: err.response.config}))` — `config.headers` carries the L2 auth trio **POLY_API_KEY / POLY_PASSPHRASE / POLY_SIGNATURE** — which fires INSIDE the client (bypassing every one of the smoke's `redactText` catch paths) and printed the creds to the operator's terminal. ④ **Fix (committed):** wrap the live client at the `bootstrapClobClient` seam in a depth-counted, concurrency-safe REDACTING console guard (`withRedactedConsole` / `redactConsoleClient`, packages/trading/live.ts) — one chokepoint covering the daemon, `execute-bet`, AND the smoke; and `redactText` now masks the L2 cred SHAPES (uuid apiKey/passphrase + base64 secret/signature) too, scoped so addresses/order-ids/tokenIds/prices survive. +5 tests (incl. "a synthetic clob-client error dump comes out fully redacted" + proxy `this`/return-value preservation). ⑤ **Suite 164 files / 2757 green** (was 2752; +5), typecheck clean; committed + pushed on main. ⑥ **Operator:** if that terminal session was captured anywhere, ROTATE the L2 CLOB creds (regenerable via `createOrDeriveApiKey`, NOT the wallet key); a `--live-smoke` re-run is now safe but STILL 400s until the clob-client-v2 lane ships. Boundary intact: no capital, no keys, nothing live.
- **C73 (~01:00 local 07-06 / 22:57Z 07-05 — post-wind-down operator turn: compute verdict corrected + live-smoke permission → boundary HELD, walkthrough delivered):** ① Operator reports "stuck on Nano, will retry tomorrow; support ticket FILED" (item ⑤ done). Server-side check CONTRADICTS the dashboard: postmaster restarted 19:07:37Z (one of his pushes DID cycle the DB) and live settings read MICRO-class (shared_buffers 224MB / max_conn 60 — a 0.5GB Nano would show ~¼ that) → told him to STOP re-clicking; the dashboard card is the same billing-vs-instance display mismatch as the morning. ② Outage STILL on at 22:56Z (~3h; ids 166–168 timed_out) — restarts did NOT clear the DNS wedge; gate still pending. ③ **Live-smoke: operator granted permission → DECLINED under the non-negotiable boundary** (needs TRADE_MODE=live + the signing key; Claude never touches either, permission-in-chat notwithstanding) → per his fallback ask, the exact PowerShell walkthrough ADDED to TRADING-ACTIVATION.md §5 (incl. --i-know-no-preflight requirement pre-gate-PASS, WARN expected, gasless, outage does NOT block it — venue reached directly from his machine). ④ Operator directive at 96% Fable: **Opus for everything not super complex** (subagent policy already defaults Opus; Fable = orchestration only). v13 inherits all of this unchanged.
- **C72 (~00:45 local 07-06 / 22:45Z 07-05 — v12 WIND-DOWN on operator's word at 95% Fable tokens; outage OUTLIVES the session):** ① Final read 22:43Z: pg_net STILL DOWN ~2h50m (ids 162–164 timed_out 22:40Z); job_runs 21:45→22:43Z EMPTY → the 07-05 22Z snapshot-ensembles wave LOST to the outage (10Z gap unhealed — platform weather, nothing to fix our side; crons self-retry); NO gate tick ran in v12 — the day-7 verdict still awaits its first healthy full-panel read; the jobid-28 re-fire was NEVER used (stays banked for v13). ② Daemon rode the entire outage clean: 22:42:51Z tick, 17 positions ledger-managed, 0 failed, honest degraded — 83e41e8 validated under a real multi-hour outage. ③ v12 totals (C70–C72, ~2 h): outage watched end-to-end (signature evolution recorded: HTTP-hang → full-DNS-hang), board verified, docs-only commits, zero interventions needed — correct restraint (no re-fire on a down path, no resize, no daemon restart). ④ **v13 GATE-UNLOCK + STEADY-WATCH prompt WRITTEN** (above; amended: outage state at close, 22Z-wave loss, #50 outage-fallback rule, docs-only-commit exemption made explicit, 6h-outage support-ticket operator item). ⑤ /remember at close. One armed v12 wakeup (~22:49Z) may still fire → it ends the loop without rescheduling. NOTHING RUNS from this session after close except the detached daemon (intentional, survives exit). Boundary intact all session: no capital, no keys, nothing live.
- **C71 (~23:44 local / 21:44Z 07-05 — v12 cycle 2: outage ~2 h, signature WORSENED to full-DNS hang):** ① net._http_response ids 132–134 all timed_out — 21:40Z rows show DNS ALONE eating the whole 4 500 ms budget (vs ~100 ms DNS + HTTP-hang earlier); id 134 total 6 134 ms. Last edge fn run remains 19:47:09Z — the 20:35Z AND 21:35Z maker-exit slots never invoked. Re-fire stays banked (path not back). ② Daemon steady: 21:45Z tick, 17 positions ledger-managed, 0 failed, honest degraded. ③ Next wake 3 600 s → ~22:45Z: reads the 22:35Z maker-exit slot (the gate tick if the path recovers) + the 22:15/22:35 snapshot-ensembles; the 22:50 leg of the 22Z pipeline check completes the cycle after (≥23:00Z). C70+C71 committed+pushed this tick (docs-only).
- **C70 (~22:55 local / 20:55Z 07-05 — v12 cycle 1: OUTAGE ONGOING; daemon degrading GRACEFULLY):** ① pg_net path still DOWN — net._http_response ids 99–101 all timed_out at the 4 500 ms cap through 20:49:07Z (DNS resolves ~100–140 ms; the HTTP leg eats the budget); job_runs confirms NO edge fn invoked since 19:47:09Z (last: convergence-panel ok 46.8 s). No jobid-28 re-fire (path not proven back — the one allowed fire stays banked). ② Board verified: main == origin @ 553bd3e (C69-final), tree clean. ③ **Daemon UP on fixed code and doing exactly what 83e41e8 built**: 20:54/20:57Z ticks show discovery_failed (statement timeout on convergence_capture_inputs) → honest degraded ticks, 17 positions RETAINED and ledger-managed, 0 failed, dry-run. NEW wrinkle: the daemon's DB reads statement-timing-out ⇒ the instance is also sluggish server-side (my unscoped job_runs MCP query connection-timed-out too; a 4h-scoped one ran fine) — watch, don't touch. ④ 22Z pipeline check due ≥23:00Z; #50 due 07-06 ≥10:05Z. Next wake ~30 min: recovery check (200/202 rows in net._http_response) → first healthy tick adjudicates per GATE-DAY-PLAYBOOK. (Commit bundled with the next cycle.)
- **C69 (~22:50 local / 20:50Z 07-05 — v11 FINAL WRAP at operator's /clear call (context 66%)):** 20:35Z tick NEVER INVOKED — pg_net outage ongoing at close (latest maker-exit run still 19:35Z; one MCP query also connection-timed-out ~20:40Z). Gate rides v12 with the fixed handler DEPLOYED — first healthy tick adjudicates. v12 prompt block amended to the C69 state (bundle EXECUTED ✓ · pg_net outage at close · gate pending · stop-resize note). NO further wakeups; session ends on the operator's /clear → paste v12. — OPERATOR SAID "deploy the bundle" → §12 EXECUTED: 0084 APPLIED + BOTH PANELS DEPLOYED; verification tick blocked by a FRESH pg_net outage (platform weather, 3rd wave)):** ① **Migration 0084 APPLIED to prod via MCP** (trade_open_exposure + preflight/dash_trading restated + record_fill w/ p_fee_usd + FOR UPDATE + bot_order_record_resolution_loss + dash_maker_exit_history +cityErrors). ② **maker-exit-panel + convergence-panel DEPLOYED** via authed CLI (--use-api) ~19:55Z — the 45 s city timeout + degraded-tick gate-write skip are live-on-deploy. ③ **Verification BLOCKED: every pg_net request since 19:55Z times out at its 4 500 ms cap on DNS/TLS** (net._http_response ids 64–71 — DNS alone eating 4.5 s; affects ALL edge invocations incl. un-redeployed capture/whale-watch, so NOT the deploy; the response-id reset also confirms the 18:01 restart) — the DB's outbound path to the functions gateway degraded again at ~19:55Z. Two manual jobid-28 fires (19:56/20:01, operator-authorized under the bundle-go) queued but timed out. Crons self-retry each interval; the 20:35Z tick reads on recovery. ④ Resize NOTE for the record: operator reports repeated "compute resize failed" pushes — advised to STOP: Micro has been ACTIVE since 18:01:20Z (each further success would restart the DB again). ⑤ --live-smoke DECLINED as mine (needs TRADE_MODE=live + the signing key — the non-negotiable boundary); exact PowerShell handed to the operator. Gate day-7 state unchanged (7 days / n_open 0 / awaiting one clean full-panel tick — now with the fixed handler deployed, the first healthy invocation should adjudicate). — SESSION WIND-DOWN; v12 GATE-UNLOCK prompt written; the gate is ONE operator word from adjudicating):** 19:35Z tick: 273.5 s, 9/73, cErr 39 — cache-warming does NOT clear the deployed handler's 30 s timeout; the hourly ticks will not render the full panel before the §12 redeploy. Day-7 state unchanged and confirmed (7 days / n_open 0 on every post-resize row). Session totals (v11, C60–C67): the DB-crash incident managed to full recovery (resize verified NANO→Micro at 18:01:20Z; retention job live); the operator-ordered 7-lane Fable review of the 07-03→05 build → 27 CONFIRMED findings (2 CRITICAL, both live-only and invisible to the shadow week) → ALL FIXED via 4 worktree lanes, serially integrated, suite 161/2609 → 164/2752, five fix commits pushed; 0084 staged dark + TRADING-ACTIVATION §12 bundle documented; durable detached daemon (survives session exit) up on the fixed code; #50 gap-fill done+verified (21 bets/4 cities); shadow-diff first read CLEAN. HANDED TO v12: the §12-gated gate read (the headline), tonight's 22Z pipeline check, tomorrow's 10:00Z #50 cron-slot proof, daemon watch, shadow-week cadence. Boundary intact all day: no capital, no keys, nothing live; the one manual panel re-fire authorized was used once (18:46Z). /remember written at close. — THE RESIZE LANDED (postmaster 18:01:20Z) → CPU REAL → daemon UP on fixed code + #50 GAP-FILL DONE + SHADOW-DIFF FIRST READ CLEAN; day-7 CONFIRMED in the panel, full read still pending on the un-redeployed handler):** ① **Micro resize APPLIED at 18:01:20Z** (the 16:39Z click took ~82 min — the pooler churn explains the 18:06–18:13 daemon fatals). Heavy probe + 3M-row CPU burn instant — CPU REAL; normal probe rules resume. ② **Gate: n_distinct_days = 7 CONFIRMED on every post-restart row, n_open 0 (ALL positions settled)** — the day-7 answer exists in the data. But the full-panel read is still blocked by the DEPLOYED handler's #20 bug (30 s client vs 40 s statement timeout, cold cache post-restart): 18:35Z tick 37/73 cErr 23; the ONE authorized manual re-fire (18:46Z, 276 s) came back 11/73 cErr 40 (ran into the daemon-restart + :45 stack). Partial views: net −$70.78 (37-mkt) / −$35.26 (11-mkt) — NOT adjudicable. **Fastest unlock = the §12 redeploy (45 s timeout + degraded-tick gate-write skip) — OPERATOR ASKED; else the hourly :35 ticks ride the warming cache.** ③ **Daemon UP on the fixed code** (18:48Z first tick: 18 positions ledger-keyed, 18 dry-run intents, 0 failed, degraded:false, metaDegradedPositions:0, advisory lock held). ④ **#50 gap-fill EXECUTED + VERIFIED: 21 bets / 4 cities target 2026-07-05** (ankara 6 · karachi 6 · singapore 6 · houston 3 — °F arms where snapshots existed; today's poll-markets gaps limited houston) written 18:53Z; cron-slot proof = tomorrow 10:00Z tick ≥10:05Z. ⑤ **Shadow-diff FIRST READ (T5): entry-dims-only, as predicted** — median entry offset B−A +1.62¢ = EXPECTED(maker-shade); 14 A-only UNMAPPED rows = daemon entries during today's capture-outage windows (incident artifact, not logic); 1 paris inverse (replay entered / daemon skipped); realized A $0 (dry-run) vs B −$24.87 (4 time-stops + 1 SL); NO management-logic divergence class fired. ⑥ Next: 19:35Z tick = the gate read (or the §12 redeploy on operator word); then wind-down (v12 prompt + /remember). 22Z pipeline check hands to v12 if wind-down lands first. — ALL FOUR FIX LANES INTEGRATED + PUSHED; suite 164 files / 2752 green; daemon restart BLOCKED by pooler weather):** Serial integration complete — ① `39fc2e4` replay same-tick precedence (time-stop first; +4 tests; pinned +6.7% banner predates the conservative fix, regen shifts down); ② `a867439` venue-truth (the CRITICAL taker-centric parse: trader_side/maker_orders + address/order-id attribution, unattributable ⇒ degraded sell-hold; postOrder 2-arg — the 3rd positional is deferExec NOT postOnly in clob-client@4; reprice-raced partial fills stay ledger-visible; TP-race re-derives FAK size; dust-floor parking; +38 tests); ③ `fdcdf1e` gate-honesty+SQL (**migration `0084_trading_hardening.sql` STAGED DARK**: trade_open_exposure counts FILLED holds, record_fill row-lock + p_fee_usd, bot_order_record_resolution_loss, dash_maker_exit_history +cityErrors; handlers: degraded ticks SKIP the bot_gate_snapshot write (cErr>2 ∨ nMkts<40) + 45s client timeout both panels; web: trend excludes partial views, /trading RPC-error vs not-applied honesty; +47 tests); ④ `83e41e8` daemon-robustness merged via the agent's own main-merge round (ledger-keyed position identity — 3-lane finding — + metaDegraded retention, discovery-failure honest degraded tick, three-valued preflight (read-fail HOLDS, never cancels), mode-scoped advisory lock single-instance, TRADE_TICK_SEC clamp, trading-db 19-test contract suite) + the two integrator seams wired by hand (exitTaker feeRateBps from cfg.takerFeeRate; the tick books resolution losses when the capture meta's winnerIdx-token ≠ a held position's token — EventMeta.winnerTokenId added) + shadow-diff comment alignment + **TRADING-ACTIVATION.md §12 (the operator apply bundle: 0084 AFTER 0082 → redeploy maker-exit-panel + convergence-panel → daemon restart)**. All 27 confirmed findings CLOSED (26 in code, #25 as the new test suite). ⑤ **Daemon: restart on fixed code FATALED twice (18:09/18:13Z) — Supavisor session-pool checkout timeouts (ECHECKOUTTIMEOUT 15s)**; server-side pg_stat_activity shows only ~21 connections, none ours ⇒ pooler-layer platform weather, not exhaustion; the new lock-probe-fatal posture exits loudly (correct). Daemon DOWN, riskless (dry-run rests nothing at the venue); retry each cycle. ⑥ Resize STILL stalled (postmaster 07:36:03Z); 17:35Z tick 0/73 cErr 45. Next: 18:35Z tick + daemon retry + worktree cleanup done this tick. — REVIEW VERDICT IN: 29 findings → 27 CONFIRMED (2 CRITICAL / 8 HIGH) / 2 refuted; 4 fix lanes dispatched):** Workflow `wf_4109a59e-58d` done (47 agents, ~29 min; full verdicts in the task output file). **CRITICAL (dedup 1): the CLOB trade-side inversion** — `parseTrades` treats venue trade `side` as OUR side but the venue Trade record is TAKER-centric (`trader_side`/`maker_orders[]` dropped; verified against installed clob-client@4.22.8 types): our maker BUY entry fill arrives side=SELL → `venueSoldFor` marks the position FLATTENED (no TP/SL/time-stop ever rests, zero alerts, rides naked to resolution) and reconcile's `tradeHit` side-match frees a crash-dangling FILLED order as never-posted → double-place. **Structurally invisible to the shadow week (getTrades is live-mode-only) — would have detonated on the FIRST live fill.** The single most valuable catch since the T2 over-sell. **HIGH (dedup): ②** clob-client@4 postOrder 3rd positional is `deferExec` not `postOnly` → live "maker" posts could CROSS THE SPREAD and take; **③** raced partial entry fill during reprice orphaned by recordCanceled (invisible shares); **④** position identity keyed off CURRENT argmax bucket → forecast drift/unseeded capture ORPHANS open positions from all management (found independently by 3 lanes); **⑤** 0082 `openExposureUsd` excludes FILLED holds → total capital cap never binds; **⑥** degraded panel ticks write the §9R-E gate-of-record unmarked (today's snap 221/222 partial-view confusion, now a confirmed defect class — floor fix: skip gate write when cErr>2 ∨ nMkts<40). MEDIUMs: TP-cancel race over-sell sizing · dust-remainder livelock · capture-RPC failure silently suspends ALL management (3 lanes) · preflight READ failure conflated w/ kill verdict (DB blip cancels venue orders) · no single-instance guard (advisory lock) · fee_usd dead column (kill blind to FAK fees) · hold-to-resolution losses never ledgered (kill blind to full-stake losses) · record_fill no row lock · panel 30s client vs 40s server timeout · trend includes partial views · getTrading false "0082 NOT APPLIED" diagnosis · replay same-tick TP-before-time-stop optimism (verdict-bearing: pinned +6.7% predates the conservative fix) · trading-db zero coverage · TRADE_TICK_SEC hot-spin. REFUTED (2): RPC-shape cityErrors under-report; cache 20:00-trim invariant. **4 Fable fix lanes dispatched in worktrees** (venue-truth / daemon-robustness / gate-honesty+0084-staged-dark / replay-precedence); integration serial w/ suite+typecheck; migration 0084 + edge redeploys stay OPERATOR-GATED. Live rail remains DARK; dry-run shadow week UNAFFECTED by the criticals (live-only paths). — the operator's Micro click (16:39Z) STALLED like the first two; CODE-REVIEW workflow dispatched):** ① Operator clicked Upgrade-to-Micro ~16:39Z → postmaster STILL 07:36:03Z at 16:43 + 16:59Z; dashboard re-read (17:00Z): compute card STILL NANO + the same "already paying for Micro" banner + a NEW warning "Your project is currently exhausting multiple resources, and its performance is affected" + platform-incident banner still up ⇒ **third failed resize — stuck at Supabase's end**. Paths: wait out the platform incident and re-click, or support ticket referencing the incident. Gate label consequently rides the hourly :35 ticks (a lucky quiet-window clean tick) or the post-resize re-fire; 16:35Z tick: same starved signature (5/73, cErr 40, 281.6 s). ② **CODE REVIEW (operator-ordered, blanket approval): Workflow `wf_4109a59e-58d` dispatched ~16:40Z** — 7 Fable reviewer lanes over `b416f31..HEAD` (the whole 07-03→07-05 build, ~27k insertions/112 files: exec-rail, daemon, sql 0075–0083, edge panels, web, research-sim, seams+gaps) → adversarial verify (2 refuters critical/high, 1 medium) → consolidated verdicts; fixes only AFTER verification, suite+typecheck before any commit. Running in background. ③ Detached daemon riding fine; shadow week accruing. Next: workflow completion notification + the 17:35Z tick + resize watch. — steady-state watch tick; no state change):** postmaster unchanged (no Micro click yet). 15:35Z maker-exit tick: 291.2 s, 5/73 mkts, cErr 40, makerFillRate 0.2 on the tiny view — trend 1→0→5, still starved. Convergence oscillation continues (14:47 38/0 clean · 15:02 15/5 · 15:17 38/0 clean · 15:32 24/3). Detached daemon healthy (15:40 tick: 11 positions, 9 dry-run reprice intents, 0 failed). Gate blocked on the operator's Micro click; #50 + shadow-diff stay deferred. Next read: 16:35Z tick. (Committed bundled with the next cycle.) — OPERATOR BLANKET APPROVAL → 3 items EXECUTED + THE RESIZE MYSTERY SOLVED: the project is STILL ON NANO):** Operator asked open-items/timeline + granted blanket approval for self-executable items. ① **RESIZE VERIFIED VIA HIS LOGGED-IN CHROME (Settings→Compute and Disk): the resize NEVER APPLIED — compute is NANO (shared CPU / 0.5 GB), with the banner "This Project is already paying for Micro Compute. You can upgrade to Micro Compute at any time"** — billing moved to Micro, the instance didn't; the platform-incident banner ("investigating a technical issue") still up. Explains the whole day: no real CPU ever arrived; recovery = burst refill on Nano. NOT clicking Upgrade myself (DB restart during a platform incident whose failure class is exactly project-status changes) — **the one-click Micro upgrade is now THE critical path for the gate label; operator's call on timing.** ② **DURABLE DAEMON: swapped** — detached dry-run copy (cmd tree 72484, TRADE_TICK_SEC=60, log `out/trade-bot-detached.log`, out/ gitignored) started 14:28:57Z, first tick healthy (10 positions reconstructed, 0 failed); killed BOTH stuck morning trees + the surviving stopgap — exactly ONE daemon now; survives session exit; stop via `taskkill /T /F /PID 72484`. ③ **RETENTION JOB APPLIED (approved): cron jobid 30**, `15 3 * * *`, batched DELETE (≤50k rows/night) on cron.job_run_details — backlog drains over several nights, no IO spike. ④ **GATE: 14:35Z tick completed 272.5 s 'ok' but 0/73 mkts, cityErrors 44** (snap 229 / gate row worthless) — worse than 13:35's 1/73; authoritative stays snap 220 INSUFFICIENT 73/34/6 n_open 5. Pattern this hour: poll-markets clean :05–:25 then failed :30 (statement timeout); convergence 14:17 near-clean (29 entries/cErr 2) but 14:32 degraded (0/10/0) — Nano oscillates with load. NO manual re-fires (each ≈270 s wasted load). Capture confirmed back at */10 and healthy (80 ins/tick). ⑤ #50 gap-fill + shadow-diff STAY deferred (fragile DB). Next: hourly :35 ticks ride; wake ~15:41Z reads the 15:35Z tick; 22Z pipeline check tonight. Operator items now: the Micro click (gate-critical) · optional --live-smoke · that's it. — v11 cycle 1: PARTIAL RECOVERY confirmed (first clean panel tick since the crash) + THE STOPGAP DAEMON SURVIVED THE /clear):** ① Health: light query instant; the heavy metadata probe (pg_total_relation_size on opening_captures, 1.40 GB) instant on retry (first attempt was a cold-pool connection timeout, not starvation); postmaster start UNCHANGED 07:36:03Z — no restart, consistent with a cgroup-style resize; dashboard verification still owed by the operator. ② **RECOVERY IS PARTIAL, TRENDING GOOD: convergence 13:32Z tick FULLY CLEAN** (52.1 s, 39 entries, cErr 0, captureRows 6102, snap 564) — the record-RPC completes again; **maker-exit 13:35Z tick wrote (snap 228 / gate snap 222) but DEGRADED** — 283.4 s, 1/73 mkts, cityErrors 44 → 1-market partial view, NOT adjudicable; poll-markets still statement-timing-out (13:30 `upsert_market_snapshots`, 13:35 `poll_known_events`); captures healthy (79 ins @ 13:30), whale-watch full cadence, side-rails 17/19/20 back and ok (cross-venue + arb-depth 13:30 clean). Deadman halt 1 at the 13:30 health-monitor = the expected panel-staleness page; should clear now that panel ticks write. ③ **GATE: authoritative read remains snap 220 (05:36Z) INSUFFICIENT 73/34/6-of-7, n_open 5**; the 14:35Z scheduled tick is the next adjudication candidate — NO manual re-fire while per-city reads still time out. ④ **DAEMON: the C58 stopgap SURVIVED the /clear** (process tree of 12:12:53Z alive; bot_tick_log continuous 12:13→13:43Z, 28 paper ticks, avg gap ~200 s — 60 s cadence stretched by DB latency; **ledger 11 placed / 15 canceled / 0 failed, newest place 13:41:58Z**) — the shadow week is accruing, presumption of no-daemon was WRONG in the good direction. ALSO FOUND: the operator's TWO morning daemon trees (06:32Z + 06:36Z starts) are alive-but-STUCK (no ticks since 07:46Z) — operator should Ctrl-C those two terminals when convenient; duplicate wake-up would be idempotency-safe but noisy. ⑤ #50 gap-fill + shadow-diff first read: DEFERRED one more cycle (two RPC classes still failing); 22Z pipeline check tonight. Next wake ~14:15Z (light check), then the 14:35Z gate read.
- **C59 (~15:45 local / 13:45Z 07-05 — SESSION WIND-DOWN at 55% context; v11 RECOVERY-WATCH prompt written; stopgap daemon dies at /clear):** Operator called the pause. Post-C58 developments: ① operator pushed the compute resize to 2-core/1GB TWICE — postmaster NEVER cycled (07:36:03Z start persists); brief fast-query window ~13:04 suggested compute may have improved, then flakiness resumed; ② **status.supabase.com confirms an ONGOING platform incident since Jun 30: "Project status change failures in multiple regions" — compute capacity, eu-north-1 included** — resizes are exactly the failing operation class; our PG 17.6.1.127 > the recommended .121 floor (new machine types available on retry). Advice given: STOP re-pushing; verify in Settings→Compute and Disk later. ③ ALL crons restored ~13:06Z (capture */10 + panels 26/28 + side-rails 17/19/20) — self-healing posture: panel ticks retry hourly, fail benignly until real CPU. The 13:12 manual catch-up tick died with the operator's second restart push. ④ PowerShell forms given for the daemon + --live-smoke ($env: syntax; the bash one-liner was the error source). ⑤ Wind-down: C59 tick + v11 prompt (above) + /remember + close summary. OPEN AT CLOSE: gate day-7 label (first healthy full-panel tick) · #50 gap-fill + tomorrow's cron-slot proof · shadow-diff first read · operator: resize verification, durable daemon, optional --live-smoke, retention-job approval. Board: main == origin @ this commit; suite 161/2609 green; boundary intact all day (no capital, no keys, nothing live). (~14:30 local / 12:30Z 07-05 — ROOT CAUSE FOUND: the DB CRASHED/RESTARTED at 07:36:03Z (`pg_postmaster_start_time`); panels stay DOWN today; stopgap daemon UP):** ① Check #2 (11:20–12:16Z) read CALM (whale-watch 49/49 ok full cadence · poll-markets 8/9 · captures 2/2 ok 17 s · zero statement-timeouts) → convergence canary re-enabled for its 12:17Z tick → **FAILED the same way (105.3 s → `record_convergence_panel timed out after 15000ms`)** — small writes fine, ONLY the big TOAST panel-snapshot writes fail. ② Chasing that: relation 204950 (the morning tuple-lock wait) = `maker_exit_panel` (self-contention between the reaped tick's DB-alive statement and the 07:42 manual tick — explained, not current); no old transactions held now; then `pg_stat_user_tables` showed IMPOSSIBLE counters (opening_captures n_live 1002, autovacuum_count 0 everywhere) = stats reset ⇒ **`pg_postmaster_start_time()` = 2026-07-05 07:36:03Z — CRASH/RESTART confirmed.** Rewrites the narrative: 06:35 reap = pre-crash pressure; 07:35 "job startup timeout" = the instance dying; cold cache + drained IO burst budget from 07:36 onward = every failure since (incl. the 07:42 manual tick's cityErrors 28 and all 15 s record-RPC timeouts); the daemon died 07:46 losing its connection. Crash cause unknown from SQL (OOM suspected — the 07:30–07:35 window stacked capture+convergence+the failing maker-exit+daemon+probes); **operator must read dashboard → Reports → Database (memory/IO/instance health) for the verdict.** ③ DECISION: **NO panel retries today** (each retry burns ~100 s + fails the same write; risks re-OOM if that's the cause) — jobs 26+28 stay PAUSED, side-rails 17/19/20 stay paused, capture */20 + Lane-A core + whale-watch keep running; re-enable attempt tonight ~22:30Z (post-22Z-pipeline, cache warmed by the day's light traffic) or immediately after an operator compute upsize. Gate day-7 label consequently WAITS for the first healthy panel tick. ④ **Stopgap daemon (MY session, `TRADE_TICK_SEC=60`) STARTED 12:12:56Z + healthy** (first tick: 4 positions reconstructed + 4 reprices + 2 NEW 07-06 entries; ledger now 7 placed/9 canceled, 0 failed) — dies at session end; operator's durable copy still pending. ⑤ #50 gap-fill + the first shadow-diff read both DEFERRED to DB recovery. Operator asks standing: IO/memory graph read · durable daemon · optional --live-smoke. (~13:20 local / 11:20Z 07-05 — light check #1: NOT calm → DEEPER SHED (side-rails paused)):** 11:11Z read: pg_cron launching again but degraded — whale-watch firing every ~5–8 min (not 1), capture MISSED 10:20+10:40 and the 11:00 run exceeded 11 min, `record_reward_snapshots` (11:00) + `upsert_ensemble_rows` (10:35, the 10Z pipeline snapshot — self-heals at 22Z) both statement-timed-out. Two alter attempts also connection-timed-out (11:10 cluster) → backed off 5 min → **PAUSED the three analytics side-rails: jobs 17 (reward-snapshot) + 19 (arb-depth) + 20 (cross-venue) — all DORMANT-verdict surfaces, trivially reversible** (verified with 26+28 still paused, capture */20 active). Running now = Lane-A core + throttled capture + whale-watch. Check #2 ~12:15Z; canary re-enable pushed to ≥13:15Z on two calm checks. (~12:10 local / 10:10Z 07-05 — IO INCIDENT ESCALATED to FULL LOAD-SHED: 50-min refill was NOT enough; the 09:45 manual catch-up tick FAILED the same write-timeout way; #50's 10:00Z cron slot LOST):** ① The 09:45:52 manual maker-exit tick ran **322.6 s then FAILED on `record_maker_exit_panel timed out after 15000ms`** — identical to convergence's 08:32 failure; postgres logs show the tick smothering the instance (whale-watch "job startup timeout" ×3 09:52–09:54 + AccessExclusiveLock/ShareLock waits + statement-timeout bursts), recovering ~09:57 after it died. ② The 10:00Z stack NEVER CLAIMED (no job_runs rows: capture 10:00, **city-paper-trade 10:00 — #50's cron-slot proof slips to tomorrow**; gap-fill via the C45 `city-sim.ts` idiom once the DB is healthy, arms permitting). ③ The 10:02 convergence canary: 'ok' but **fully degraded — 0 entries / cityErrors 10 / captureRows 0 / 112.8 s** (its own DB reads failed). ④ Diagnosis REVISED: the disk-IO budget is drained to BASELINE — idle-looking between bursts, but any cold-data touch crawls; 50 min of panel-pause + late capture-throttle did not refill it; my own incident-response ticks/probes were themselves significant consumers (the 102 s `cron.job_run_details` seq scan, two manual panel ticks). **FULL LOAD-SHED @ ~10:07Z (verified): jobs 26 + 28 PAUSED, capture STAYS `*/20`, NO further manual ticks.** Plan: light job_runs-only checks at ~11:10Z + ~12:10Z; re-enable convergence as the single canary ~13:00Z ONLY on two calm checks; maker-exit re-enable after a clean convergence tick; capture back to `*/10` at full recovery. ⑤ EXPECTED PAGES (pre-warned, real-staleness-by-design, this incident not a new one): bot deadman ~10:46Z (last paper tick 07:46:37); capture deadman possible if the 10:20 capture tick also fails. Gate: last authoritative read remains snap 220 (05:36Z) INSUFFICIENT 73/34/6 n_open 5; snap 221/224-era partial views not adjudicable. Daemon still down (operator restart pending). OPERATOR ASK: check Supabase dashboard → Reports → Database → **Disk IO budget** to confirm/deny the throttle read; if the instance shows sustained exhaustion, the structural options (instance upsize · panel cadence reduction · job_run_details retention) are HIS call, staged on request. (~10:55 local / 08:55Z 07-05 — IO-BUDGET INCIDENT (the 07-03 class, 3rd occurrence) → PANELS PAUSED per the pre-authorized playbook; DAEMON DOWN ~07:46Z — operator restart needed):** Escalating evidence 08:32–08:52Z: convergence 08:32 tick FAILED (`record_convergence_panel timed out after 15000ms`) · maker-exit 08:35 scheduled tick ABSENT (2nd consecutive pg_cron startup failure after 07:35) · convergence 08:47 tick NEVER STARTED · my own mgmt-api probes timing out · pg_stat_activity: a 102 s `DataFileRead` IO-wait on a `cron.job_run_details` probe (my earlier query — that table is huge/unindexed; NEVER probe it under pressure; it self-terminated). Diagnosis: **drained disk-IO budget** (07-03 incident class) — the 06:35 wall-limit reap, both pg_cron startup timeouts, the 15 s RPC write timeout, and plausibly the 07:42 manual tick's cityErrors 28 are all downstream symptoms; opening-capture stayed healthy throughout (27–31 s, ins 74–109). **Playbook executed (2-consecutive rule met on BOTH panel lanes): jobs 26 (convergence-panel) + 28 (maker-exit-panel) PAUSED `active=false` ~08:53Z** — panels are re-replays, pausing loses ZERO data; capture (job 21) deliberately KEPT RUNNING (pausing it would lose book states permanently — deviation from 07-03's capture-throttle, justified by capture health). **Re-enable ~09:50Z + immediate manual maker-exit tick** (feeds the bot deadman before its 10:46Z staleness horizon — 180 min from the last paper tick 07:46:37; also the authoritative full-panel gate read). **DAEMON: DOWN since ~07:46:37Z** — bot_tick_log shows the daemon writes mode='paper' heartbeats (101 rows 06:50–07:46 ≈ its 30 s cadence; ZERO 'dry-run' rows ever — §8's single-mode deadman gap is real and the mode mapping should be verified when convenient), silent since; live_orders frozen since 07:31:59 (5 canceled + 5 placed, 0 failed — clean stop, nothing at the venue). Likely crashed in the DB slow window. OPERATOR: restart `TRADE_MODE=dry-run pnpm tsx scripts/trade-bot.ts` when convenient (reconstruction resumes management; picks up the 3 merged fixes: log-suppression 7e9604e + §11 e45e6b7+62579d7). Gate unchanged: INSUFFICIENT 6-of-7, no 07-05 settle visible as of snap 221 (partial view). #50 (10:00Z city-paper-trade, job 27) rides the refilled budget. (~10:30 local / 08:30Z 07-05 — MAKER-EXIT PANEL OUTAGE handled (2 missed ticks → manual catch-up tick OK) + §11 FOLLOW-UPS BUILT, LENSED MERGE-CLEAN, MERGED):** ① **Panel incident:** 06:35Z tick reaped ("exceeded wall limit, ADR-12") + 07:35Z pg_cron itself failed ("job startup timeout" — fn never invoked; cron.job_run_details) = 2 consecutive missed ticks, DIFFERENT failure modes (not the identical-failure playbook signature). Manually re-fired job 28's own command via a DO block (command never echoed) → tick OK in 243.1 s, snap 221 written, deadman staleness reset (no operator page). Tick ran DEGRADED — cityErrors 28, only 22/73 mkts scored → snap 221 is a partial view (C50 03:37Z artifact class): 22/13/**6 days** n_open 2 net −$24.5 on the truncated panel; still INSUFFICIENT, no 07-05 settle visible. Venue slowness window ~06:35–07:45 passed: convergence 07:47 tick fully clean (38.1 s, 39 entries, cErr 0) after four degraded ticks (5/5/5/3). **08:35Z scheduled tick = the pg_cron recovery test + the authoritative gate read.** ② **§11 follow-ups (build lane b): BUILT by s11-builder (opus worktree) → lens MERGE-CLEAN → MERGED (ff) + both lens LOWs APPLIED at merge** (C36 precedent): §11.1 `tradesResponseTruncated` (cursor/at-limit ⇒ degraded, exactly-like-a-throw into the sell-hold; also hardens the reconcile trades read → HELD) · §11.2 kill-cancel gated on `entryPollFresh` (stale poll ⇒ deferred + WARN, partial never orphaned). +24 tests; lens verified: no false-degraded on real client shapes (bare array, cursor branch dead), dry-run alert-silent (operator's daemon gets ZERO new alerts), tests falsifying, main() import-guard holds. LOW-1 WARN wording (resting entry CAN still fill under kill+outage, §9R-bounded) + LOW-2 docs scope-honesty (guard is LATENT defense-in-depth — current clob-client exhausts cursors internally) both applied. TRADING-ACTIVATION §11 both CLOSED. ③ Shadow week: daemon repriced its 5 entries ~07:31Z (5 canceled + 5 fresh placed, 0 failed — correct dry-run reprice). ④ Operator Q&A (Karachi 36 vs google 33): NOT an algo change — monsoon-onset regime flip; June-learned biases (JMA −1.9/GFS −1.7) pull cool models UP exactly when the cool cluster is right; actuals 33/34/36/34; logged as an observation (no new levers — backlog closed). Suite on merged tree + typecheck: see commit. #50 rides ≥10:05Z. (~09:15 local / 07:15Z 07-05 — v10 cycle 1: board verified, small-build (a) SHIPPED — clob-client bootstrap 400-dump suppressed; two watch items armed):** Board: main == origin @ `a8bb8cf`, tree clean. **GATE (snap 220, 05:36Z): INSUFFICIENT 73/34/6 unchanged, n_open 5** — no 07-05-targeted settle yet; snap 221 MISSING because the **06:35Z maker-exit-panel tick FAILED: "reaped by health-monitor: exceeded wall limit (ADR-12)"** (04:35/05:35 clean at 122/114 s) → single failure = benign per protocol; **the 07:35Z tick adjudicates** (2 consecutive reaps = incident playbook; probe after 07:42Z). **SHADOW WEEK: alive** — live_orders mode='dry-run' 5×'placed' (was 4 at C52), newest 07:01:42Z = the OPERATOR's daemon copy confirmed up post-handoff; 0 failed. **Convergence offset VERIFIED LIVE** (ticks 06:32/06:47/07:02 match `2,17,32,47`) BUT the two post-0083/post-daemon ticks 06:47+07:02 both read cityErrors 5 / entries 16 / captureRows ~2960 (clean tick: 0/39/~5900) — status ok so NOT the failure playbook; watching 07:17/07:32 before calling it (candidate causes: transient venue errs vs listing-day rollover vs 0083 interaction). Capture: all ok 29–77 s, ins>0 on every tick except a single inserted:0 at 06:40 (captured 101 — one-off, benign-watch). **BUILD (a) SHIPPED:** `suppressConsoleDuring` in `packages/trading/src/live.ts` scope-silences console for exactly the `createOrDeriveApiKey()` bootstrap call (the expected derive→create 400 made clob-client console.error the full axios error incl. transient L1 auth headers — C51 hygiene item); finally-guaranteed restore, throw paths stay loud; +2 tests (drop-inside/restore-after/pass-through + restore-on-throw). Suite **160 files / 2585 green** (+2), typecheck clean. #50 (10:00Z city-paper-trade proof) rides ≥10:05Z. Next lane: §11 follow-up (getTrades cursor/at-limit ⇒ degraded) if the watch stays quiet. — SESSION WIND-DOWN at 51% context; v10 DAY-WATCH prompt written; daemon HANDED TO THE OPERATOR'S TERMINAL):** Operator called the /clear. Written: the **v10 prompt above** (day watch: gate day-7 + #50 + shadow-week accrual + the 3 small authorized hardening items — clob-client log suppression / §11 follow-ups / the first shadow-diff read). **The session's background daemon copy (task b92hxtcbw) STOPPED at this tick** — the operator starts his own durable copy (`TRADE_MODE=dry-run pnpm tsx scripts/trade-bot.ts`); a dry-run gap is riskless (no capital, entries for today already positioned in the ledger; management resumes on restart via reconstruction). /remember written. Board at close: main == origin @ this commit; suite 160/2583 green; gate INSUFFICIENT 73/34/6 n_open 5; daemon ledger 4 dry-run placed rows; #50 (10:00Z) + the gate label ride v10.
- **C51 (~08:45 local / 06:45Z 07-05 — OPERATOR APPROVED ALL MORNING ITEMS → EXECUTED: 0082 APPLIED · smoke PASS · cron offset LIVE · one blocking defect found+fixed (0083) · the DRY-RUN DAEMON IS RUNNING — the shadow week has begun):** ① **0082 APPLIED** via MCP apply_migration + verified (config seeded mode='off' $10/$25/$40/$100/kill $30∨25%, 14 fns, both partial-unique indexes, preflight correctly BLOCKS w/ 3 reasons; /trading renders live). ② **convergence cron offset applied** — job 26 → `2,17,32,47 * * * *` (de-collides the all-night :00/:30 blip class). ③ **smoke PASS** (after fix): `derived OK · apiKey 632d3ff9… · sigType 2 · funder set` + `funder recognized · 0 open orders` — wallet/creds/funder wiring proven against the live venue; --live-smoke left for the operator (needs TRADE_MODE=live). **FIX 1 (`cd7740f`): the Deno-style `npm:` CLOB-client specifiers fail under Node/tsx** — bootstrapClobClient is now runtime-aware (Deno→npm:, Node→bare) + ethers@^5.7.2 / @polymarket/clob-client@^4 land as @weather-edge/trading deps ("nothing installed until the live phase deploys it" — deployed). ④ **first daemon start EXPOSED a blocker: `convergence_capture_inputs` strips the bucket IDENTITY fields** (economics-only JSON — no tokenYes/conditionId) → every EntryCandidate mapped to ''/'' → venue "Invalid token id" on every book read. **FIX 2 = migration `0083` (ADDITIVE: +conditionId/tokenYes/tokenNo per bucket) APPLIED to prod + verified**; twin test registered. ⑤ **daemon RESTARTED → HEALTHY**: first tick 4 candidates/4 intents (1 dry_run w/ full redacted maker payload, 3 idempotent duplicates), steady state 4 positions reconstructed/tick, skips=4 (no re-entry), failed 0; **ledger: 4 dry-run `placed` rows, 0 junk**. Suite 160/2583 green, typecheck clean. NOTE: clob-client's own 400-fallback logging dumps transient auth headers (public addr + timestamped sig — NO key material, no rotation needed); suppression = follow-up. Daemon runs as a session background task — the operator should move it under his own terminal/supervisor for durability. Remaining: #50 10:00Z verification · gate label (day 7) · both ride the watch.
- **C50 (~08:40 local / 06:40Z 07-05 — OPERATOR RETURNED → v9 NIGHT LOOP WIND-DOWN; full report delivered):** Watch cycles C49→C50 all green: gate snaps 217–220 INSUFFICIENT 73/34/**6-of-7** frozen, **n_open climbing 2→4→5** (07-05-targeted entries accruing; NONE settled yet → day 7 still pending, time-stop worst case later today); net −$160.05 unchanged (the 03:37Z −$144 read was a degraded-tick partial-view artifact, snap 219 confirmed). Prod: maker-exit 03:35Z one-off degraded tick (143 s/66/cityErrors 5) → 04:35Z + 05:35Z clean (122/113.9 s, 73/0) — note-and-watch closed, no playbook; the :00 capture-collision convergence blip recurred hourly-ish (01:30/03:00/04:00/05:00Z, single + self-recovering every time — the staged `2,17,32,47` cron-offset recommendation stands); capture healthy throughout (ins 75→109, fresh 07-06 markets listing). **Night slate final: T1+T2+T3+T4+T5 merged lens-clean + T6 docs — main == origin @ `baa3305`, suite 160 files / 2583 green.** Remaining on the board: the 10:00Z §4 verification (#50) + the gate label (#30) — both ride the day session. Wind-down: this tick + /remember + the morning report ending with the handoff §"Operator morning items".
- **C49 (~04:25 local / 02:25Z 07-05 — T4 + T5 MERGED on lens-clean verdicts → the LIVE-RAIL BUILD SLATE IS COMPLETE (T1/T2/T3/T4/T5 all on main); gate n_open 0→2 — first 07-05-targeted entries LIVE):** **T4 (`/trading` console)** delivered `a3deca5` (546-LOC page + getTrading loader + 8 tests; RPC-only off operator-guarded `dash_trading()`; explicit "0082 NOT APPLIED" empty-state = the day-one prod state; 3 payload gaps flagged as inherent to the frozen 0082 shape — fill-aggregates-only, open-LIVE-only order list, caps naming split) → **t4-lens R1: MERGE-CLEAN** (0 C/H/M, 2 LOW, 3 cosmetic; read-only/auth/degradation/parsing/risk-math invariants all verified) → LOW-1 (defensive inner-preflight guards) APPLIED at merge (C36 precedent), LOW-2 (frac=0 pathological meter display) WAIVED (SQL interlock authoritative; default 0.25) → **MERGED `dd25f7d` + pushed** (suite 2563/159 green, typecheck clean). **T5 (shadow-diff harness, BUILD-ONLY)** delivered `bb8a455` (945-LOC + 14 tests; eventId-join alignment; the load-bearing structural finding: the DRY-RUN ledger is ENTRY-INTENTS-ONLY — no fills → exits/P&L are side-B-only until live, documented) → **t5-lens R1: FINDINGS 3 MED / 4 LOW** (M1 stake-knob mismatch scored on 100% of rows; M2 the REAL multi-bucket capital-doubling anomaly tagged-not-scored; M3 expected classes inflating the score against the documented contract; + reprice/failure conflation, dead timing column, city-scope coupling, coverage gaps) → **AMEND `5d4e33e`: all 7 closed** (two-pass global normalization w/ ≥3-sample floor; multiBucket +50 + severity-ordered notes; intent-only suppression + a BONUS principled fix — realizedPnlUsd now requires a SELL fill per the 0082 N1 realized-at-sell convention, killing a fabricated −P&L on every open BUY; 6 locking fixtures, tests 14→20) → **t5-lens R2: MERGE-CLEAN** (all 7 verified closed w/ delete-the-guard falsification; 1 NEW LOW L-N1 — live-only whole-position-cashflow vs per-sold-share basis during a partial-sell window, self-heals at full close, dry-run-immune) → **L-N1 applied as the lens-prescribed comment caveat at merge; T5 MERGED + pushed.** Suite/typecheck on each merged tree recorded in the commits. **GATE (snap 216, 01:36Z): INSUFFICIENT 73/34/6 — but n_open 0→2** (first 07-05-targeted entries; day 7 = first settle). PROD 00:00–01:45Z: two single known-benign blips both recovered next tick (maker-exit 00:35Z 208 s/46/cityErrors 17 → 01:35Z clean 97.7 s/73/0 · convergence 01:30Z :30-collision 13/6 → 01:45Z clean 37/0); capture healthy ins 75→94. Task #54 complete. NEXT: T6 docs (orchestrator-inline) → watch mode (gate day-7 + the 10:00Z §4 verification).
- **C48 (~01:58 local 07-05 / 23:58Z 07-04 — v9 cycle 1: T2 recovery adjudicated → RE-DISPATCHED fresh; gate 6/7 unchanged; prod fully green):** Board verified: main == origin @ `b1f40dd`, tree clean. **Clock note: the C47/handoff "01:55Z" stamps were local times mislabeled as Z — true wall clock at this tick is 23:58Z 07-04** (night duties unaffected; the 10:00Z 07-05 verification is ~10h out). **T2 RECOVERY (handoff §"T2 recovery"): the stale worktree `agent-acc5ba897c3517d45` is EMPTY** — branch at base `d071c62`, zero commits past main, clean status, none of the four deliverables exist → the incomplete/absent path: stale worktree ignored, **T2 re-dispatched fresh (t2-daemon, opus, worktree isolation, background) with the handoff §"T2 brief" verbatim, base = current main `b1f40dd`.** GATE (snap 214, 23:36Z): INSUFFICIENT — 73/34/**6-of-7** unchanged (net −$160.05, makerFillRate 0.082, rebate $0, n_open 0); day 7 = first 07-05-targeted settled entry. PROD (21:30–23:50Z): capture */10 all ok 6.6–50.5 s / ins 61–69 · maker-exit :35 ×3 all ok 105–123 s / 73 / 0 · convergence */15 all ok 26–45 s / 36 / 0 **incl. every :00/:30 collision tick — zero blips this window**. Tick held uncommitted to bundle with the T2 merge (C44 precedent; suite runs once on the merged tree). Next: t2-lens on delivery → merge → T4+T5 parallel. **↳ TWIST (~00:35Z): the ORIGINAL t2-daemon had NOT died at /clear** — it survived as a background task and delivered `c3bc49d` on its own branch (all four deliverables, +2288 LOC/9 files, +48 tests, suite 2527/157 claimed green, §15 intact; flagged: single-mode deadman gap, capture-window-bounded restart reconstruction, venue-floor smoke size, DATABASE_URL creds). The fresh duplicate dispatched by this cycle was **TaskStopped mid-start (zero commits — no conflict)**. t2-lens dispatched on the c3bc49d diff vs main per handoff §t2-lens. **↳ LENS R1 (~01:30Z 07-05): FINDINGS — 1 CRITICAL / 2 MEDIUM / 2 LOW, all adjudicated REAL-fix, none waived.** CRIT-1: filled maker-TP never subtracted from `remaining` → naked FAK over-SELL on the modal WINNING path (TP fills → time-stop clock later fires exit_taker for the full size; live-only, dry-run-blind since refreshFill early-returns in dry-run). MED-2: reprice_entry bypasses preflight → unfilled entries keep working through a kill state. MED-3: FAK partial exit rows are venue-dead but non-terminal → remainder re-fires silently blocked as `duplicate`; stuck time-stop remainder rides to resolution. LOW-4: --i-know-no-preflight escape bypassed the TRADE_MODE gate too (strict reading imposed: escape bypasses ONLY preflight). LOW-5: dual exit rows double-count (rides CRIT-1 fix). Amendment dispatched to the ORIGINAL t2 builder (resumed from transcript, same branch); delta re-lens (scoped R2) on its commit. **↳ AMEND-1 `9b10ca2` (~02:00Z): all 5 closed** — CRIT-1 fixed DEEPER than the lens sketch (soldSize = max(Σ visible tp/sl/ts fills, VENUE getTrades sell-fill truth) — ledger-only sums re-open the over-sell because by_intent hides canceled rows' fills; + TP-cancel race guard aborts the taker on allCanceled=false) · MED-2 cancel_entry on kill · MED-3 venue-dead FAK corpse adjudication (partial OR 0-fill placed, orderId-bearing) with a false-green-proving FakeLedger · LOW-4 mode-first smoke gate · LOW-5 rides CRIT-1. Suite 2547 green (+20), typecheck clean. **↳ LENS R2 (~02:30Z): all 5 CLOSED verified (delete-the-guard falsification confirmed on every new test) — 0 CRIT/HIGH/MED, but 2 NEW LOWs from the fixes:** NEW-LOW-1 getTrades outage degrades the over-sell guarantee to the venue balance check (WARN-only today; safety-load-bearing read) · NEW-LOW-2 kill-cancel of a PARTIAL entry terminals the row → position unreconstructable → held shares lose SL/time-stop backstop. Lens says mergeable-as-follow-ups; **adjudicated FIX-NOW** (order-posting code, builder warm, within the 3-round cap) → AMEND-2 dispatched: exit self-pause + CRITICAL alert on venueSold outage; kill-cancel scoped to fully-unfilled entries (partial remainder deliberately kept working — preflight already counts it committed). R3 = scoped verify of the two deltas, then merge. **↳ AMEND-2 `446f15d` (~02:50Z): both LOWs closed** — NEW-LOW-1: getTrades now safety-load-bearing (`soldTruthDegraded` holds ALL sells for the position + CRITICAL `TRADE_BOT_SELL_HOLD` pages every affected tick, no dedupe; resumes correctly sized on recovery; 7 tests) · NEW-LOW-2: kill-cancel scoped to fully-unfilled entries (partial remainder deliberately left working — the venue-only-cancel alternative evaluated + REJECTED with documented rationale). Suite 2555 green (+8), typecheck clean. **↳ LENS R3 (~03:15Z): MERGE-CLEAN** — both R2 LOWs verified closed (delete-the-guard falsification confirmed), docs consistent; 2 new LOWs found + **WAIVED as follow-ups** (stated reason: near-zero trigger probability at this strategy's ~1-BUY/≤3-SELL per-token trade count, §9R-capped, venue-balance backstopped; recorded in TRADING-ACTIVATION §11): NEW-LOW-3 getTrades silent-truncation can false-negative the degraded flag (single-asset_id call won't paginate here) · NEW-LOW-4 a poll-missed partial entry fill concurrent with a kill-cancel orphans that fill (fully-filled case self-protected via allCanceled=false). **Convergence R1→R3: 1 CRIT + 2 MED + 2 LOW → 2 LOW → 2 narrower-LOW (waived) — the asymptotic pattern; stopped on MERGE-CLEAN per protocol. T2 MERGED to main (stack c3bc49d+9b10ca2+446f15d, zero conflicts); suite+typecheck on the merged tree recorded below; task #53 complete. NEXT: T4+T5 parallel dispatch.**
- **C47 (~03:55 local 07-05 / 01:55Z [ACTUAL ~01:55 local / 23:55Z 07-04 — see C48 clock note] — operator to bed → NIGHT WIND-DOWN of this session; v9 prompt + LIVE-RAIL-NIGHT-HANDOFF written; T2 in flight will die at /clear):** Operator: "/clear + write the night loop prompt." Written: **`docs/ops/LIVE-RAIL-NIGHT-HANDOFF.md`** (board, T2 recovery procedure + the T2/t2-lens/T4/T5/T6 briefs verbatim, the proven lens→amend→merge protocol, final seam contracts, operator morning items) + the **v9 prompt above** (night: finish T2→T4/T5→T6 with lens loops + all v8.1 watch duties + the 10:00Z verification). t2-daemon (branch `worktree-agent-acc5ba897c3517d45`) dies at /clear — v9 cycle 1 recovers or re-dispatches from the handoff. Gate at last read: 6/7 unchanged. Operator setup DONE tonight: dedicated wallet created+funded (§9R envelope), `.env.local` populated (key/funder/sigtype — via editor, never chat). Session tally: gap-fill executed+verified (C45) · T1+T3 built, 3-round-lensed, MERGED (C46) · T2 dispatched · 1 CRITICAL + 1 HIGH + 1 cross-midnight circuit-breaker defect caught pre-merge by the lens loop. Next session = v9.
- **C46 (~03:40 local 07-05 / 01:40Z — LIVE-RAIL T1+T3 MERGED to main; 0082 staged DARK; T2 daemon dispatching):** **Operator override ~22:47 local 07-04 authorized the LIVE-RAIL slate** (buy-side execution + activation console, tasks #51–#54; runner=LOCAL daemon, stake default $10/hard-ceiling $25 §9R; operator populated `.env.local` himself — key/funder/sigtype, never in chat). Both lanes built on opus worktrees with full adversarial lens loops: **T3 (activation console)** `742018d` — migration `0082` (trade_config + audit + trade_gate_override w/ 14d-capped expiry + live_orders/live_fills + 7 bot_order_* RPCs + trade_live_preflight interlock [live requires FORWARD gate PASS or explicit override] + shared `trade_today_realized_loss()` + dash_trading) — 3 lens rounds + endorsed micro-pass (R1: 1MH/3M/5L incl. the never-expiring override; R2: verified + found the cross-midnight daily-loss blind spot → realized-P&L-at-sell redefinition + exact fill_notional; R3: MERGE-CLEAN). **STAGED DARK — 0082 NOT applied; apply is operator-gated.** **T1 (maker execution rail)** `c27e531`+`38a3148` — MakerExecutor (price-enforced maker + post_only defense-in-depth, taker FAK exits, cancel/reprice, partial fills, mode off/dry-run/live default-never-live, dry-run writes mode-scoped ledger rows, reconcileOpenOrders STARTUP-ONLY sweep, redaction, fail-loud parsers) — 3 lens rounds (R1: **1 CRITICAL** post-success-error freed the idempotency key → double order — CLOSED w/ regression test; R2: all 8 verified + **1 HIGH** lost-response residual → hold-at-intent fix; R3 scoped: MERGE-CLEAN). Suite on merged main **155 files / 2479 green**, typecheck clean (one T1/T3 TradeMode seam fix `38a3148`). Boundary intact throughout: no capital, no keys touched, nothing live, no DB writes. NEXT: T2 daemon (opus worktree) + smoke script → T4/T5. Gate + prod reads ride the next watch tick.
- **C45 (~00:10 local 07-05 / 22:10Z 07-04 — v8.1 cycle 2: §3 GAP-FILL EXECUTED + VERIFIED — 24/24 bets, 4 cities, full arm sets; gate 6/7 unchanged; prod fully green):** Window confirmed open (22:08Z ≥ 22:00Z) → ran `city-sim.ts --from 2026-07-04 --to 2026-07-04` (local env creds). **Verified per-city: KHOU (°F) 6 bets arms 11–16 · LTAC 6 bets arms 11–16 · OPKC 6 bets arms 10–15 · WSSS 6 bets arms 10–15 — exactly the spec'd universe, all written 22:08Z.** CITY-SIM-PLACEMENT-FIX banner updated (§3 done; only §4 remains — the 07-05 10:00Z cron-slot proof, task #50). **GATE (21:36Z snap 212): INSUFFICIENT — 73/34/6-of-7 unchanged** (fillRate 0.082, rebate $0, n_open 0; day 7 = first 07-05-settled entry, expected through 07-05). Prod: maker-exit 21:35Z ok 114 s/73/0 · capture 26–86 s ins 67–69 · convergence clean incl. the 22:00 :00-tick (45 s/36/0 — no collision blip this hour). **Board note: the operator OVERRODE v8.1's no-build-lanes mid-session (22:47 local) → the LIVE-RAIL slate is running: T1 (maker exec rail) + T3 (activation console, 0082 staged-dark) dispatched on opus worktrees (#51/#52); T2 daemon + T4/T5 queued (#53/#54); runner = LOCAL daemon per operator; stake default $10/hard-ceiling $25 (§9R). Gate-watch duties continue inside this loop.** Committing this tick + C44 + the banner (suite+typecheck first).
- **C44 (~23:30 local / 21:30Z 07-04 — v8.1 cycle 1: gate 6/7 unchanged; prod green, one benign :00 convergence blip; 07-04 gap-fill PENDING its ≥22:00Z window):** Board verified main == origin @ `6d59e6c`, tree clean. **GATE (20:36Z snap 211): INSUFFICIENT — 73 mkts / 34 cities / 6 of 7 days** (net −$160.05, makerFillRate 0.082 vs 0.49 backtest, rebate $0, n_open 0) — no 07-05-targeted entry has settled yet; day 7 expected through 07-05 per C42. Prod (3h window): maker-exit 18:35/19:35/20:35Z ok 108–115 s / 73 mkts / 0 cityErrors (in the 95–250 s band) · capture */10 all ok 26–86 s, inserted 69–71 (the 21:00Z tick 85.9 s with 2 `get_build_inputs` statement-timeout seed skips — single, quality-gate fallback held, note only) · convergence 21:00Z **blip** (77 s / 4 mkts / cityErrors 9 / captureRows 489 — the :00 capture-collision class) → 21:15Z fully recovered (30 s / 36 mkts / 0) — single occurrence, no playbook per the v8.1 known-benign rule. **Gap-fill check (cycle-1 conditional): `city_paper_bets` has NO target-07-04 rows** (latest = the 07-03 20:11Z manual run) but now = 21:24Z < the ≥22:00Z window (KHOU's last arm locks 21:00Z + book margin) → deferred to cycle 2, wake ~22:06Z: run `city-sim.ts --from 2026-07-04 --to 2026-07-04`, verify per-city rows, update the CITY-SIM-PLACEMENT-FIX banner, commit+push (this tick held uncommitted to bundle into that commit; suite runs once). Tasks #49 (gap-fill) + #50 (the 07-05 10:00Z cities:4/placed>0 verification) armed.
- **C43 (~21:30 local / 19:30Z 07-04 — OPERATOR GO → the CITY-SIM-PLACEMENT-FIX bundle steps 1–3 EXECUTED LIVE):** Operator (in-session): "go". **Migration 0081 APPLIED** via MCP apply_migration (19:26Z) · **city-paper-trade REDEPLOYED** via authed CLI (19:27Z) · **smoke PASS** — `city_sim_active_configs()` now returns `{rows:[…]}` (jsonb object / rows array / 4 configs: ankara, houston, karachi, singapore). Remaining: the §3 gap-fill for target 07-04 at ≥22:00Z (`pnpm tsx scripts/city-sim.ts --from 2026-07-04 --to 2026-07-04` — waits for houston/ankara's late arm snapshots) + the §4 verification at the 07-05 10:00Z tick (cities:4/placed>0). Wakeup chain armed to ~22:05Z; the v8 prompt gets rewritten (v8.1) once the gap-fill lands.
- **C42 (~21:15 local / 19:15Z 07-04 — DAY-BUILD WIND-DOWN (D8 executed); loop v7 ENDS, v8 GATE-WATCH prompt written):** Final gate read (18:36Z snap 209): **INSUFFICIENT — 73/34/6-of-7 unchanged** (net −$160.05, fillRate 0.082, rebate $0, n_open 0; the 17:36Z 72/33 dip was a 1-city degraded tick, fully recovered — NOT window aging). Day 7 = the first 07-05-targeted settled entry; zero entries all day (July-4-thin books vs the $150 depth floor) ⇒ label expected ~07-05; playbook + deadmen carry it. **DAY SCORECARD: D5 ✅ `75f5194` (flagship, lens MERGE-CLEAN, EHAM self-check PASS 0.981) · D6 ✅ `169846c` · D7 ✅ `c8d8291` (lens MERGE-CLEAN, LOW applied) · #31 ✅ (°F sane; exposed the placement defect) · cps-fix ✅ `9717461` (0081 STAGED DARK + bundle `docs/ops/CITY-SIM-PLACEMENT-FIX.md`) · D8 ✅ (POST-FABLE-HANDOFF "What changed" section + BUILD-STATE ☀️ 07-04 addendum + this tick + v8 prompt).** Suite 151 files / 2308 green throughout; every commit pushed (Vercel). Prod green; two benign transient blip classes recorded (C40/C41) + the convergence cron-offset recommendation staged. **OPERATOR PENDING: (1) the CITY-SIM-PLACEMENT-FIX bundle (apply 0081 → redeploy city-paper-trade → 07-04 gap-fill → verify 10:00Z tick cities:4/placed>0); (2) optional convergence cron offset `2,17,32,47`; (3) the gate label ~07-05 (KILL pre-authorized per playbook · PASS stage-only). Next session: /clear + paste the v8 GATE-WATCH prompt above.**
- **C41 (~16:40 local / 14:40Z 07-04 — watch: gate 6/7 unchanged through 14:36Z; maker-exit recovered clean; convergence :30-tick contention pattern identified):** GATE (snaps 204/205): INSUFFICIENT 73/34/6, net −$160.05, n_open 0 — no 07-05 entry has settled yet. Maker-exit 13:35Z + 14:35Z ticks CLEAN (105.8 s + 103.7 s / 73 / 0) → the 12:35Z partial (cityErrors 10) was a one-off, note-and-watch CLOSED, no playbook. **Convergence blipped a second time at 14:30Z (77.1 s / 14 mkts / 5 errors), NON-consecutive (14:15Z clean 31.4 s/36/0) — both blips (11:30Z, 14:30Z) land exactly on ticks co-scheduled with an opening-capture start (:00/:30 collision points); intermittent DB contention, self-recovering, deadmen correctly silent.** Not actionable under today's approvals (cron changes not pre-approved) → STAGE for the operator in D8: consider offsetting the convergence cron (e.g. `2,17,32,47 * * * *`) to de-collide from capture's */10 grid. Watch continues hourly.
- **C40 (~14:45 local / 12:45Z 07-04 — watch: 12:35Z maker-exit tick PARTIAL (cityErrors 10 → snapshot 56/26); explained, note-and-watch armed):** GATE (snap 203, 12:37:59Z): INSUFFICIENT, 6 days — but n_markets 73→56 / n_cities 34→26 / net −$134.01: NOT data loss, the 12:35Z tick ran degraded (cityErrors **10** vs bar ≤2, 177.5 s vs ~100 s — per-city series-read timeouts under a midday DB slowdown; the WS-1 bounded-pool partial-view degradation working as designed; same class as the 11:30Z convergence blip, 5 errors, which recovered at 11:45Z). Day count NOT regressed → no package-3. Also established: all 45 **07-05-targeted events listed 07-03 17:11Z–07-04 01:10Z** (earlier than C31's ~09:00–15:00Z estimate) yet **n_open 0** — the replay hasn't settled-entered any (likely the $150 depth floor unmet on thin fresh books); day 7 lands on the first 07-05 entry+exit, possibly later than mid-day (time-stop worst case = tomorrow morning). Wind-down at ~20:00Z regardless per the v7 clause; D8 will hand the label to the deadmen + playbook. **Watch: if the 13:35Z maker-exit tick repeats cityErrors>2, that is two identical failures → the pre-authorized incident playbook.**
- **C39 (~13:53 local / 11:53Z 07-04 — watch cycle: gate 6/7 through the 11:35Z tick; one convergence blip, recovered):** GATE (snaps 201/202, 10:36Z/11:36Z): INSUFFICIENT — 73/34/6-of-7 frozen, **n_open 0** (no 07-05-targeted entries yet — day 7 still pending, those markets list ~09:00–15:00Z and must enter+settle). Prod: maker-exit 11:35Z ok 99.3 s/73/0 · capture 15–34 s · convergence 11:30Z **blip** (69.9 s/14 mkts/cityErrors 5 — single occurrence, prior + next ticks clean: 11:45Z 61.3 s/36/0) → note-and-watch CLOSED, no playbook (transient venue fetch errors, likely contention with the co-scheduled capture tick). All D-lanes + #31 + cps-fix done; waiting only on the gate label. Next reads per hourly :36 snapshot.
- **C38 (~12:35 local / 10:35Z 07-04 — cps-fix INTEGRATED + PUSHED `9717461`; 0081 STAGED DARK; operator bundle ready):** The fix lane delivered in ~18 min with live-verified falsification proofs BOTH ways (reverting the handler read fails the new fake-port test; reverting 0081 trips the new tripwire). Footprint: 0081 ({rows} envelope; inner aggregate verified byte-identical against the LIVE prod def fetched at 10:20Z) · deploy-order-safe 3-shape `readActiveConfigs()` · index.ts targetDate body-parse (the agent's one scope add — ACCEPTED: without it the documented gap-fill silently no-ops; non-consuming req.clone(), regex-guarded, silent fallback to the normal tick) · scripts/city-sim.ts tolerant unwrap · class TRIPWIRE over all 17 public no-arg jsonb RPCs (sole offender was this RPC). **Review-lens WAIVER, documented:** the orchestrator performed the lens personally — root cause + live RPC def verified against prod first-hand, every diff hunk read, agent proofs falsification-grade; a second independent lens would re-verify already-prod-verified claims. Merged tree **151 files / 2308 green + typecheck clean** → pushed. Worktree removed. **OPERATOR BUNDLE (docs/ops/CITY-SIM-PLACEMENT-FIX.md): apply 0081 → redeploy city-paper-trade → gap-fill target 07-04 (after ~22:00Z when houston/ankara arms complete) → verify next 10:00Z tick shows cities:4/placed>0.** Task #48 build-complete.
- **C37 (~12:20 local / 10:20Z 07-04 — #31 EXECUTED: °F path SANE, but the 10:00Z tick's placement is STRUCTURALLY DEAD → root-caused to the 0044 jsonb-array trap; fix lane dispatched):** **GATE (09:36Z snap 200): INSUFFICIENT — 73/34/6-of-7 unchanged** (net −$160.05, fillRate 0.082, rebate $0). Prod green (maker-exit 09:35Z 113 s/73/0 · capture 28–53 s/75 · convergence 24–44 s/0). **#31:** 10:00Z city-paper-trade tick status ok BUT `stats {cities:0, graded:12, placed:0}` → dug in: `city_sim_config` healthy (4 active rows, ankara/houston arms 11–16, thru 07-31) yet **NO target-07-04 bets exist for ANY city, and every existing bet traces to the backfill scripts (placed_at 23:07Z 07-02 / 20:11Z 07-03) — the cron has NEVER placed.** ROOT CAUSE (verified): `city_sim_active_configs()` RETURNS jsonb as a **top-level array** → `supabasePort` (db.ts:44–48) misclassifies bare arrays as RETURNS-TABLE row sets per its documented invariant → handler's `cfgRows[0]?.city_sim_active_configs` is undefined → `configs=[]` → `cities:0`. Grading unaffected (`city_sim_grade_inputs` already {rows}-wraps — the SAME 0044 trap this RPC violates). The PGlite twin can't see it (twin's `select * from fn()` always yields the wrapped shape). The 07-03 silent-pause diagnosis (active=false) was real but MASKED this second, deeper defect — backfills kept the ledger alive. **°F CONFIRMATION (the #31 payload, on the graded 07-03 backfill day): SANE end-to-end** — 2°F ladder ("94-95°F"/"96-97°F"), convert-then-round correct (96.08→96→"96-97°F" won; 93.91 lift→94 lost vs actual 96), shares/fee/P&L reconcile; dash surfaces 4 cities. **Dispatched `cps-fix` (opus worktree, task #48): migration 0081 ({rows} wrap) + deploy-order-safe handler read + a failing-against-old-code port-shape test + a class TRIPWIRE (no public no-arg jsonb fn may return a top-level array) + `docs/ops/CITY-SIM-PLACEMENT-FIX.md` operator bundle. Build staged dark — the deploy (0081 + fn redeploy + 07-04 gap-fill) is OPERATOR-GATED per today's boundaries.** #31 recorded complete; the 07-04 °F graded-day remainder is superseded by the gap-fill step.
- **C36 (~11:15 local / 09:15Z 07-04 — D5 FLAGSHIP INTEGRATED + PUSHED `75f5194` on a MERGE-CLEAN lens → ALL THREE DAY-BUILD LANES LANDED):** D5 delivered the full brief: 45/45 cities × 20 years ERA5 (900 chunks, zero 429s/failures), committed `city-climatology.ts` asset (~88KB) + catalog (slugs/tz verbatim vs 0066/0067 per lens diff), display-only /paper-trade tile + per-arm clim-floor column, EHAM self-check **PASS** (peakedCorr 0.981 / maxAbsDiff 0.270 / histCorr 0.892 / medianΔ 1h). Lens `d5-lens`: **MERGE-CLEAN, zero must-fix** — math is the literal Amsterdam engine reused (imports the shared helpers), Open-Meteo tz semantics verified no-double-convert, Amsterdam asset byte-identical, display-only proven by repo-wide import grep, monotonicity verified programmatically across all 45. Its one LOW advisory applied at merge: self-check abs-diff backstop 0.30→0.35 (corr stays the binding gate; doc updated). The 180MB ERA5 disk cache MOVED to the main tree (`scripts/.cache/era5/`, gitignored) so re-emits/self-checks stay fetch-free. Merged tree **151 files / 2304 tests green + typecheck clean** (2101+203 reconciles) → pushed (Vercel). Worktree removed. **DAY-BUILD slate: D5 ✅ D6 ✅ D7 ✅.** Remaining: #31 (≥10:05Z) · gate day-7 (mid-day) · D8 wind-down.
- **C35 (~10:52 local / 08:52Z 07-04 — D7 INTEGRATED + PUSHED `c8d8291` on a MERGE-CLEAN lens):** D7 reported in ~17 min with the honest deviation the brief pre-authorized: the full 45-city per-source table was NEVER archived in-repo (only the ~2,100-event aggregate-by-lead + illustrative extremes + the "8 cities" guard) → built from the canonically recorded numbers + caveated the gap on the asset AND the page; no re-derivation. Opus lens `d7-lens`: **MERGE-CLEAN** — all numbers trace verbatim (MAKER-EXIT-SIM.md:253–261 · CONVERGENCE-TUNING.md:13,152–155 · CITY-SIM.md §7), selector diagnostic correctly NOT framed as accuracy, consensusSource note rendered operator-gated/DORMANT, calibration.test.ts byte-identical, scope exactly 4 files. Its one LOW applied at merge: the regenerated 819-event corroboration (74.4% vs 53.0% ±1 + the ~2,100-event lead-1 79/62) now renders next to the 708-event detail table, values from SELECTOR_DIAGNOSTIC_META (never re-typed); /data render test extended with 6 asset-coupled assertions. Merged tree **147 files / 2101 tests green + typecheck clean** → pushed (Vercel). Worktree removed. D5 alone in flight.
- **C34 (~10:37 local / 08:37Z 07-04 — D6 INTEGRATED + PUSHED `169846c`):** D6 reported in ~8 min: footprint exactly per brief (2 files, +65/−7 — home `/` twelve-ways verdict hero between the skill strip and the /efficiency proof section + extended render test). Verified the diff by hand: SIGNAL_HERO numbers IMPORTED (zero re-typed literals), pure existing `.ams-dash` classes, test assertions coupled to the asset. No lens needed (imported-asset numbers, not computed/copied). Merged tree: **suite 146 files / 2080 tests green + typecheck clean** (observed file count is 146, not the logged 144 — test COUNT 2080 matches; the 144 was a miscount, correcting the baseline here). Committed scoped + pushed → Vercel deploying. Worktree removed. D5 + D7 still in flight (~30 min). #31 at ≥10:05Z.
- **C33 (~10:30 local / 08:25Z 07-04 — v7 cycle 1: D5/D6/D7 DISPATCHED on opus; gate 6/7 unchanged; prod green):** Board verified main == origin @ `b90c7b7`. **GATE (07:36:57Z snap 198): INSUFFICIENT — 73 mkts / 34 cities / 6 of 7 days** (net −$160.05, makerFillRate 0.082 vs 0.49 backtest, rebate $0, 0 open; 07:35Z tick qualifyingTickFrac 0 / dominantDisqualifier 'none' — the 1b reward term stays bounded at zero). Day 7 = the first 07-05-targeted position settling, expected mid-day (C31's settled-entry-date mechanism). **Prod green on all three bars:** capture 25–55 s / 75–88 inserts · maker-exit 07:35Z ok 117.0 s / 73 mkts / 0 cityErrors / snap 201 · convergence 27–43 s / 0 errors / 35 mkts. **D5 (ERA5 45-city climatology) + D6 (home twelve-ways hero) + D7 (/data model-use panel) dispatched as parallel background OPUS worktree agents, slate briefs pasted verbatim + standing constraints** (tasks #44–46 in_progress). #31 waits for ≥10:05Z. Next wake 1500 s.
- **C32 (~10:15 local / 08:15Z 07-04 — OPERATOR BLANKET APPROVAL for the day → 0079+0080 APPLIED LIVE; DAY-BUILD slate D3–D8 + v7 loop written):** Operator (going away for the day): "approval to all items needed to be done — map out the day's action tasks, maximize the build." Executed immediately: **migrations `0079_maker_exit_assumptions_history` + `0080_dash_city_forecast` APPLIED to prod via MCP apply_migration (08:10Z)** — smoke calls return ERR_FORBIDDEN from operator_guard on every non-operator path (the guard verified unbypassable, exactly the 0065/0073 contract; function behavior is pinned by the PGlite twins in the 2080-green suite; the operator's logged-in session lights the /maker-exit sparklines + /paper-trade intended-bet tiles — no fn redeploys needed, read-only RPCs). **Scope check that changed the plan: WS1-ROLLOUT.md line 33 keeps maker-exit-panel HOURLY by deliberate decision** (4×/hour heavy ticks = the incident class WS-1 killed) → the tempting gate-day cadence restore is NOT on the board. **DAY-BUILD slate D3–D8 written above + the v7 loop prompt** (D3 gate-day per playbook · D4 #31 at ≥10:05Z · D5 ERA5 45-city peak-hour climatology, the flagship · D6 home twelve-ways hero · D7 /data per-city model-by-use-case panel · D8 docs wind-down). Approval boundaries recorded: KILL pre-authorized to record; PASS stage-only; capital/wallet/keys OPERATOR-PHYSICAL (unchanged, non-delegable); new migrations beyond 0079/0080 stage-only; no new signal hunts. Subagents OPUS. Session /remember + /clear handoff next — the v7 loop takes the day.
- **C31 (~02:50 local / 00:50Z 07-04 — FINAL night tick: gate 6/7 with the DAY-COUNT MECHANISM CORRECTED; early WIND-DOWN, loop stopped):** **GATE (00:36:58Z snap 191): INSUFFICIENT — 73/34/6-of-7, aggregates unchanged** (net −$160.05, makerFillRate 0.082 vs 0.49, rebate $0, 0 open; qualifyingTickFrac trend now **0×5** through the 00:35Z tick, dominantDisqualifier 'none'). **CORRECTION to C15's "label renders when 07-03 grades": n_distinct_days counts SETTLED-ENTRY dates, not graded target-dates.** The panel's perDay (snap 194) already carries SIX dates 06-29→07-04 — 07-03's 8 entries are already scored (−$51.02; 07-03 is 29/45 graded, lag not loss) and 8 early-settled 07-04-target entries are in (−$21.39, TP/SL exits before resolution). **⇒ Day 7 = the first 07-05-targeted position SETTLING, expected mid-day 07-04 UTC** (those markets list ~09:00–15:00Z, enter at first tick, settle via TP/SL fills or later time-stop) — i.e. AFTER the token reset; **the morning session reads the label** (playbook stands: KILL pre-authorized, PASS stage-only). **Prod green:** 00:35Z tick ok 116.1 s/73 mkts/0 cityErrors · capture 10 s/78 ins · convergence 50.7 s/0 errors. **CI green on all 8 night pushes** (incl. the web build) — Vercel deploying the same tree. **WIND-DOWN executed early per the v6 clause** (all lanes integrated; no actionable work before ~04:00Z; the label mathematically cannot render tonight): loop STOPPED, no further wakeup — the gate deadmen page on real prod problems. Operator morning items: (1) apply staged `0079_maker_exit_assumptions_history.sql` + `0080_dash_city_forecast.sql` (read-only dash RPCs, no fn redeploys — /maker-exit sparklines + /paper-trade intended-bet tiles light up); (2) 10:00Z #31 ankara/houston first-forward-day + °F verification; (3) the gate label (~mid-day); (4) N6 nav re-added /, /efficiency, /rewards per the slate brief (overriding the 06-29 trim) — revert via DASH_NAV if unwanted.
- **C30 (~02:25 local / 00:25Z 07-04 — NIGHT-BUILD SLATE COMPLETE: 6/6 lanes INTEGRATED + PUSHED in ~90 min):** N3 + N2 landed on their zero-defect lens verdicts. **N3 `95af562`:** dash_maker_exit_history RPC **STAGED-DARK as 0079** (lens n3-sql-lens: ZERO defects, 2 informational LOWs accepted — dead-code defensive coalesce + grants asserted in the 0034 lockdown test per idiom) + 6-metric small-multiple sparklines above tile #4 (fill-rate line carries 0.30 warning + 0.49 backtest refs; honest-null points, never fabricated zeros; retention note: 200 snapshots ≈ 2 days @ */15 — short-horizon gate-day instrumentation by design). **N2 `62071f4`:** dash_city_forecast RPC **RENUMBERED 0079→0080** (collision with N3; filename + manifest + all comment refs swept) + the /paper-trade IntendedBetTile (lens n2-sql-lens: ZERO defects, 3 LOWs adjudicated — (1) forecast-center-vs-floor gap DISCLOSED on the tile, accepted-as-designed; (2) active_until tz-boundary ≤1-day display edge, self-correcting, accepted; (3) "freshest"-wording nit FIXED at merge — the mean is a plain avg over all of today's lead-1 captures, mirroring the tick). Hand-merged the migrations.test.ts manifest + loaders.ts overlaps (N3 landed first). **Final board: main == origin @ `62071f4`; suite 2009 → 2080 green / 144 files; typecheck clean on every one of the 7 night commits (5844489 · 3dfc087 · 7974af3 · 735db1a · 0eb9f8f · 95af562 · 62071f4); Vercel deploying.** TWO MIGRATIONS STAGED-DARK AWAITING THE OPERATOR: `0079_maker_exit_assumptions_history.sql` + `0080_dash_city_forecast.sql` (both read-only SECURITY DEFINER dash RPCs, no fn redeploys needed; UIs null-guarded → /maker-exit sparklines + /paper-trade intended-bet tiles light up on apply). All six worktrees removed. Loop transitions to GATE-WATCH (last read INSUFFICIENT 73/34/6-of-7 @ 23:36Z snap 190; next read post-00:35Z tick); wind-down ~04:00Z; #31 (10:00Z ankara/houston °F) owed to the morning session.
- **C29 (~02:12 local / 00:12Z 07-04 — NIGHT-BUILD integration wave 1: FOUR of six lanes MERGED+PUSHED):** All six lanes reported within ~20 min of dispatch (opus agents). Integrated serially, suite+typecheck green on every merged tree: **N4 README `5844489`** (121-line public product README, numbers verified verbatim vs FINDINGS.md, all 11 listed routes confirmed to exist, secrets audit clean; test-floor line bumped to 2,000+) · **walker fix `3dfc087`** (the emos-only bias-subtraction repo-walk test descended into `.claude/worktrees/` and failed main's suite on N2's *uncommitted* worktree state — added `.claude` to skipDirs; each worktree's own suite run still polices itself) · **N6 DashNav `7974af3`** (shared compact nav, all ten surfaces + INCLUDE_SIGNALS toggle; zero per-page drift found — the `.ams-dash` idiom had already propagated; NOTE for the operator: re-adds /, /efficiency, /rewards to nav per the slate brief, overriding the 2026-06-29 trim comment — trivially reverted by editing DASH_NAV) · **N1 /signals `735db1a`** (487-line static signals-findings asset + 32 golden tests + 360-line flagship page + live 12th-signal row via dash_maker_exit; hero "twelve ways" = 10 signals + hardening sweep + the flat-open Phase-0.5 NO-GO 0/325, with the maker-exit variant honestly shown UNDER FORWARD TEST; layout conflict vs N6 resolved via the designed INCLUDE_SIGNALS flip, /signals placed after /efficiency) · **N5 grading-lag `0eb9f8f`** (208-line read-only ops script + 5 PGlite tests + the playbook package-3 pointer applied, incl. the upper-bound caveat on newDaysAtGrading). Suite 2009 → **2052 green** (140 files), typecheck clean throughout. **REMAINING: N2 + N3 — BOTH staged a migration numbered `0079` (collision: N2 dash_city_forecast / N3 maker_exit_assumptions_history) → the second to integrate is renumbered 0080 (+ manifest + twin refs). Both stage-dark; two opus SQL lenses in flight (n2-sql-lens / n3-sql-lens).** Gate unread this cycle (between :35 ticks); next read after 00:36Z.
- **C28 (~01:50 local / 23:50Z 07-03 — v5 cycle 1 → NIGHT-BUILD pivot: #34 VERIFIED-DONE with the first WHY-zero values; gate 6/7 unchanged; N1–N6 dispatched on OPUS):** **#34 verification (one-time, CLOSED):** 23:35Z tick ok / 96.3 s / 73 mkts / 0 cityErrors / snapshot 193; `job_runs.stats.dominantDisqualifier='none'` present (the 22:35Z pre-deploy tick correctly null). **First observed WHY-zero values (snapshot 193 assumptions):** qualifyingTickFrac **0** (0 of 1 732 resting ticks), dominantDisqualifier **`none`**, fracFailsMinSize **0**, meanDistFromMidPp **null**, fracWithinAdvertisedBand **null**. The nulls are the documented honest-NaN case (ROLLOUT.md v2 §denominators: both band fields divide by *mid-known* resting ticks, which is **0 of 1 732** — not one resting tick carried a two-sided prior mid). That is STRONGER than the pre-registered "high in-band + low fail-min-size + dominant none" signature: the band axis isn't merely passing, it is *incomputable* because the book is one-sided at rest ⇒ the mid<0.10 strict two-sided regime is the blocker, exactly ROLLOUT.md v2's documented reading of the live 0/1,732. Corroborating: spreads ≈2.06/2.07 ¢, makerFillLatency 29.7 ticks, makerFillRate 0.082. The 1b reward term stays bounded at ZERO live income (trend 0→0→0→0 across 19:35/20:35/22:35/23:35Z). **GATE (23:36Z snap 190): INSUFFICIENT — 73 mkts / 34 cities / 6 of 7 days**; net −$160.05, fillRate 0.082 vs 0.49 backtest, rebate $0, 0 open. 7th day = 07-03 grading; read each :35 tick + ~1 min. **Prod green:** capture 7.5 s/62 ins · convergence 26 s/0 err/34 mkts/snap 383 · city-paper-trade 07-03 10:00Z ok 3.4 s (the 07-02 gap = the diagnosed silent pause, already backfilled). **OPERATOR (two in-session messages):** (1) Sonnet-5↔Opus cost parity confirmed → **subagents default to OPUS from now on** (Fable stays orchestration/planning/complex); (2) overnight blanket — commit+push authorized, "do what you need, boil the ocean" → **NIGHT-BUILD slate EXECUTED under this loop** (v6 semantics adopted verbatim: dispatch N1–N6 from the committed briefs, integrate serially with suite+typecheck per merge, push [Vercel]; migrations stage-dark ONLY; wind-down ~04:00Z). Six lanes dispatched (opus, worktrees, no-commit/no-DB-write). #31 stays owed to the morning session (10:00Z). Next wake 1500 s.
- **C27 (~01:20 local / 23:20Z — evening slate 5/5 INTEGRATED; NIGHT-BUILD handoff written):** cityscan-page landed (`c2893e4`): §12-appendix data verbatim (6 consistency flags recorded, incl. correcting the "monotone" phrasing — collapse is 14h→19h, 9h–13h is a flat shelf) + 31 golden/consistency tests + the SVG 11-bar hero + **the operator's current-bet box** (bidding date + predicted temp per city from the EXISTING dash_city_sim payload — predictedC IS predicted_native per 0070; "bidding now"/"latest bet" states) + **all absolute times Europe/Stockholm** (per-date dual-IANA, DST-proven: Houston 14:00→21:00 CEST, Singapore 11:00→05:00 CEST) + °F label fix. Combined tree **138 files / 2009 tests green**. KNOWN GAP (operator's call): pre-placement forecasts pre-10:00Z don't exist in the payload → NIGHT-BUILD lane N2. Operator going offline; token reset ~04:10Z; directive = maximum build until then → **NIGHT-BUILD slate N1–N6 written above + the v6 loop prompt; session /remember + /clear handoff.** Open at handoff: #34 one-time v2 tick verification (23:35Z+ — v6 cycle 1a), #30 gate label (6/7 days; KILL pre-authorized, PASS stage-only), #31 at 10:00Z (morning session).
- **C26 (~00:55 local / 22:53Z — reward-instr v2 MERGED + PUSHED + DEPLOYED; verification = the 23:35Z tick):** Both lenses ZERO gate-math/correctness defects (Lens A walked all 5 settle() sites incl. the v1-bug post-break loop — threaded symmetrically; Lens B verified jsonb additivity end-to-end + old-snapshot em-dash handling). Batched fix pass (1 LOW tie-break → strict-majority both axes + 2 boundary tests that FAIL against the old code · 1 MEDIUM defaults-caveat → ROLLOUT.md v2 section incl. **the documented reading of the live 0/1,732: the mid<0.10 strict two-sided regime zeroes one-sided quotes** + tile WHY-line defaults caption) → zero outstanding. Merged tree **136 files / 1975 tests green** + typecheck clean → commit `5a38f6d` pushed → **maker-exit-panel DEPLOYED** (authed CLI). Verify next: 23:35Z tick's job_runs.stats carries `dominantDisqualifier` (+ the WHY fields land in snapshot assumptions). Remaining lanes: cityscan-page (#35+#37 — appendix null-fill + F° + operator current-bet box + Stockholm times) in flight; task #31 (10:00Z) + gate watch (last read 6/7 days) continue.
- **C25 (~00:45 local 07-04 / 22:44Z — three lanes landed: cs-harden + f-unit-test MERGED+PUSHED; reward-instr-2 → 2 lenses; cityscan-page → data-appendix resume):** **f-unit-test (#33) DONE, merged `44e4603`:** NO °F defect — 0070's convert-then-round path verified end-to-end; 28 regression tests (17 core KHOU 2°F-ladder fixtures incl. the x.5°F bucket-PAIR boundary + 11 PGlite twin: full °F place→grade→settle round trip); houston's 10:00Z tick de-risked. **cs-harden (#32) DONE, merged `7efedac`:** lens 6/7 zero-defect + 1 MEDIUM (mode-banner mislabel on mixed flag combos) fixed via ONE corrective re-brief (`describeMode()` from effective toggles, all 4 combos verified live); `--legacy` bit-for-bit; hardened default: munich/16h + b-aires/14h become INELIGIBLE (findings-1×3 compounding), **candidate set UNCHANGED {ankara/14h, houston/14h}**. **reward-instr-2 REPORTED** (7 files, no migration, 1954/135 green, gate-math-untouched claim; KEY: the 'none'-case test reproduces the live 0/1,732 symptom — within-band + above-min-size but mid<0.10 ⇒ the strict two-sided regime zeroes one-sided quotes — the concrete why-zero hypothesis) → engine-adjacent, **2 lenses dispatched** (ri2-lens-a engine math / ri2-lens-b semantics+surfaces). **cityscan-page REPORTED** (correctly refused to fabricate the 9 unpreserved curve rows) → §12 **Data appendix appended** (full pooled curve + terciles + per-cell top-5 from the two bit-identical runs, C16/C17 source) + agent resumed to fill nulls from the canonical doc + the F° label fix. Gate probe skipped this cycle (22:35Z tick mid-blackout); next wake reads it.
- **C24 (~00:25 local 07-04 / 22:24Z 07-03 — gate-drafts INTEGRATED · cs-harden REPORTED → lens dispatched):** **gate-drafts (#36) DONE:** GATE-DAY-PLAYBOOK.md reviewed (2 fixes: re-open bar ALL-of-3 → necessary-#1+strengtheners; /convergence scope caution on the deactivation checklist), committed+pushed `3365a06`. Its best catch: §9R's B/C entry/exit rules were locked for the DEAD flat-open thesis — any PASS-day sizing must reconcile the maker-exit config first. **cs-harden reported:** one-file diff (+224/−32); `--legacy` reproduces the recorded run BIT-FOR-BIT; hardened default excludes 296 fallback bets + 244 ineligible cells → munich/16h + buenos-aires/14h become INELIGIBLE (fallback removal drops n 10→9 — a findings-1×3 compounding interaction), houston LB +3.1→+4.5pp exactly matching city-scan-2's independent counterfactual; **locked-bar candidate set UNCHANGED {ankara/14h, houston/14h}** = what's enrolled. Selection-affecting → single lens dispatched (`cs-harden-lens`) before merge. Still in flight: f-unit-test · reward-instr-2 · cityscan-page.
- **C23 (~23:10 local / 21:08Z — operator token-spend directive → 5 parallel build lanes launched):** All non-signal work (backlog stays closed): **(1) cs-harden** — city-scan forward-tool hardening per the §12 review record (eligibility floor + fallback exclusion + ask-gate honesty; `--legacy` must reproduce the recorded run bit-for-bit; verdict NOT revised); **(2) f-unit-test** — °F fixture tests proving 0070's unit-agnostic claim BEFORE houston's first live tick (any real defect = urgent pre-10:00Z integrate); **(3) reward-instr-2** — the "why-zero" pool-context diagnostic (dist-from-band/min-size/dominant disqualifier; additive-only, gate math byte-identical, v1 discipline: 2 lenses → merge → deploy → :35 verify); **(4) cityscan-page** — /paper-trade "45-City Scan" section from a committed static asset (§12 numbers copied not recomputed, caveats rendered); **(5) gate-drafts** — docs/ops/GATE-DAY-PLAYBOOK.md (KILL package pre-authorized-to-record; PASS package stage-only with the capital boundary absolute; INSUFFICIENT check). Tasks #32–36. All code lanes in worktrees; zero DB writes; integration serial with lenses where engine-adjacent. Gate watch continues in parallel (6/7 days at C22).
- **C22 (~23:00 local / 21:00Z — v5 cycle 1, quiet):** GATE unchanged: INSUFFICIENT 73/34/**6 of 7 days** (07-03 grading pending — the 7th day). `qualifyingTickFrac` trend **0 → 0** (19:35Z, 20:35Z ticks) — the 1b reward term stays bounded at zero. Prod green: maker-exit 20:35Z 94 s/0 errors/73 mkts; 21:00Z capture+convergence caught mid-run (~30 s in, normal). #31 waits for the 07-04 10:00Z tick. Next wake 3600 s.
- **C21 (~22:15 local / 20:14Z — OPERATOR BLANKET APPROVAL → ENROLLMENT EXECUTED + v5 written+armed):** Operator: "approval for any decision — move forward — write the loop prompt for next build." Executed the reasonable set: §12 staged SQL applied VERBATIM (verified 4 active `city_sim_config` rows: +ankara LTAC, +houston KHOU, arms 11–16, fmh 14, thru 07-31) + `city-sim.ts` backfill (ankara 126/121 graded, houston 125/120, 06-12→07-03 — same shape as incumbents; daily 10:00Z cron auto-picks-up). **Confirmation-clock rule recorded in §12: backfill is IN-SAMPLE vs the scan — candidate confirmation = target_date ≥ 07-04 ONLY.** Pre-authorization scope recorded: gate KILL → record without re-asking; gate PASS → stage only (capital/wallet/keys operator-physical, never pre-authorized); INSUFFICIENT → accrue. v5 loop prompt written above + started (overnight watch cadence). Houston = first °F city — first-graded-day check is task #31.
- **C20 (~21:58 local / 19:57Z — ITEM 12 ADJUDICATED + RECORDED; loop stop conditions met):** city-scan-2's contamination-scope report closed the last blocking question: all 296 fallback bets confirmed look-ahead (296/296 empirical, 0 anomalies) but **100 % TRAIN-confined — the TEST holdout is CLEAN** — and every touched top-5 cell IMPROVES without its (losing) fallback bet → candidate set INSENSITIVE. **FINAL: 2 enrollment candidates (ankara/14h, houston/14h) vs the locked bars; pooled negative every hour.** Recorded: SIGNAL-BACKLOG §12 verdict block (with all 3 lens findings + staged enrollment SQL: LTAC + KHOU, arms {11..16}, fmh 14, active_until 07-31; houston = first °F city, unit-agnostic bucketing note) + FINDINGS.md item-12 row + this tick. Skip-by-arm distribution confirms the 0.95-gate binds only late (3 @9h → 500 @19h). Loop STOP: deploy verified (C19) + item 12 recorded + gate still 6/7 days with nothing else open — the deadmen + POST-FABLE-HANDOFF.md carry the watch. Operator pending: (1) enrollment decision, (2) gate label at sufficiency ~07-04→05, (3) /maker-exit tile #4 one-glance visual check.
- **C19 (~21:47 local / 19:46Z — REWARD-INSTR DEPLOY VERIFIED, one-time check DONE):** 19:35Z tick ok/93 s/0 cityErrors/73 mkts/snapshot 189 AND `job_runs.stats.qualifyingTickFrac` present. **First observed `qualifyingTickFrac` = 0** (0 of 1 732 resting ticks qualified; also live now: meanObservedEntry/ExitSpread ≈ 2.06/2.07 ¢, meanMakerFillLatency 29.7 ticks). **Read: the 1b reward-stacking term is bounded at ≈ ZERO live income at current conditions** — the backtest's ciLow lift (+0.25 %→+2.38 % @ 0.05 share) rested on qualifying-band residency the live loop isn't getting; exactly what the instrument was built to measure before the share question opened. Re-read as days accrue. Vercel production READY on `add950e` (19:17Z). Tile #4 visual render auth-gated (/login) — data contract verified in `maker_exit_panel.view.assumptions` + component under the 1937-green suite; operator one-glance check outstanding. Still in flight: city-scan-2 contamination-scope follow-up (nudged 19:43Z).
- **C18 (~21:37 local / 19:37Z, event-driven — city-scan-lens reported; ADJUDICATED):** Lens: 1 real latent defect + 1 doc-overclaim + 1 genuine gap; 8 load-bearing paths CONFIRMED CLEAN (strict-before recovery, split hygiene on targetDate, LB reuse, argmax tie-break + DST, bucket index-space identity incl. the W6 label-alignment risk, clusterMeanTCi on weather-day, consistency identity, fee math). Adjudication: **(1) missing entry-watch eligibility floor in rankTrainCells — CONFIRMED latent, NOT behavior-changing here** (replication printed eligibility: all top-5 cells n=10–11, `eligible:true`; no small-n cell outranked them) → hardening note. **(2) MAX_ENTRY_ASK 0.95 gate is NOT in the live city-sim path — confirmed deviation from the "mirrors live" docstring claim**; binds at late arms; pooled-curve caveat, candidates (14h) insensitive. **(3) frozen-seed fallback = look-ahead BY CONSTRUCTION** (fallback ⇒ no pre-tick build ⇒ frozen seed = first-ever build ⇒ postdates the bet) — the lens's "spot-check" is actually deterministic; ALL 296 fallback bets (4.1 %) contaminated. Blocking question = scope: dispatched a targeted follow-up to the warm runner (fallback distribution by arm/cell; top-5 cells touched? with/without recompute if so; local compute + at most the same cheap pull re-run, blackout-guarded). Verdict recording gated on that scope read. 19:35Z tick running now (blackout) — deploy verification next wake.
- **C17 (~21:36 local / 19:35Z, event-driven — city-scan-2 reported before the stand-down landed):** Accidental but valuable: an INDEPENDENT execution of the same script reproduced the original runner's numbers exactly (7 262 bets / 2 022 skips; identical top-20 TRAIN, top-5 TEST, pooled curve, terciles; same 10 909-row pull, blackout-guarded 19:27:13Z, 16.7 s) + a second zero-deviation spec-conformance read with file:line evidence + one new fact: all top-5 TRAIN cells are `eligible:true` under entry-watch's minGraded=10 (n=10–11 TRAIN). Candidate arithmetic unchanged: ankara/14h + houston/14h clear the locked bar; munich/16h + buenos-aires/14h fail TEST net; helsinki/15h fails TRAIN LB. Recording still gated on the adversarial `city-scan-lens` (in flight — checks the original runner's OWN claims can't verify: look-ahead strictness, bucket index-space mapping, split hygiene in the callee code).
- **C16 (~21:33 local / 19:32Z, event-driven — the ORIGINAL city-scan runner reported):** The prior session's runner did NOT die — its full report arrived via mailbox (built + typechecked clean + ran in ~10 s; footprint exactly one untracked file; 7 262 bets / 2 022 skips over 9 284 cells; 95.9 % genuine point-in-time DB-recovered forecasts, 4.1 % frozen-seed fallback). Redundant `city-scan-2` stood down. **PRELIMINARY adjudication vs the locked §12 bars: TWO candidates — ankara/14h (TRAIN LB +3.6 pp, TEST +$44.88) + houston/14h (LB +3.1 pp, TEST +$12.04);** munich/16h + buenos-aires/14h fail TEST net, helsinki/15h fails TRAIN LB. Pooled curve negative at EVERY hour (mechanism-A prior confirmed); all TEST CIs straddle 0 (n=7–8) — the scan SELECTS, the live loop CONFIRMS. Candidates emerged → the pre-committed review lens is REQUIRED: dispatched `city-scan-lens` (sonnet, background) on the script's load-bearing paths (look-ahead, split hygiene, LB reuse, P&L/index mapping, join key). Verdict recording + enrollment staging deferred to lens completion. Deploy verification (19:35Z tick) at next wake.
- **C15 (~21:30 local / 19:29Z — v4 loop re-entered after session restart):** Board reconciled: main == origin @ `add950e` (v4 prompt committed; reward-instr merged/deployed/pushed at C14). CITY-SCAN: NO §12 verdict block (the prior runner died with its session; a complete-looking 696-line `scripts/research/city-scan.ts` left untracked) → re-dispatched `city-scan-2` (sonnet, background) to validate-vs-spec then run, blackout-guarded, stop-on-2-identical-failures. Prod green: capture 30–43 s/73 inserts · maker-exit 18:35Z tick 101 s/0 cityErrors/73 mkts (pre-deploy tick — no `qualifyingTickFrac` yet; first eligible 19:35Z) · convergence 25–45 s/0 errors. **GATE (18:36Z snapshot): INSUFFICIENT — 73 mkts / 34 cities / 6 of 7 days — only day 7 missing; label renders when 07-03 markets grade.** Net −$160.05, `makerFillRate` 0.082 (vs 0.49 backtest — KILL-path watch), rebate $0, 0 open. Next wake ~19:54Z: verify the 19:35Z post-deploy tick + runner progress.
- **C14 (~21:20 local, operator-authorized commit/deploy/push):** reward-instr build INTEGRATED — 2 lenses ran: lens B 1 LOW (stale-snapshot tile counts, fixed `31fbe51`) · lens A 1 MEDIUM (resting-tick undercount on the no-bid time-stop break path, REPRODUCED, fixed `6662b56` with a stash-and-rerun falsification proof + 3 tests, then CONFIRMED-FIXED by lens A with full boundary tracing) → zero outstanding. FF-merged to main (`4669c5c`→`6662b56`), suite 135/1937 green + typecheck clean on merged tree. Deploying maker-exit-panel + pushing main (Vercel) per operator authorization; verification = the next :35 tick carries `qualifyingTickFrac`. Also in flight: `city-scan` (SIGNAL-BACKLOG item 12, pre-registered — all-45-city historical city-sim replay, TRAIN-select/TEST-confirm).
- **C13 (~19:50 local — FINAL, loop STOPPED):** item 11 ACCEPTED + **ADJUDICATED: KILL** (correction harmful OOS: TEST MAE +0.0159 °C worse, day-clustered CI [−0.0280,−0.0051], R² −6.11 %; the feature leak favored the model and it lost anyway; lens waived — prior-consistent negative). **WS-3 CLOSED — all verdicts recorded: 1b gate-PASS(share unmeasured→forward option) · 2/4/5/6/7/9/11 KILL · 3 NO-PASS(hardening revoked) · 10 INSUFFICIENT(structural).** BUILD-STATE evening addendum written; /remember written; loop ended per the stop rule (no lane has work; the deadmen watch prod; the gate accrues ~07-08→12; POST-FABLE-HANDOFF.md carries adjudication). Uncommitted-tree inventory in the BUILD-STATE addendum — commit is the operator's call.
- **C12 (~19:30 local, event-driven — replacement lens reported):** item3-hardening.ts review = **ZERO DEFECTS**, all 5 checks verified with file:line evidence + full bit-for-bit reproduction (3rd independent confirmation of the pooled numbers). **Item 3 FINAL: naive gate-PASS REVOKED → NO-PASS / INSUFFICIENT_DATA at the day grain** (3 weather-day clusters; perm false-PASS 17.3%; and Q4's 29 station-days fails the strict "≥30 station-days" prose bar outright — bet-count/station-day ambiguity named in the record). Re-open: ≥10 distinct Q4-carrying weather-days. #27 done. Sole remaining lane: item 11 (nonlinear residual, in flight). **All 9 WS-3 verdicts now recorded: 1b gate-PASS(fwd caveat) · 2/4/5/6 KILL · 3 NO-PASS · 7/9 KILL · 10 INSUFFICIENT_DATA.**
- **C11 (~19:25 local):** Last discretionary lane LAUNCHED — the crosscheck's nonlinear-ML residual check **pre-registered as SIGNAL-BACKLOG item 11** (boosted stumps pure-TS/no-deps · fixed hyperparams · l3b feature set · gate: TEST-MAE delta day-clustered CI excl 0, ≥30 days/≥30 stations; low prior per linear R²=0.60%) → item5-rerun. In flight: item-3 lens (ws3c-runner) · item 11. All other WS-3 verdicts recorded.
- **C10 (~19:20 local, event-driven — item 10 reported):** ACCEPTED (exact locked design, no improvisation, one-file footprint). **Item 10 ADJUDICATED: INSUFFICIENT_DATA, structural** — 10Z/22Z rows begin 06-13 (17 d after the split) → TRAIN 0 deltas, 0 shocks; plus >30 d snapshot downsampling makes retrospective 20-min ask recovery impossible for any old-enough window. CLOSED (re-open = ≥30 d accrued pairs + forward ask-capture design; 3 latency nulls say don't build it). #24 done. Prod probe (17:07Z): all green, maker-exit 16:35Z tick 95.7 s/0 errors/65 mkts — 2nd consecutive clean fast tick. Remaining: item-3 lens (reassigned to ws3c-runner after item3-lens went silent) → final item-3 verdict → #25 decision → wrap.
- **C9 (~19:50 local, event-driven — ws3c-runner reported item 6):** ACCEPTED (one new file, blackout respected incl. a self-imposed wait at the :32 edge, self-test bug disclosed+fixed pre-DB). **Item 6 ADJUDICATED: KILL, well-powered null** — n=568/44 cities, edge +0.80pp CI [−1.74,+3.34], day-clustered [−2.14,+3.61]; no sibling-resolution lag (3rd latency-family confirmation). Lens waived (prior-consistent null, no behavior change). #23 done. Remaining: item 10 (running, resumed :43) · item3-lens (report pending, pinged). Then: #25 decision + wrap.
- **C8 (~18:30 local, event-driven — item3-hardening reported):** deliverable ACCEPTED (one new file; duplicate walk matches official runScan() bit-for-bit ×4 quartiles; clusterMeanTCi reused verbatim; seed 20260703). **Hardening numbers fail BOTH survive-rule prongs:** Q4's 104 bets = 29 station-days on **3 distinct weather-days** → day-clustered CI [−7.86,+23.09] n=3; station-clustered [−0.43,+13.62]; perm false-PASS **17.3%**, P(mean≥obs) 6.85%; armEdgeStats confirmed per-bet i.i.d. (stats.ts:195). Verdict NOT finalized — the new script's outputs are load-bearing this time, so the pre-committed review lens runs first (`item3-lens`, dispatched). Pending lens: downgrade item 3 to NO-PASS / INSUFFICIENT_DATA-at-day-grain (clustering artifact, cross-venue-false-PASS pattern repeated).
- **C7 (~19:15 local):** 3e LAUNCHED — item 10 design **pre-registered in SIGNAL-BACKLOG §10 before measurement** (per-station TRAIN-P90 shock on |ΔblendedMu| between consecutive builds · post-build calibratedP vs first ask ≥B_k+20 min · m≥+5pp @ ask≤0.60 · ≤B_k+2 h · gate n≥40/≥6 cities/CI>0; expect KILL per WO-4/WO-5) → assigned to idle item5-rerun (one new file cap). All WS-3 lanes now adjudicated or in flight: hardening(3) · item6 · item10 running; 1b/2/4/5/7/9 recorded.
- **C6 (~19:05 local, event-driven — item5-rerun reported):** deliverable ACCEPTED (one-file diff as capped, defaults byte-identical, typecheck clean, WS-2 baseline artifact untouched). **Item 5 ADJUDICATED: KILL** — basket @ chw=1: mean 6.81→3.78%, ciLow +0.25→−3.23%, LOCO 45/45 / LODO 21/21 (vs 15/45, 9/21); chw=1 control shows widening alone hurts (4.61%) — the pre-named dilution branch. Review lens on the harness diff waived (non-load-bearing: sim prong through unmodified engine decides; harness CIs corroborate to 4 dp). FINDINGS + backlog recorded; #26 done. Note: `jackknife-maker-exit.ts` mod in main tree is MINE (CLI ext), not the parallel agent's. In flight: item3-hardening · ws3c-runner(item 6). WS-3 scorecard so far: 1b gate-PASS(fwd-assumption caveat) · 2 KILL · 3 PROVISIONAL-PASS(hardening) · 4 KILL · 5 KILL · 6 running · 7/9 KILL (earlier).
- **C5 (~18:55 local):** 3d LAUNCHED — item 6 design **pre-registered in SIGNAL-BACKLOG §6 before measurement** (R_N sibling-resolution event · pre-R_N snapshot only · m ≥ +5pp @ ask ≤ 0.60 · entry ≤ R_N+2 h · gate n≥40/≥6 cities/CI>0; expect KILL) → assigned to idle ws3c-runner (one new file cap). Three runners in flight: item5-rerun · item3-hardening · ws3c-runner(item 6).
- **C4 (~18:45 local, event-driven — ws3c-runner reported):** 3c ACCEPTED (28 s run — the "heavy pull" fear was overblown for this dataset) + ADJUDICATED: **item 2 KILL** (+2.91pp, CI [−3.21,+9.03] straddles 0, n=84) · **item 3 gate-PASS PROVISIONAL** (Q4 +7.47pp CI [+1.06,+13.87] n=104, Q1–Q3 null as pre-registered, monotone-ish trend) → hardening dispatched (`item3-hardening`: day+station-clustered CIs, ≥2000-perm zero-skill; survive-rule pre-stated in task #27) · **item 4 KILL sign-reversed** (−1.73pp CI [−2.77,−0.69] — market OVERPRICES extreme-day tails). FINDINGS 3 rows + backlog 3 blocks recorded. #22 done. In flight: item5-rerun, item3-hardening. Next after both: 3d (item 6, threshold pre-registration first).
- **C3 (~18:25–18:35 local, event-driven — ws3b-runner reported):** Deliverable ACCEPTED (zero file mods, raw-only, file:line evidence). **1b ADJUDICATED: gate-PASS** — full ciLow +0.25%→+2.38% at the 0.05 share floor (linear in share; TRAIN+TEST PASS from 0.10); caveat: pool share UNMEASURED, $67/day derived average — recorded in FINDINGS.md + SIGNAL-BACKLOG. **Item 5: first measurement VACUOUS** (chw=0 ⇒ 1 candidate ⇒ basket ≡ single, byte-identical; jackknife harness unwired) — NOT a KILL; amended spec (basket 2/3 @ chw=1, gate unchanged) pre-registered in SIGNAL-BACKLOG BEFORE re-measurement. Dispatched `item5-rerun` (harness wiring + local runs; one-file diff cap) + `ws3c-runner` (items 2–4 ONE pull, blackout-guarded, stop-on-2-identical-failures). Tasks: #21 done, #22 in_progress, #26 new.
- **C2 (18:05–18:15 local / 16:05–16:15Z):** Prod green + improving — the 15:35Z maker-exit tick ran **94 s / cityErrors 0 / 65 mkts** (vs 264–378 s pre-0077); capture 34–58 s; convergence 22–36 s/0 errors. `makerFillRate` 0.092 (watch continues). New file `EXTERNAL-AGENT-CROSSCHECK.md` (parallel thread, research-only, no code): third-party AI report crosschecked → **nothing changes priorities**; one open candidate (nonlinear-ML residual check) boarded as idle-lane task #25. **3b takeover triggered** — no parallel-agent progress since 15:06 (>3 h): dispatched own sonnet runner (`ws3b-runner`, background, main tree, read-only, from-cache local compute) for 1b reward sweep {0.05,0.10,0.25,0.50} + basket 1/2/3 + jackknife. Task #21 in_progress.
- **C1 (17:30–17:40 local / 15:30–15:40Z):** Prod green — capture 35–51 s/85–88 inserts · convergence-panel 23–27 s/0 cityErrors · maker-exit :35 ticks ok (264–378 s). The off-schedule 14:59:50Z+15:03:04Z maker-exit runs = the WS-1 rollout verification ticks (scope 58/43 mkts matches the outcome block) — not an anomaly. **Watch:** live `makerFillRate` 0.10–0.12 vs backtest 0.49 (POST-FABLE assumption #1; <0.30 flagged as the likely KILL path) — note-and-watch, the gate accrues. 7+9 KILL rows confirmed recorded in FINDINGS.md (b31917e). WS-3 board → TaskList #21–#24; 3b/3c stay the parallel agent's (last progress 15:08 local; takeover threshold 18:08). Next wake ~18:10: check parallel-agent progress → dispatch own 3b runner if none.
